// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"delivrio.io/print-client/ent/predicate"
	"delivrio.io/print-client/ent/uniquecomputer"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UniqueComputerQuery is the builder for querying UniqueComputer entities.
type UniqueComputerQuery struct {
	config
	ctx        *QueryContext
	order      []uniquecomputer.OrderOption
	inters     []Interceptor
	predicates []predicate.UniqueComputer
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UniqueComputerQuery builder.
func (ucq *UniqueComputerQuery) Where(ps ...predicate.UniqueComputer) *UniqueComputerQuery {
	ucq.predicates = append(ucq.predicates, ps...)
	return ucq
}

// Limit the number of records to be returned by this query.
func (ucq *UniqueComputerQuery) Limit(limit int) *UniqueComputerQuery {
	ucq.ctx.Limit = &limit
	return ucq
}

// Offset to start from.
func (ucq *UniqueComputerQuery) Offset(offset int) *UniqueComputerQuery {
	ucq.ctx.Offset = &offset
	return ucq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ucq *UniqueComputerQuery) Unique(unique bool) *UniqueComputerQuery {
	ucq.ctx.Unique = &unique
	return ucq
}

// Order specifies how the records should be ordered.
func (ucq *UniqueComputerQuery) Order(o ...uniquecomputer.OrderOption) *UniqueComputerQuery {
	ucq.order = append(ucq.order, o...)
	return ucq
}

// First returns the first UniqueComputer entity from the query.
// Returns a *NotFoundError when no UniqueComputer was found.
func (ucq *UniqueComputerQuery) First(ctx context.Context) (*UniqueComputer, error) {
	nodes, err := ucq.Limit(1).All(setContextOp(ctx, ucq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{uniquecomputer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ucq *UniqueComputerQuery) FirstX(ctx context.Context) *UniqueComputer {
	node, err := ucq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UniqueComputer ID from the query.
// Returns a *NotFoundError when no UniqueComputer ID was found.
func (ucq *UniqueComputerQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = ucq.Limit(1).IDs(setContextOp(ctx, ucq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{uniquecomputer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ucq *UniqueComputerQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := ucq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UniqueComputer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UniqueComputer entity is found.
// Returns a *NotFoundError when no UniqueComputer entities are found.
func (ucq *UniqueComputerQuery) Only(ctx context.Context) (*UniqueComputer, error) {
	nodes, err := ucq.Limit(2).All(setContextOp(ctx, ucq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{uniquecomputer.Label}
	default:
		return nil, &NotSingularError{uniquecomputer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ucq *UniqueComputerQuery) OnlyX(ctx context.Context) *UniqueComputer {
	node, err := ucq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UniqueComputer ID in the query.
// Returns a *NotSingularError when more than one UniqueComputer ID is found.
// Returns a *NotFoundError when no entities are found.
func (ucq *UniqueComputerQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = ucq.Limit(2).IDs(setContextOp(ctx, ucq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{uniquecomputer.Label}
	default:
		err = &NotSingularError{uniquecomputer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ucq *UniqueComputerQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := ucq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UniqueComputers.
func (ucq *UniqueComputerQuery) All(ctx context.Context) ([]*UniqueComputer, error) {
	ctx = setContextOp(ctx, ucq.ctx, ent.OpQueryAll)
	if err := ucq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UniqueComputer, *UniqueComputerQuery]()
	return withInterceptors[[]*UniqueComputer](ctx, ucq, qr, ucq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ucq *UniqueComputerQuery) AllX(ctx context.Context) []*UniqueComputer {
	nodes, err := ucq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UniqueComputer IDs.
func (ucq *UniqueComputerQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if ucq.ctx.Unique == nil && ucq.path != nil {
		ucq.Unique(true)
	}
	ctx = setContextOp(ctx, ucq.ctx, ent.OpQueryIDs)
	if err = ucq.Select(uniquecomputer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ucq *UniqueComputerQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := ucq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ucq *UniqueComputerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ucq.ctx, ent.OpQueryCount)
	if err := ucq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ucq, querierCount[*UniqueComputerQuery](), ucq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ucq *UniqueComputerQuery) CountX(ctx context.Context) int {
	count, err := ucq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ucq *UniqueComputerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ucq.ctx, ent.OpQueryExist)
	switch _, err := ucq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ucq *UniqueComputerQuery) ExistX(ctx context.Context) bool {
	exist, err := ucq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UniqueComputerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ucq *UniqueComputerQuery) Clone() *UniqueComputerQuery {
	if ucq == nil {
		return nil
	}
	return &UniqueComputerQuery{
		config:     ucq.config,
		ctx:        ucq.ctx.Clone(),
		order:      append([]uniquecomputer.OrderOption{}, ucq.order...),
		inters:     append([]Interceptor{}, ucq.inters...),
		predicates: append([]predicate.UniqueComputer{}, ucq.predicates...),
		// clone intermediate query.
		sql:  ucq.sql.Clone(),
		path: ucq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (ucq *UniqueComputerQuery) GroupBy(field string, fields ...string) *UniqueComputerGroupBy {
	ucq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UniqueComputerGroupBy{build: ucq}
	grbuild.flds = &ucq.ctx.Fields
	grbuild.label = uniquecomputer.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (ucq *UniqueComputerQuery) Select(fields ...string) *UniqueComputerSelect {
	ucq.ctx.Fields = append(ucq.ctx.Fields, fields...)
	sbuild := &UniqueComputerSelect{UniqueComputerQuery: ucq}
	sbuild.label = uniquecomputer.Label
	sbuild.flds, sbuild.scan = &ucq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UniqueComputerSelect configured with the given aggregations.
func (ucq *UniqueComputerQuery) Aggregate(fns ...AggregateFunc) *UniqueComputerSelect {
	return ucq.Select().Aggregate(fns...)
}

func (ucq *UniqueComputerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ucq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ucq); err != nil {
				return err
			}
		}
	}
	for _, f := range ucq.ctx.Fields {
		if !uniquecomputer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ucq.path != nil {
		prev, err := ucq.path(ctx)
		if err != nil {
			return err
		}
		ucq.sql = prev
	}
	return nil
}

func (ucq *UniqueComputerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UniqueComputer, error) {
	var (
		nodes = []*UniqueComputer{}
		_spec = ucq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UniqueComputer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UniqueComputer{config: ucq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ucq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ucq *UniqueComputerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ucq.querySpec()
	_spec.Node.Columns = ucq.ctx.Fields
	if len(ucq.ctx.Fields) > 0 {
		_spec.Unique = ucq.ctx.Unique != nil && *ucq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ucq.driver, _spec)
}

func (ucq *UniqueComputerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(uniquecomputer.Table, uniquecomputer.Columns, sqlgraph.NewFieldSpec(uniquecomputer.FieldID, field.TypeString))
	_spec.From = ucq.sql
	if unique := ucq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ucq.path != nil {
		_spec.Unique = true
	}
	if fields := ucq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, uniquecomputer.FieldID)
		for i := range fields {
			if fields[i] != uniquecomputer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ucq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ucq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ucq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ucq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ucq *UniqueComputerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ucq.driver.Dialect())
	t1 := builder.Table(uniquecomputer.Table)
	columns := ucq.ctx.Fields
	if len(columns) == 0 {
		columns = uniquecomputer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ucq.sql != nil {
		selector = ucq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ucq.ctx.Unique != nil && *ucq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ucq.predicates {
		p(selector)
	}
	for _, p := range ucq.order {
		p(selector)
	}
	if offset := ucq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ucq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UniqueComputerGroupBy is the group-by builder for UniqueComputer entities.
type UniqueComputerGroupBy struct {
	selector
	build *UniqueComputerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ucgb *UniqueComputerGroupBy) Aggregate(fns ...AggregateFunc) *UniqueComputerGroupBy {
	ucgb.fns = append(ucgb.fns, fns...)
	return ucgb
}

// Scan applies the selector query and scans the result into the given value.
func (ucgb *UniqueComputerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ucgb.build.ctx, ent.OpQueryGroupBy)
	if err := ucgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UniqueComputerQuery, *UniqueComputerGroupBy](ctx, ucgb.build, ucgb, ucgb.build.inters, v)
}

func (ucgb *UniqueComputerGroupBy) sqlScan(ctx context.Context, root *UniqueComputerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ucgb.fns))
	for _, fn := range ucgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ucgb.flds)+len(ucgb.fns))
		for _, f := range *ucgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ucgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ucgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UniqueComputerSelect is the builder for selecting fields of UniqueComputer entities.
type UniqueComputerSelect struct {
	*UniqueComputerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ucs *UniqueComputerSelect) Aggregate(fns ...AggregateFunc) *UniqueComputerSelect {
	ucs.fns = append(ucs.fns, fns...)
	return ucs
}

// Scan applies the selector query and scans the result into the given value.
func (ucs *UniqueComputerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ucs.ctx, ent.OpQuerySelect)
	if err := ucs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UniqueComputerQuery, *UniqueComputerSelect](ctx, ucs.UniqueComputerQuery, ucs, ucs.inters, v)
}

func (ucs *UniqueComputerSelect) sqlScan(ctx context.Context, root *UniqueComputerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ucs.fns))
	for _, fn := range ucs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ucs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ucs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
