// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"delivrio.io/print-client/ent/predicate"
	"delivrio.io/print-client/ent/remoteconnection"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// RemoteConnectionQuery is the builder for querying RemoteConnection entities.
type RemoteConnectionQuery struct {
	config
	ctx        *QueryContext
	order      []remoteconnection.OrderOption
	inters     []Interceptor
	predicates []predicate.RemoteConnection
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RemoteConnectionQuery builder.
func (rcq *RemoteConnectionQuery) Where(ps ...predicate.RemoteConnection) *RemoteConnectionQuery {
	rcq.predicates = append(rcq.predicates, ps...)
	return rcq
}

// Limit the number of records to be returned by this query.
func (rcq *RemoteConnectionQuery) Limit(limit int) *RemoteConnectionQuery {
	rcq.ctx.Limit = &limit
	return rcq
}

// Offset to start from.
func (rcq *RemoteConnectionQuery) Offset(offset int) *RemoteConnectionQuery {
	rcq.ctx.Offset = &offset
	return rcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rcq *RemoteConnectionQuery) Unique(unique bool) *RemoteConnectionQuery {
	rcq.ctx.Unique = &unique
	return rcq
}

// Order specifies how the records should be ordered.
func (rcq *RemoteConnectionQuery) Order(o ...remoteconnection.OrderOption) *RemoteConnectionQuery {
	rcq.order = append(rcq.order, o...)
	return rcq
}

// First returns the first RemoteConnection entity from the query.
// Returns a *NotFoundError when no RemoteConnection was found.
func (rcq *RemoteConnectionQuery) First(ctx context.Context) (*RemoteConnection, error) {
	nodes, err := rcq.Limit(1).All(setContextOp(ctx, rcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{remoteconnection.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rcq *RemoteConnectionQuery) FirstX(ctx context.Context) *RemoteConnection {
	node, err := rcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RemoteConnection ID from the query.
// Returns a *NotFoundError when no RemoteConnection ID was found.
func (rcq *RemoteConnectionQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = rcq.Limit(1).IDs(setContextOp(ctx, rcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{remoteconnection.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rcq *RemoteConnectionQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := rcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RemoteConnection entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RemoteConnection entity is found.
// Returns a *NotFoundError when no RemoteConnection entities are found.
func (rcq *RemoteConnectionQuery) Only(ctx context.Context) (*RemoteConnection, error) {
	nodes, err := rcq.Limit(2).All(setContextOp(ctx, rcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{remoteconnection.Label}
	default:
		return nil, &NotSingularError{remoteconnection.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rcq *RemoteConnectionQuery) OnlyX(ctx context.Context) *RemoteConnection {
	node, err := rcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RemoteConnection ID in the query.
// Returns a *NotSingularError when more than one RemoteConnection ID is found.
// Returns a *NotFoundError when no entities are found.
func (rcq *RemoteConnectionQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = rcq.Limit(2).IDs(setContextOp(ctx, rcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{remoteconnection.Label}
	default:
		err = &NotSingularError{remoteconnection.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rcq *RemoteConnectionQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := rcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RemoteConnections.
func (rcq *RemoteConnectionQuery) All(ctx context.Context) ([]*RemoteConnection, error) {
	ctx = setContextOp(ctx, rcq.ctx, ent.OpQueryAll)
	if err := rcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RemoteConnection, *RemoteConnectionQuery]()
	return withInterceptors[[]*RemoteConnection](ctx, rcq, qr, rcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rcq *RemoteConnectionQuery) AllX(ctx context.Context) []*RemoteConnection {
	nodes, err := rcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RemoteConnection IDs.
func (rcq *RemoteConnectionQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if rcq.ctx.Unique == nil && rcq.path != nil {
		rcq.Unique(true)
	}
	ctx = setContextOp(ctx, rcq.ctx, ent.OpQueryIDs)
	if err = rcq.Select(remoteconnection.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rcq *RemoteConnectionQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := rcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rcq *RemoteConnectionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rcq.ctx, ent.OpQueryCount)
	if err := rcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rcq, querierCount[*RemoteConnectionQuery](), rcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rcq *RemoteConnectionQuery) CountX(ctx context.Context) int {
	count, err := rcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rcq *RemoteConnectionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rcq.ctx, ent.OpQueryExist)
	switch _, err := rcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rcq *RemoteConnectionQuery) ExistX(ctx context.Context) bool {
	exist, err := rcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RemoteConnectionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rcq *RemoteConnectionQuery) Clone() *RemoteConnectionQuery {
	if rcq == nil {
		return nil
	}
	return &RemoteConnectionQuery{
		config:     rcq.config,
		ctx:        rcq.ctx.Clone(),
		order:      append([]remoteconnection.OrderOption{}, rcq.order...),
		inters:     append([]Interceptor{}, rcq.inters...),
		predicates: append([]predicate.RemoteConnection{}, rcq.predicates...),
		// clone intermediate query.
		sql:  rcq.sql.Clone(),
		path: rcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RemoteURL string `json:"remote_url,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RemoteConnection.Query().
//		GroupBy(remoteconnection.FieldRemoteURL).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rcq *RemoteConnectionQuery) GroupBy(field string, fields ...string) *RemoteConnectionGroupBy {
	rcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RemoteConnectionGroupBy{build: rcq}
	grbuild.flds = &rcq.ctx.Fields
	grbuild.label = remoteconnection.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RemoteURL string `json:"remote_url,omitempty"`
//	}
//
//	client.RemoteConnection.Query().
//		Select(remoteconnection.FieldRemoteURL).
//		Scan(ctx, &v)
func (rcq *RemoteConnectionQuery) Select(fields ...string) *RemoteConnectionSelect {
	rcq.ctx.Fields = append(rcq.ctx.Fields, fields...)
	sbuild := &RemoteConnectionSelect{RemoteConnectionQuery: rcq}
	sbuild.label = remoteconnection.Label
	sbuild.flds, sbuild.scan = &rcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RemoteConnectionSelect configured with the given aggregations.
func (rcq *RemoteConnectionQuery) Aggregate(fns ...AggregateFunc) *RemoteConnectionSelect {
	return rcq.Select().Aggregate(fns...)
}

func (rcq *RemoteConnectionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rcq); err != nil {
				return err
			}
		}
	}
	for _, f := range rcq.ctx.Fields {
		if !remoteconnection.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rcq.path != nil {
		prev, err := rcq.path(ctx)
		if err != nil {
			return err
		}
		rcq.sql = prev
	}
	return nil
}

func (rcq *RemoteConnectionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RemoteConnection, error) {
	var (
		nodes = []*RemoteConnection{}
		_spec = rcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RemoteConnection).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RemoteConnection{config: rcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rcq *RemoteConnectionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rcq.querySpec()
	_spec.Node.Columns = rcq.ctx.Fields
	if len(rcq.ctx.Fields) > 0 {
		_spec.Unique = rcq.ctx.Unique != nil && *rcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rcq.driver, _spec)
}

func (rcq *RemoteConnectionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(remoteconnection.Table, remoteconnection.Columns, sqlgraph.NewFieldSpec(remoteconnection.FieldID, field.TypeString))
	_spec.From = rcq.sql
	if unique := rcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rcq.path != nil {
		_spec.Unique = true
	}
	if fields := rcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, remoteconnection.FieldID)
		for i := range fields {
			if fields[i] != remoteconnection.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rcq *RemoteConnectionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rcq.driver.Dialect())
	t1 := builder.Table(remoteconnection.Table)
	columns := rcq.ctx.Fields
	if len(columns) == 0 {
		columns = remoteconnection.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rcq.sql != nil {
		selector = rcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rcq.ctx.Unique != nil && *rcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rcq.predicates {
		p(selector)
	}
	for _, p := range rcq.order {
		p(selector)
	}
	if offset := rcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RemoteConnectionGroupBy is the group-by builder for RemoteConnection entities.
type RemoteConnectionGroupBy struct {
	selector
	build *RemoteConnectionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rcgb *RemoteConnectionGroupBy) Aggregate(fns ...AggregateFunc) *RemoteConnectionGroupBy {
	rcgb.fns = append(rcgb.fns, fns...)
	return rcgb
}

// Scan applies the selector query and scans the result into the given value.
func (rcgb *RemoteConnectionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rcgb.build.ctx, ent.OpQueryGroupBy)
	if err := rcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RemoteConnectionQuery, *RemoteConnectionGroupBy](ctx, rcgb.build, rcgb, rcgb.build.inters, v)
}

func (rcgb *RemoteConnectionGroupBy) sqlScan(ctx context.Context, root *RemoteConnectionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rcgb.fns))
	for _, fn := range rcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rcgb.flds)+len(rcgb.fns))
		for _, f := range *rcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RemoteConnectionSelect is the builder for selecting fields of RemoteConnection entities.
type RemoteConnectionSelect struct {
	*RemoteConnectionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rcs *RemoteConnectionSelect) Aggregate(fns ...AggregateFunc) *RemoteConnectionSelect {
	rcs.fns = append(rcs.fns, fns...)
	return rcs
}

// Scan applies the selector query and scans the result into the given value.
func (rcs *RemoteConnectionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rcs.ctx, ent.OpQuerySelect)
	if err := rcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RemoteConnectionQuery, *RemoteConnectionSelect](ctx, rcs.RemoteConnectionQuery, rcs, rcs.inters, v)
}

func (rcs *RemoteConnectionSelect) sqlScan(ctx context.Context, root *RemoteConnectionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rcs.fns))
	for _, fn := range rcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
