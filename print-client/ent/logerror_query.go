// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"delivrio.io/print-client/ent/logerror"
	"delivrio.io/print-client/ent/predicate"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// LogErrorQuery is the builder for querying LogError entities.
type LogErrorQuery struct {
	config
	ctx        *QueryContext
	order      []logerror.OrderOption
	inters     []Interceptor
	predicates []predicate.LogError
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LogErrorQuery builder.
func (leq *LogErrorQuery) Where(ps ...predicate.LogError) *LogErrorQuery {
	leq.predicates = append(leq.predicates, ps...)
	return leq
}

// Limit the number of records to be returned by this query.
func (leq *LogErrorQuery) Limit(limit int) *LogErrorQuery {
	leq.ctx.Limit = &limit
	return leq
}

// Offset to start from.
func (leq *LogErrorQuery) Offset(offset int) *LogErrorQuery {
	leq.ctx.Offset = &offset
	return leq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (leq *LogErrorQuery) Unique(unique bool) *LogErrorQuery {
	leq.ctx.Unique = &unique
	return leq
}

// Order specifies how the records should be ordered.
func (leq *LogErrorQuery) Order(o ...logerror.OrderOption) *LogErrorQuery {
	leq.order = append(leq.order, o...)
	return leq
}

// First returns the first LogError entity from the query.
// Returns a *NotFoundError when no LogError was found.
func (leq *LogErrorQuery) First(ctx context.Context) (*LogError, error) {
	nodes, err := leq.Limit(1).All(setContextOp(ctx, leq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{logerror.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (leq *LogErrorQuery) FirstX(ctx context.Context) *LogError {
	node, err := leq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LogError ID from the query.
// Returns a *NotFoundError when no LogError ID was found.
func (leq *LogErrorQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = leq.Limit(1).IDs(setContextOp(ctx, leq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{logerror.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (leq *LogErrorQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := leq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LogError entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LogError entity is found.
// Returns a *NotFoundError when no LogError entities are found.
func (leq *LogErrorQuery) Only(ctx context.Context) (*LogError, error) {
	nodes, err := leq.Limit(2).All(setContextOp(ctx, leq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{logerror.Label}
	default:
		return nil, &NotSingularError{logerror.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (leq *LogErrorQuery) OnlyX(ctx context.Context) *LogError {
	node, err := leq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LogError ID in the query.
// Returns a *NotSingularError when more than one LogError ID is found.
// Returns a *NotFoundError when no entities are found.
func (leq *LogErrorQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = leq.Limit(2).IDs(setContextOp(ctx, leq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{logerror.Label}
	default:
		err = &NotSingularError{logerror.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (leq *LogErrorQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := leq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LogErrors.
func (leq *LogErrorQuery) All(ctx context.Context) ([]*LogError, error) {
	ctx = setContextOp(ctx, leq.ctx, ent.OpQueryAll)
	if err := leq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LogError, *LogErrorQuery]()
	return withInterceptors[[]*LogError](ctx, leq, qr, leq.inters)
}

// AllX is like All, but panics if an error occurs.
func (leq *LogErrorQuery) AllX(ctx context.Context) []*LogError {
	nodes, err := leq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LogError IDs.
func (leq *LogErrorQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if leq.ctx.Unique == nil && leq.path != nil {
		leq.Unique(true)
	}
	ctx = setContextOp(ctx, leq.ctx, ent.OpQueryIDs)
	if err = leq.Select(logerror.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (leq *LogErrorQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := leq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (leq *LogErrorQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, leq.ctx, ent.OpQueryCount)
	if err := leq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, leq, querierCount[*LogErrorQuery](), leq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (leq *LogErrorQuery) CountX(ctx context.Context) int {
	count, err := leq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (leq *LogErrorQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, leq.ctx, ent.OpQueryExist)
	switch _, err := leq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (leq *LogErrorQuery) ExistX(ctx context.Context) bool {
	exist, err := leq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LogErrorQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (leq *LogErrorQuery) Clone() *LogErrorQuery {
	if leq == nil {
		return nil
	}
	return &LogErrorQuery{
		config:     leq.config,
		ctx:        leq.ctx.Clone(),
		order:      append([]logerror.OrderOption{}, leq.order...),
		inters:     append([]Interceptor{}, leq.inters...),
		predicates: append([]predicate.LogError{}, leq.predicates...),
		// clone intermediate query.
		sql:  leq.sql.Clone(),
		path: leq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Error string `json:"error,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LogError.Query().
//		GroupBy(logerror.FieldError).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (leq *LogErrorQuery) GroupBy(field string, fields ...string) *LogErrorGroupBy {
	leq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LogErrorGroupBy{build: leq}
	grbuild.flds = &leq.ctx.Fields
	grbuild.label = logerror.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Error string `json:"error,omitempty"`
//	}
//
//	client.LogError.Query().
//		Select(logerror.FieldError).
//		Scan(ctx, &v)
func (leq *LogErrorQuery) Select(fields ...string) *LogErrorSelect {
	leq.ctx.Fields = append(leq.ctx.Fields, fields...)
	sbuild := &LogErrorSelect{LogErrorQuery: leq}
	sbuild.label = logerror.Label
	sbuild.flds, sbuild.scan = &leq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LogErrorSelect configured with the given aggregations.
func (leq *LogErrorQuery) Aggregate(fns ...AggregateFunc) *LogErrorSelect {
	return leq.Select().Aggregate(fns...)
}

func (leq *LogErrorQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range leq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, leq); err != nil {
				return err
			}
		}
	}
	for _, f := range leq.ctx.Fields {
		if !logerror.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if leq.path != nil {
		prev, err := leq.path(ctx)
		if err != nil {
			return err
		}
		leq.sql = prev
	}
	return nil
}

func (leq *LogErrorQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LogError, error) {
	var (
		nodes = []*LogError{}
		_spec = leq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LogError).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LogError{config: leq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, leq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (leq *LogErrorQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := leq.querySpec()
	_spec.Node.Columns = leq.ctx.Fields
	if len(leq.ctx.Fields) > 0 {
		_spec.Unique = leq.ctx.Unique != nil && *leq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, leq.driver, _spec)
}

func (leq *LogErrorQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(logerror.Table, logerror.Columns, sqlgraph.NewFieldSpec(logerror.FieldID, field.TypeString))
	_spec.From = leq.sql
	if unique := leq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if leq.path != nil {
		_spec.Unique = true
	}
	if fields := leq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, logerror.FieldID)
		for i := range fields {
			if fields[i] != logerror.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := leq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := leq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := leq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := leq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (leq *LogErrorQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(leq.driver.Dialect())
	t1 := builder.Table(logerror.Table)
	columns := leq.ctx.Fields
	if len(columns) == 0 {
		columns = logerror.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if leq.sql != nil {
		selector = leq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if leq.ctx.Unique != nil && *leq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range leq.predicates {
		p(selector)
	}
	for _, p := range leq.order {
		p(selector)
	}
	if offset := leq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := leq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LogErrorGroupBy is the group-by builder for LogError entities.
type LogErrorGroupBy struct {
	selector
	build *LogErrorQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (legb *LogErrorGroupBy) Aggregate(fns ...AggregateFunc) *LogErrorGroupBy {
	legb.fns = append(legb.fns, fns...)
	return legb
}

// Scan applies the selector query and scans the result into the given value.
func (legb *LogErrorGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, legb.build.ctx, ent.OpQueryGroupBy)
	if err := legb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LogErrorQuery, *LogErrorGroupBy](ctx, legb.build, legb, legb.build.inters, v)
}

func (legb *LogErrorGroupBy) sqlScan(ctx context.Context, root *LogErrorQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(legb.fns))
	for _, fn := range legb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*legb.flds)+len(legb.fns))
		for _, f := range *legb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*legb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := legb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LogErrorSelect is the builder for selecting fields of LogError entities.
type LogErrorSelect struct {
	*LogErrorQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (les *LogErrorSelect) Aggregate(fns ...AggregateFunc) *LogErrorSelect {
	les.fns = append(les.fns, fns...)
	return les
}

// Scan applies the selector query and scans the result into the given value.
func (les *LogErrorSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, les.ctx, ent.OpQuerySelect)
	if err := les.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LogErrorQuery, *LogErrorSelect](ctx, les.LogErrorQuery, les, les.inters, v)
}

func (les *LogErrorSelect) sqlScan(ctx context.Context, root *LogErrorQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(les.fns))
	for _, fn := range les.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*les.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := les.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
