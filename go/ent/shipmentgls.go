// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/shipment"
	"delivrio.io/go/ent/shipmentgls"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ShipmentGLS is the model entity for the ShipmentGLS schema.
type ShipmentGLS struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// ConsignmentID holds the value of the "consignment_id" field.
	ConsignmentID string `json:"consignment_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShipmentGLSQuery when eager-loading is set.
	Edges                 ShipmentGLSEdges `json:"edges"`
	shipment_shipment_gls *pulid.ID
	selectValues          sql.SelectValues
}

// ShipmentGLSEdges holds the relations/edges for other nodes in the graph.
type ShipmentGLSEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Shipment holds the value of the shipment edge.
	Shipment *Shipment `json:"shipment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentGLSEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// ShipmentOrErr returns the Shipment value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentGLSEdges) ShipmentOrErr() (*Shipment, error) {
	if e.Shipment != nil {
		return e.Shipment, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: shipment.Label}
	}
	return nil, &NotLoadedError{edge: "shipment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShipmentGLS) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shipmentgls.FieldID, shipmentgls.FieldTenantID:
			values[i] = new(pulid.ID)
		case shipmentgls.FieldConsignmentID:
			values[i] = new(sql.NullString)
		case shipmentgls.ForeignKeys[0]: // shipment_shipment_gls
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShipmentGLS fields.
func (sg *ShipmentGLS) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shipmentgls.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sg.ID = *value
			}
		case shipmentgls.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				sg.TenantID = *value
			}
		case shipmentgls.FieldConsignmentID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field consignment_id", values[i])
			} else if value.Valid {
				sg.ConsignmentID = value.String
			}
		case shipmentgls.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field shipment_shipment_gls", values[i])
			} else if value.Valid {
				sg.shipment_shipment_gls = new(pulid.ID)
				*sg.shipment_shipment_gls = *value.S.(*pulid.ID)
			}
		default:
			sg.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShipmentGLS.
// This includes values selected through modifiers, order, etc.
func (sg *ShipmentGLS) Value(name string) (ent.Value, error) {
	return sg.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the ShipmentGLS entity.
func (sg *ShipmentGLS) QueryTenant() *TenantQuery {
	return NewShipmentGLSClient(sg.config).QueryTenant(sg)
}

// QueryShipment queries the "shipment" edge of the ShipmentGLS entity.
func (sg *ShipmentGLS) QueryShipment() *ShipmentQuery {
	return NewShipmentGLSClient(sg.config).QueryShipment(sg)
}

// Update returns a builder for updating this ShipmentGLS.
// Note that you need to call ShipmentGLS.Unwrap() before calling this method if this ShipmentGLS
// was returned from a transaction, and the transaction was committed or rolled back.
func (sg *ShipmentGLS) Update() *ShipmentGLSUpdateOne {
	return NewShipmentGLSClient(sg.config).UpdateOne(sg)
}

// Unwrap unwraps the ShipmentGLS entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sg *ShipmentGLS) Unwrap() *ShipmentGLS {
	_tx, ok := sg.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShipmentGLS is not a transactional entity")
	}
	sg.config.driver = _tx.drv
	return sg
}

// String implements the fmt.Stringer.
func (sg *ShipmentGLS) String() string {
	var builder strings.Builder
	builder.WriteString("ShipmentGLS(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sg.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", sg.TenantID))
	builder.WriteString(", ")
	builder.WriteString("consignment_id=")
	builder.WriteString(sg.ConsignmentID)
	builder.WriteByte(')')
	return builder.String()
}

// ShipmentGLSs is a parsable slice of ShipmentGLS.
type ShipmentGLSs []*ShipmentGLS
