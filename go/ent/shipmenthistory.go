// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/changehistory"
	"delivrio.io/go/ent/shipment"
	"delivrio.io/go/ent/shipmenthistory"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ShipmentHistory is the model entity for the ShipmentHistory schema.
type ShipmentHistory struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// Type holds the value of the "type" field.
	Type shipmenthistory.Type `json:"type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShipmentHistoryQuery when eager-loading is set.
	Edges                           ShipmentHistoryEdges `json:"edges"`
	change_history_shipment_history *pulid.ID
	shipment_shipment_history       *pulid.ID
	selectValues                    sql.SelectValues
}

// ShipmentHistoryEdges holds the relations/edges for other nodes in the graph.
type ShipmentHistoryEdges struct {
	// ChangeHistory holds the value of the change_history edge.
	ChangeHistory *ChangeHistory `json:"change_history,omitempty"`
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Shipment holds the value of the shipment edge.
	Shipment *Shipment `json:"shipment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
	// totalCount holds the count of the edges above.
	totalCount [3]map[string]int
}

// ChangeHistoryOrErr returns the ChangeHistory value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentHistoryEdges) ChangeHistoryOrErr() (*ChangeHistory, error) {
	if e.ChangeHistory != nil {
		return e.ChangeHistory, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: changehistory.Label}
	}
	return nil, &NotLoadedError{edge: "change_history"}
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentHistoryEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// ShipmentOrErr returns the Shipment value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentHistoryEdges) ShipmentOrErr() (*Shipment, error) {
	if e.Shipment != nil {
		return e.Shipment, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: shipment.Label}
	}
	return nil, &NotLoadedError{edge: "shipment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShipmentHistory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shipmenthistory.FieldID, shipmenthistory.FieldTenantID:
			values[i] = new(pulid.ID)
		case shipmenthistory.FieldType:
			values[i] = new(sql.NullString)
		case shipmenthistory.ForeignKeys[0]: // change_history_shipment_history
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		case shipmenthistory.ForeignKeys[1]: // shipment_shipment_history
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShipmentHistory fields.
func (sh *ShipmentHistory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shipmenthistory.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sh.ID = *value
			}
		case shipmenthistory.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				sh.TenantID = *value
			}
		case shipmenthistory.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				sh.Type = shipmenthistory.Type(value.String)
			}
		case shipmenthistory.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field change_history_shipment_history", values[i])
			} else if value.Valid {
				sh.change_history_shipment_history = new(pulid.ID)
				*sh.change_history_shipment_history = *value.S.(*pulid.ID)
			}
		case shipmenthistory.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field shipment_shipment_history", values[i])
			} else if value.Valid {
				sh.shipment_shipment_history = new(pulid.ID)
				*sh.shipment_shipment_history = *value.S.(*pulid.ID)
			}
		default:
			sh.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShipmentHistory.
// This includes values selected through modifiers, order, etc.
func (sh *ShipmentHistory) Value(name string) (ent.Value, error) {
	return sh.selectValues.Get(name)
}

// QueryChangeHistory queries the "change_history" edge of the ShipmentHistory entity.
func (sh *ShipmentHistory) QueryChangeHistory() *ChangeHistoryQuery {
	return NewShipmentHistoryClient(sh.config).QueryChangeHistory(sh)
}

// QueryTenant queries the "tenant" edge of the ShipmentHistory entity.
func (sh *ShipmentHistory) QueryTenant() *TenantQuery {
	return NewShipmentHistoryClient(sh.config).QueryTenant(sh)
}

// QueryShipment queries the "shipment" edge of the ShipmentHistory entity.
func (sh *ShipmentHistory) QueryShipment() *ShipmentQuery {
	return NewShipmentHistoryClient(sh.config).QueryShipment(sh)
}

// Update returns a builder for updating this ShipmentHistory.
// Note that you need to call ShipmentHistory.Unwrap() before calling this method if this ShipmentHistory
// was returned from a transaction, and the transaction was committed or rolled back.
func (sh *ShipmentHistory) Update() *ShipmentHistoryUpdateOne {
	return NewShipmentHistoryClient(sh.config).UpdateOne(sh)
}

// Unwrap unwraps the ShipmentHistory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sh *ShipmentHistory) Unwrap() *ShipmentHistory {
	_tx, ok := sh.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShipmentHistory is not a transactional entity")
	}
	sh.config.driver = _tx.drv
	return sh
}

// String implements the fmt.Stringer.
func (sh *ShipmentHistory) String() string {
	var builder strings.Builder
	builder.WriteString("ShipmentHistory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sh.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", sh.TenantID))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", sh.Type))
	builder.WriteByte(')')
	return builder.String()
}

// ShipmentHistories is a parsable slice of ShipmentHistory.
type ShipmentHistories []*ShipmentHistory
