// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"delivrio.io/go/ent/shipment"
	"delivrio.io/go/ent/shipmentusps"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ShipmentUSPS is the model entity for the ShipmentUSPS schema.
type ShipmentUSPS struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// TrackingNumber holds the value of the "tracking_number" field.
	TrackingNumber string `json:"tracking_number,omitempty"`
	// Postage holds the value of the "postage" field.
	Postage float64 `json:"postage,omitempty"`
	// ScheduledDeliveryDate holds the value of the "scheduled_delivery_date" field.
	ScheduledDeliveryDate time.Time `json:"scheduled_delivery_date,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShipmentUSPSQuery when eager-loading is set.
	Edges                  ShipmentUSPSEdges `json:"edges"`
	shipment_shipment_usps *pulid.ID
	selectValues           sql.SelectValues
}

// ShipmentUSPSEdges holds the relations/edges for other nodes in the graph.
type ShipmentUSPSEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Shipment holds the value of the shipment edge.
	Shipment *Shipment `json:"shipment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentUSPSEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// ShipmentOrErr returns the Shipment value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentUSPSEdges) ShipmentOrErr() (*Shipment, error) {
	if e.Shipment != nil {
		return e.Shipment, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: shipment.Label}
	}
	return nil, &NotLoadedError{edge: "shipment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShipmentUSPS) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shipmentusps.FieldID, shipmentusps.FieldTenantID:
			values[i] = new(pulid.ID)
		case shipmentusps.FieldPostage:
			values[i] = new(sql.NullFloat64)
		case shipmentusps.FieldTrackingNumber:
			values[i] = new(sql.NullString)
		case shipmentusps.FieldScheduledDeliveryDate:
			values[i] = new(sql.NullTime)
		case shipmentusps.ForeignKeys[0]: // shipment_shipment_usps
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShipmentUSPS fields.
func (su *ShipmentUSPS) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shipmentusps.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				su.ID = *value
			}
		case shipmentusps.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				su.TenantID = *value
			}
		case shipmentusps.FieldTrackingNumber:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tracking_number", values[i])
			} else if value.Valid {
				su.TrackingNumber = value.String
			}
		case shipmentusps.FieldPostage:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field postage", values[i])
			} else if value.Valid {
				su.Postage = value.Float64
			}
		case shipmentusps.FieldScheduledDeliveryDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field scheduled_delivery_date", values[i])
			} else if value.Valid {
				su.ScheduledDeliveryDate = value.Time
			}
		case shipmentusps.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field shipment_shipment_usps", values[i])
			} else if value.Valid {
				su.shipment_shipment_usps = new(pulid.ID)
				*su.shipment_shipment_usps = *value.S.(*pulid.ID)
			}
		default:
			su.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShipmentUSPS.
// This includes values selected through modifiers, order, etc.
func (su *ShipmentUSPS) Value(name string) (ent.Value, error) {
	return su.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the ShipmentUSPS entity.
func (su *ShipmentUSPS) QueryTenant() *TenantQuery {
	return NewShipmentUSPSClient(su.config).QueryTenant(su)
}

// QueryShipment queries the "shipment" edge of the ShipmentUSPS entity.
func (su *ShipmentUSPS) QueryShipment() *ShipmentQuery {
	return NewShipmentUSPSClient(su.config).QueryShipment(su)
}

// Update returns a builder for updating this ShipmentUSPS.
// Note that you need to call ShipmentUSPS.Unwrap() before calling this method if this ShipmentUSPS
// was returned from a transaction, and the transaction was committed or rolled back.
func (su *ShipmentUSPS) Update() *ShipmentUSPSUpdateOne {
	return NewShipmentUSPSClient(su.config).UpdateOne(su)
}

// Unwrap unwraps the ShipmentUSPS entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (su *ShipmentUSPS) Unwrap() *ShipmentUSPS {
	_tx, ok := su.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShipmentUSPS is not a transactional entity")
	}
	su.config.driver = _tx.drv
	return su
}

// String implements the fmt.Stringer.
func (su *ShipmentUSPS) String() string {
	var builder strings.Builder
	builder.WriteString("ShipmentUSPS(")
	builder.WriteString(fmt.Sprintf("id=%v, ", su.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", su.TenantID))
	builder.WriteString(", ")
	builder.WriteString("tracking_number=")
	builder.WriteString(su.TrackingNumber)
	builder.WriteString(", ")
	builder.WriteString("postage=")
	builder.WriteString(fmt.Sprintf("%v", su.Postage))
	builder.WriteString(", ")
	builder.WriteString("scheduled_delivery_date=")
	builder.WriteString(su.ScheduledDeliveryDate.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ShipmentUSPSs is a parsable slice of ShipmentUSPS.
type ShipmentUSPSs []*ShipmentUSPS
