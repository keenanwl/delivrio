// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/carrier"
	"delivrio.io/go/ent/carrierpostnord"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CarrierPostNord is the model entity for the CarrierPostNord schema.
type CarrierPostNord struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// Default empty to allow creation from dialog with followup editing
	CustomerNumber string `json:"customer_number,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CarrierPostNordQuery when eager-loading is set.
	Edges                     CarrierPostNordEdges `json:"edges"`
	carrier_carrier_post_nord *pulid.ID
	selectValues              sql.SelectValues
}

// CarrierPostNordEdges holds the relations/edges for other nodes in the graph.
type CarrierPostNordEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Carrier holds the value of the carrier edge.
	Carrier *Carrier `json:"carrier,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierPostNordEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// CarrierOrErr returns the Carrier value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierPostNordEdges) CarrierOrErr() (*Carrier, error) {
	if e.Carrier != nil {
		return e.Carrier, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: carrier.Label}
	}
	return nil, &NotLoadedError{edge: "carrier"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CarrierPostNord) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case carrierpostnord.FieldID, carrierpostnord.FieldTenantID:
			values[i] = new(pulid.ID)
		case carrierpostnord.FieldCustomerNumber:
			values[i] = new(sql.NullString)
		case carrierpostnord.ForeignKeys[0]: // carrier_carrier_post_nord
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CarrierPostNord fields.
func (cpn *CarrierPostNord) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case carrierpostnord.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				cpn.ID = *value
			}
		case carrierpostnord.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				cpn.TenantID = *value
			}
		case carrierpostnord.FieldCustomerNumber:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field customer_number", values[i])
			} else if value.Valid {
				cpn.CustomerNumber = value.String
			}
		case carrierpostnord.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field carrier_carrier_post_nord", values[i])
			} else if value.Valid {
				cpn.carrier_carrier_post_nord = new(pulid.ID)
				*cpn.carrier_carrier_post_nord = *value.S.(*pulid.ID)
			}
		default:
			cpn.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CarrierPostNord.
// This includes values selected through modifiers, order, etc.
func (cpn *CarrierPostNord) Value(name string) (ent.Value, error) {
	return cpn.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the CarrierPostNord entity.
func (cpn *CarrierPostNord) QueryTenant() *TenantQuery {
	return NewCarrierPostNordClient(cpn.config).QueryTenant(cpn)
}

// QueryCarrier queries the "carrier" edge of the CarrierPostNord entity.
func (cpn *CarrierPostNord) QueryCarrier() *CarrierQuery {
	return NewCarrierPostNordClient(cpn.config).QueryCarrier(cpn)
}

// Update returns a builder for updating this CarrierPostNord.
// Note that you need to call CarrierPostNord.Unwrap() before calling this method if this CarrierPostNord
// was returned from a transaction, and the transaction was committed or rolled back.
func (cpn *CarrierPostNord) Update() *CarrierPostNordUpdateOne {
	return NewCarrierPostNordClient(cpn.config).UpdateOne(cpn)
}

// Unwrap unwraps the CarrierPostNord entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cpn *CarrierPostNord) Unwrap() *CarrierPostNord {
	_tx, ok := cpn.config.driver.(*txDriver)
	if !ok {
		panic("ent: CarrierPostNord is not a transactional entity")
	}
	cpn.config.driver = _tx.drv
	return cpn
}

// String implements the fmt.Stringer.
func (cpn *CarrierPostNord) String() string {
	var builder strings.Builder
	builder.WriteString("CarrierPostNord(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cpn.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", cpn.TenantID))
	builder.WriteString(", ")
	builder.WriteString("customer_number=")
	builder.WriteString(cpn.CustomerNumber)
	builder.WriteByte(')')
	return builder.String()
}

// CarrierPostNords is a parsable slice of CarrierPostNord.
type CarrierPostNords []*CarrierPostNord
