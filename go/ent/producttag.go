// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"delivrio.io/go/ent/producttag"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ProductTag is the model entity for the ProductTag schema.
type ProductTag struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProductTagQuery when eager-loading is set.
	Edges        ProductTagEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ProductTagEdges holds the relations/edges for other nodes in the graph.
type ProductTagEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Products holds the value of the products edge.
	Products []*Product `json:"products,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int

	namedProducts map[string][]*Product
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProductTagEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// ProductsOrErr returns the Products value or an error if the edge
// was not loaded in eager-loading.
func (e ProductTagEdges) ProductsOrErr() ([]*Product, error) {
	if e.loadedTypes[1] {
		return e.Products, nil
	}
	return nil, &NotLoadedError{edge: "products"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProductTag) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case producttag.FieldID, producttag.FieldTenantID:
			values[i] = new(pulid.ID)
		case producttag.FieldName:
			values[i] = new(sql.NullString)
		case producttag.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProductTag fields.
func (pt *ProductTag) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case producttag.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pt.ID = *value
			}
		case producttag.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				pt.TenantID = *value
			}
		case producttag.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				pt.Name = value.String
			}
		case producttag.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pt.CreatedAt = value.Time
			}
		default:
			pt.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProductTag.
// This includes values selected through modifiers, order, etc.
func (pt *ProductTag) Value(name string) (ent.Value, error) {
	return pt.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the ProductTag entity.
func (pt *ProductTag) QueryTenant() *TenantQuery {
	return NewProductTagClient(pt.config).QueryTenant(pt)
}

// QueryProducts queries the "products" edge of the ProductTag entity.
func (pt *ProductTag) QueryProducts() *ProductQuery {
	return NewProductTagClient(pt.config).QueryProducts(pt)
}

// Update returns a builder for updating this ProductTag.
// Note that you need to call ProductTag.Unwrap() before calling this method if this ProductTag
// was returned from a transaction, and the transaction was committed or rolled back.
func (pt *ProductTag) Update() *ProductTagUpdateOne {
	return NewProductTagClient(pt.config).UpdateOne(pt)
}

// Unwrap unwraps the ProductTag entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pt *ProductTag) Unwrap() *ProductTag {
	_tx, ok := pt.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProductTag is not a transactional entity")
	}
	pt.config.driver = _tx.drv
	return pt
}

// String implements the fmt.Stringer.
func (pt *ProductTag) String() string {
	var builder strings.Builder
	builder.WriteString("ProductTag(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pt.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", pt.TenantID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(pt.Name)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(pt.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// NamedProducts returns the Products named value or an error if the edge was not
// loaded in eager-loading with this name.
func (pt *ProductTag) NamedProducts(name string) ([]*Product, error) {
	if pt.Edges.namedProducts == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := pt.Edges.namedProducts[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (pt *ProductTag) appendNamedProducts(name string, edges ...*Product) {
	if pt.Edges.namedProducts == nil {
		pt.Edges.namedProducts = make(map[string][]*Product)
	}
	if len(edges) == 0 {
		pt.Edges.namedProducts[name] = []*Product{}
	} else {
		pt.Edges.namedProducts[name] = append(pt.Edges.namedProducts[name], edges...)
	}
}

// ProductTags is a parsable slice of ProductTag.
type ProductTags []*ProductTag
