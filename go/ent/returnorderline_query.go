// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/orderline"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/go/ent/returncolli"
	"delivrio.io/go/ent/returnorderline"
	"delivrio.io/go/ent/returnportalclaim"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ReturnOrderLineQuery is the builder for querying ReturnOrderLine entities.
type ReturnOrderLineQuery struct {
	config
	ctx                   *QueryContext
	order                 []returnorderline.OrderOption
	inters                []Interceptor
	predicates            []predicate.ReturnOrderLine
	withTenant            *TenantQuery
	withReturnColli       *ReturnColliQuery
	withOrderLine         *OrderLineQuery
	withReturnPortalClaim *ReturnPortalClaimQuery
	withFKs               bool
	modifiers             []func(*sql.Selector)
	loadTotal             []func(context.Context, []*ReturnOrderLine) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ReturnOrderLineQuery builder.
func (rolq *ReturnOrderLineQuery) Where(ps ...predicate.ReturnOrderLine) *ReturnOrderLineQuery {
	rolq.predicates = append(rolq.predicates, ps...)
	return rolq
}

// Limit the number of records to be returned by this query.
func (rolq *ReturnOrderLineQuery) Limit(limit int) *ReturnOrderLineQuery {
	rolq.ctx.Limit = &limit
	return rolq
}

// Offset to start from.
func (rolq *ReturnOrderLineQuery) Offset(offset int) *ReturnOrderLineQuery {
	rolq.ctx.Offset = &offset
	return rolq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rolq *ReturnOrderLineQuery) Unique(unique bool) *ReturnOrderLineQuery {
	rolq.ctx.Unique = &unique
	return rolq
}

// Order specifies how the records should be ordered.
func (rolq *ReturnOrderLineQuery) Order(o ...returnorderline.OrderOption) *ReturnOrderLineQuery {
	rolq.order = append(rolq.order, o...)
	return rolq
}

// QueryTenant chains the current query on the "tenant" edge.
func (rolq *ReturnOrderLineQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: rolq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rolq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rolq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(returnorderline.Table, returnorderline.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, returnorderline.TenantTable, returnorderline.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(rolq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReturnColli chains the current query on the "return_colli" edge.
func (rolq *ReturnOrderLineQuery) QueryReturnColli() *ReturnColliQuery {
	query := (&ReturnColliClient{config: rolq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rolq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rolq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(returnorderline.Table, returnorderline.FieldID, selector),
			sqlgraph.To(returncolli.Table, returncolli.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, returnorderline.ReturnColliTable, returnorderline.ReturnColliColumn),
		)
		fromU = sqlgraph.SetNeighbors(rolq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrderLine chains the current query on the "order_line" edge.
func (rolq *ReturnOrderLineQuery) QueryOrderLine() *OrderLineQuery {
	query := (&OrderLineClient{config: rolq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rolq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rolq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(returnorderline.Table, returnorderline.FieldID, selector),
			sqlgraph.To(orderline.Table, orderline.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, returnorderline.OrderLineTable, returnorderline.OrderLineColumn),
		)
		fromU = sqlgraph.SetNeighbors(rolq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReturnPortalClaim chains the current query on the "return_portal_claim" edge.
func (rolq *ReturnOrderLineQuery) QueryReturnPortalClaim() *ReturnPortalClaimQuery {
	query := (&ReturnPortalClaimClient{config: rolq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rolq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rolq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(returnorderline.Table, returnorderline.FieldID, selector),
			sqlgraph.To(returnportalclaim.Table, returnportalclaim.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, returnorderline.ReturnPortalClaimTable, returnorderline.ReturnPortalClaimColumn),
		)
		fromU = sqlgraph.SetNeighbors(rolq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ReturnOrderLine entity from the query.
// Returns a *NotFoundError when no ReturnOrderLine was found.
func (rolq *ReturnOrderLineQuery) First(ctx context.Context) (*ReturnOrderLine, error) {
	nodes, err := rolq.Limit(1).All(setContextOp(ctx, rolq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{returnorderline.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rolq *ReturnOrderLineQuery) FirstX(ctx context.Context) *ReturnOrderLine {
	node, err := rolq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ReturnOrderLine ID from the query.
// Returns a *NotFoundError when no ReturnOrderLine ID was found.
func (rolq *ReturnOrderLineQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = rolq.Limit(1).IDs(setContextOp(ctx, rolq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{returnorderline.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rolq *ReturnOrderLineQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := rolq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ReturnOrderLine entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ReturnOrderLine entity is found.
// Returns a *NotFoundError when no ReturnOrderLine entities are found.
func (rolq *ReturnOrderLineQuery) Only(ctx context.Context) (*ReturnOrderLine, error) {
	nodes, err := rolq.Limit(2).All(setContextOp(ctx, rolq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{returnorderline.Label}
	default:
		return nil, &NotSingularError{returnorderline.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rolq *ReturnOrderLineQuery) OnlyX(ctx context.Context) *ReturnOrderLine {
	node, err := rolq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ReturnOrderLine ID in the query.
// Returns a *NotSingularError when more than one ReturnOrderLine ID is found.
// Returns a *NotFoundError when no entities are found.
func (rolq *ReturnOrderLineQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = rolq.Limit(2).IDs(setContextOp(ctx, rolq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{returnorderline.Label}
	default:
		err = &NotSingularError{returnorderline.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rolq *ReturnOrderLineQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := rolq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ReturnOrderLines.
func (rolq *ReturnOrderLineQuery) All(ctx context.Context) ([]*ReturnOrderLine, error) {
	ctx = setContextOp(ctx, rolq.ctx, ent.OpQueryAll)
	if err := rolq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ReturnOrderLine, *ReturnOrderLineQuery]()
	return withInterceptors[[]*ReturnOrderLine](ctx, rolq, qr, rolq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rolq *ReturnOrderLineQuery) AllX(ctx context.Context) []*ReturnOrderLine {
	nodes, err := rolq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ReturnOrderLine IDs.
func (rolq *ReturnOrderLineQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if rolq.ctx.Unique == nil && rolq.path != nil {
		rolq.Unique(true)
	}
	ctx = setContextOp(ctx, rolq.ctx, ent.OpQueryIDs)
	if err = rolq.Select(returnorderline.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rolq *ReturnOrderLineQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := rolq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rolq *ReturnOrderLineQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rolq.ctx, ent.OpQueryCount)
	if err := rolq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rolq, querierCount[*ReturnOrderLineQuery](), rolq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rolq *ReturnOrderLineQuery) CountX(ctx context.Context) int {
	count, err := rolq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rolq *ReturnOrderLineQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rolq.ctx, ent.OpQueryExist)
	switch _, err := rolq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rolq *ReturnOrderLineQuery) ExistX(ctx context.Context) bool {
	exist, err := rolq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ReturnOrderLineQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rolq *ReturnOrderLineQuery) Clone() *ReturnOrderLineQuery {
	if rolq == nil {
		return nil
	}
	return &ReturnOrderLineQuery{
		config:                rolq.config,
		ctx:                   rolq.ctx.Clone(),
		order:                 append([]returnorderline.OrderOption{}, rolq.order...),
		inters:                append([]Interceptor{}, rolq.inters...),
		predicates:            append([]predicate.ReturnOrderLine{}, rolq.predicates...),
		withTenant:            rolq.withTenant.Clone(),
		withReturnColli:       rolq.withReturnColli.Clone(),
		withOrderLine:         rolq.withOrderLine.Clone(),
		withReturnPortalClaim: rolq.withReturnPortalClaim.Clone(),
		// clone intermediate query.
		sql:  rolq.sql.Clone(),
		path: rolq.path,
	}
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (rolq *ReturnOrderLineQuery) WithTenant(opts ...func(*TenantQuery)) *ReturnOrderLineQuery {
	query := (&TenantClient{config: rolq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rolq.withTenant = query
	return rolq
}

// WithReturnColli tells the query-builder to eager-load the nodes that are connected to
// the "return_colli" edge. The optional arguments are used to configure the query builder of the edge.
func (rolq *ReturnOrderLineQuery) WithReturnColli(opts ...func(*ReturnColliQuery)) *ReturnOrderLineQuery {
	query := (&ReturnColliClient{config: rolq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rolq.withReturnColli = query
	return rolq
}

// WithOrderLine tells the query-builder to eager-load the nodes that are connected to
// the "order_line" edge. The optional arguments are used to configure the query builder of the edge.
func (rolq *ReturnOrderLineQuery) WithOrderLine(opts ...func(*OrderLineQuery)) *ReturnOrderLineQuery {
	query := (&OrderLineClient{config: rolq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rolq.withOrderLine = query
	return rolq
}

// WithReturnPortalClaim tells the query-builder to eager-load the nodes that are connected to
// the "return_portal_claim" edge. The optional arguments are used to configure the query builder of the edge.
func (rolq *ReturnOrderLineQuery) WithReturnPortalClaim(opts ...func(*ReturnPortalClaimQuery)) *ReturnOrderLineQuery {
	query := (&ReturnPortalClaimClient{config: rolq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rolq.withReturnPortalClaim = query
	return rolq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ReturnOrderLine.Query().
//		GroupBy(returnorderline.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rolq *ReturnOrderLineQuery) GroupBy(field string, fields ...string) *ReturnOrderLineGroupBy {
	rolq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ReturnOrderLineGroupBy{build: rolq}
	grbuild.flds = &rolq.ctx.Fields
	grbuild.label = returnorderline.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//	}
//
//	client.ReturnOrderLine.Query().
//		Select(returnorderline.FieldTenantID).
//		Scan(ctx, &v)
func (rolq *ReturnOrderLineQuery) Select(fields ...string) *ReturnOrderLineSelect {
	rolq.ctx.Fields = append(rolq.ctx.Fields, fields...)
	sbuild := &ReturnOrderLineSelect{ReturnOrderLineQuery: rolq}
	sbuild.label = returnorderline.Label
	sbuild.flds, sbuild.scan = &rolq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ReturnOrderLineSelect configured with the given aggregations.
func (rolq *ReturnOrderLineQuery) Aggregate(fns ...AggregateFunc) *ReturnOrderLineSelect {
	return rolq.Select().Aggregate(fns...)
}

func (rolq *ReturnOrderLineQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rolq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rolq); err != nil {
				return err
			}
		}
	}
	for _, f := range rolq.ctx.Fields {
		if !returnorderline.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rolq.path != nil {
		prev, err := rolq.path(ctx)
		if err != nil {
			return err
		}
		rolq.sql = prev
	}
	if returnorderline.Policy == nil {
		return errors.New("ent: uninitialized returnorderline.Policy (forgotten import ent/runtime?)")
	}
	if err := returnorderline.Policy.EvalQuery(ctx, rolq); err != nil {
		return err
	}
	return nil
}

func (rolq *ReturnOrderLineQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ReturnOrderLine, error) {
	var (
		nodes       = []*ReturnOrderLine{}
		withFKs     = rolq.withFKs
		_spec       = rolq.querySpec()
		loadedTypes = [4]bool{
			rolq.withTenant != nil,
			rolq.withReturnColli != nil,
			rolq.withOrderLine != nil,
			rolq.withReturnPortalClaim != nil,
		}
	)
	if rolq.withReturnColli != nil || rolq.withOrderLine != nil || rolq.withReturnPortalClaim != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, returnorderline.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ReturnOrderLine).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ReturnOrderLine{config: rolq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(rolq.modifiers) > 0 {
		_spec.Modifiers = rolq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rolq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rolq.withTenant; query != nil {
		if err := rolq.loadTenant(ctx, query, nodes, nil,
			func(n *ReturnOrderLine, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	if query := rolq.withReturnColli; query != nil {
		if err := rolq.loadReturnColli(ctx, query, nodes, nil,
			func(n *ReturnOrderLine, e *ReturnColli) { n.Edges.ReturnColli = e }); err != nil {
			return nil, err
		}
	}
	if query := rolq.withOrderLine; query != nil {
		if err := rolq.loadOrderLine(ctx, query, nodes, nil,
			func(n *ReturnOrderLine, e *OrderLine) { n.Edges.OrderLine = e }); err != nil {
			return nil, err
		}
	}
	if query := rolq.withReturnPortalClaim; query != nil {
		if err := rolq.loadReturnPortalClaim(ctx, query, nodes, nil,
			func(n *ReturnOrderLine, e *ReturnPortalClaim) { n.Edges.ReturnPortalClaim = e }); err != nil {
			return nil, err
		}
	}
	for i := range rolq.loadTotal {
		if err := rolq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rolq *ReturnOrderLineQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*ReturnOrderLine, init func(*ReturnOrderLine), assign func(*ReturnOrderLine, *Tenant)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ReturnOrderLine)
	for i := range nodes {
		fk := nodes[i].TenantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rolq *ReturnOrderLineQuery) loadReturnColli(ctx context.Context, query *ReturnColliQuery, nodes []*ReturnOrderLine, init func(*ReturnOrderLine), assign func(*ReturnOrderLine, *ReturnColli)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ReturnOrderLine)
	for i := range nodes {
		if nodes[i].return_colli_return_order_line == nil {
			continue
		}
		fk := *nodes[i].return_colli_return_order_line
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(returncolli.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "return_colli_return_order_line" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rolq *ReturnOrderLineQuery) loadOrderLine(ctx context.Context, query *OrderLineQuery, nodes []*ReturnOrderLine, init func(*ReturnOrderLine), assign func(*ReturnOrderLine, *OrderLine)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ReturnOrderLine)
	for i := range nodes {
		if nodes[i].return_order_line_order_line == nil {
			continue
		}
		fk := *nodes[i].return_order_line_order_line
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(orderline.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "return_order_line_order_line" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rolq *ReturnOrderLineQuery) loadReturnPortalClaim(ctx context.Context, query *ReturnPortalClaimQuery, nodes []*ReturnOrderLine, init func(*ReturnOrderLine), assign func(*ReturnOrderLine, *ReturnPortalClaim)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ReturnOrderLine)
	for i := range nodes {
		if nodes[i].return_order_line_return_portal_claim == nil {
			continue
		}
		fk := *nodes[i].return_order_line_return_portal_claim
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(returnportalclaim.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "return_order_line_return_portal_claim" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rolq *ReturnOrderLineQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rolq.querySpec()
	if len(rolq.modifiers) > 0 {
		_spec.Modifiers = rolq.modifiers
	}
	_spec.Node.Columns = rolq.ctx.Fields
	if len(rolq.ctx.Fields) > 0 {
		_spec.Unique = rolq.ctx.Unique != nil && *rolq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rolq.driver, _spec)
}

func (rolq *ReturnOrderLineQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(returnorderline.Table, returnorderline.Columns, sqlgraph.NewFieldSpec(returnorderline.FieldID, field.TypeString))
	_spec.From = rolq.sql
	if unique := rolq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rolq.path != nil {
		_spec.Unique = true
	}
	if fields := rolq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, returnorderline.FieldID)
		for i := range fields {
			if fields[i] != returnorderline.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rolq.withTenant != nil {
			_spec.Node.AddColumnOnce(returnorderline.FieldTenantID)
		}
	}
	if ps := rolq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rolq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rolq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rolq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rolq *ReturnOrderLineQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rolq.driver.Dialect())
	t1 := builder.Table(returnorderline.Table)
	columns := rolq.ctx.Fields
	if len(columns) == 0 {
		columns = returnorderline.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rolq.sql != nil {
		selector = rolq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rolq.ctx.Unique != nil && *rolq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rolq.predicates {
		p(selector)
	}
	for _, p := range rolq.order {
		p(selector)
	}
	if offset := rolq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rolq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ReturnOrderLineGroupBy is the group-by builder for ReturnOrderLine entities.
type ReturnOrderLineGroupBy struct {
	selector
	build *ReturnOrderLineQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rolgb *ReturnOrderLineGroupBy) Aggregate(fns ...AggregateFunc) *ReturnOrderLineGroupBy {
	rolgb.fns = append(rolgb.fns, fns...)
	return rolgb
}

// Scan applies the selector query and scans the result into the given value.
func (rolgb *ReturnOrderLineGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rolgb.build.ctx, ent.OpQueryGroupBy)
	if err := rolgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReturnOrderLineQuery, *ReturnOrderLineGroupBy](ctx, rolgb.build, rolgb, rolgb.build.inters, v)
}

func (rolgb *ReturnOrderLineGroupBy) sqlScan(ctx context.Context, root *ReturnOrderLineQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rolgb.fns))
	for _, fn := range rolgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rolgb.flds)+len(rolgb.fns))
		for _, f := range *rolgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rolgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rolgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ReturnOrderLineSelect is the builder for selecting fields of ReturnOrderLine entities.
type ReturnOrderLineSelect struct {
	*ReturnOrderLineQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rols *ReturnOrderLineSelect) Aggregate(fns ...AggregateFunc) *ReturnOrderLineSelect {
	rols.fns = append(rols.fns, fns...)
	return rols
}

// Scan applies the selector query and scans the result into the given value.
func (rols *ReturnOrderLineSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rols.ctx, ent.OpQuerySelect)
	if err := rols.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReturnOrderLineQuery, *ReturnOrderLineSelect](ctx, rols.ReturnOrderLineQuery, rols, rols.inters, v)
}

func (rols *ReturnOrderLineSelect) sqlScan(ctx context.Context, root *ReturnOrderLineQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rols.fns))
	for _, fn := range rols.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rols.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rols.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
