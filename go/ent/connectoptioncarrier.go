// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/connectoptioncarrier"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ConnectOptionCarrier is the model entity for the ConnectOptionCarrier schema.
type ConnectOptionCarrier struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ConnectOptionCarrierQuery when eager-loading is set.
	Edges        ConnectOptionCarrierEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ConnectOptionCarrierEdges holds the relations/edges for other nodes in the graph.
type ConnectOptionCarrierEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant []*Tenant `json:"tenant,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int

	namedTenant map[string][]*Tenant
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading.
func (e ConnectOptionCarrierEdges) TenantOrErr() ([]*Tenant, error) {
	if e.loadedTypes[0] {
		return e.Tenant, nil
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ConnectOptionCarrier) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case connectoptioncarrier.FieldID:
			values[i] = new(pulid.ID)
		case connectoptioncarrier.FieldName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ConnectOptionCarrier fields.
func (coc *ConnectOptionCarrier) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case connectoptioncarrier.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				coc.ID = *value
			}
		case connectoptioncarrier.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				coc.Name = value.String
			}
		default:
			coc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ConnectOptionCarrier.
// This includes values selected through modifiers, order, etc.
func (coc *ConnectOptionCarrier) Value(name string) (ent.Value, error) {
	return coc.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the ConnectOptionCarrier entity.
func (coc *ConnectOptionCarrier) QueryTenant() *TenantQuery {
	return NewConnectOptionCarrierClient(coc.config).QueryTenant(coc)
}

// Update returns a builder for updating this ConnectOptionCarrier.
// Note that you need to call ConnectOptionCarrier.Unwrap() before calling this method if this ConnectOptionCarrier
// was returned from a transaction, and the transaction was committed or rolled back.
func (coc *ConnectOptionCarrier) Update() *ConnectOptionCarrierUpdateOne {
	return NewConnectOptionCarrierClient(coc.config).UpdateOne(coc)
}

// Unwrap unwraps the ConnectOptionCarrier entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (coc *ConnectOptionCarrier) Unwrap() *ConnectOptionCarrier {
	_tx, ok := coc.config.driver.(*txDriver)
	if !ok {
		panic("ent: ConnectOptionCarrier is not a transactional entity")
	}
	coc.config.driver = _tx.drv
	return coc
}

// String implements the fmt.Stringer.
func (coc *ConnectOptionCarrier) String() string {
	var builder strings.Builder
	builder.WriteString("ConnectOptionCarrier(")
	builder.WriteString(fmt.Sprintf("id=%v, ", coc.ID))
	builder.WriteString("name=")
	builder.WriteString(coc.Name)
	builder.WriteByte(')')
	return builder.String()
}

// NamedTenant returns the Tenant named value or an error if the edge was not
// loaded in eager-loading with this name.
func (coc *ConnectOptionCarrier) NamedTenant(name string) ([]*Tenant, error) {
	if coc.Edges.namedTenant == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := coc.Edges.namedTenant[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (coc *ConnectOptionCarrier) appendNamedTenant(name string, edges ...*Tenant) {
	if coc.Edges.namedTenant == nil {
		coc.Edges.namedTenant = make(map[string][]*Tenant)
	}
	if len(edges) == 0 {
		coc.Edges.namedTenant[name] = []*Tenant{}
	} else {
		coc.Edges.namedTenant[name] = append(coc.Edges.namedTenant[name], edges...)
	}
}

// ConnectOptionCarriers is a parsable slice of ConnectOptionCarrier.
type ConnectOptionCarriers []*ConnectOptionCarrier
