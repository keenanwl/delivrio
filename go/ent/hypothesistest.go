// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/connection"
	"delivrio.io/go/ent/hypothesistest"
	"delivrio.io/go/ent/hypothesistestdeliveryoption"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// HypothesisTest is the model entity for the HypothesisTest schema.
type HypothesisTest struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Active holds the value of the "active" field.
	Active bool `json:"active,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the HypothesisTestQuery when eager-loading is set.
	Edges                      HypothesisTestEdges `json:"edges"`
	hypothesis_test_connection *pulid.ID
	selectValues               sql.SelectValues
}

// HypothesisTestEdges holds the relations/edges for other nodes in the graph.
type HypothesisTestEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// HypothesisTestDeliveryOption holds the value of the hypothesis_test_delivery_option edge.
	HypothesisTestDeliveryOption *HypothesisTestDeliveryOption `json:"hypothesis_test_delivery_option,omitempty"`
	// Connection holds the value of the connection edge.
	Connection *Connection `json:"connection,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
	// totalCount holds the count of the edges above.
	totalCount [3]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e HypothesisTestEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// HypothesisTestDeliveryOptionOrErr returns the HypothesisTestDeliveryOption value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e HypothesisTestEdges) HypothesisTestDeliveryOptionOrErr() (*HypothesisTestDeliveryOption, error) {
	if e.HypothesisTestDeliveryOption != nil {
		return e.HypothesisTestDeliveryOption, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: hypothesistestdeliveryoption.Label}
	}
	return nil, &NotLoadedError{edge: "hypothesis_test_delivery_option"}
}

// ConnectionOrErr returns the Connection value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e HypothesisTestEdges) ConnectionOrErr() (*Connection, error) {
	if e.Connection != nil {
		return e.Connection, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: connection.Label}
	}
	return nil, &NotLoadedError{edge: "connection"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HypothesisTest) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case hypothesistest.FieldID, hypothesistest.FieldTenantID:
			values[i] = new(pulid.ID)
		case hypothesistest.FieldActive:
			values[i] = new(sql.NullBool)
		case hypothesistest.FieldName:
			values[i] = new(sql.NullString)
		case hypothesistest.ForeignKeys[0]: // hypothesis_test_connection
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HypothesisTest fields.
func (ht *HypothesisTest) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case hypothesistest.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ht.ID = *value
			}
		case hypothesistest.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				ht.TenantID = *value
			}
		case hypothesistest.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				ht.Name = value.String
			}
		case hypothesistest.FieldActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field active", values[i])
			} else if value.Valid {
				ht.Active = value.Bool
			}
		case hypothesistest.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field hypothesis_test_connection", values[i])
			} else if value.Valid {
				ht.hypothesis_test_connection = new(pulid.ID)
				*ht.hypothesis_test_connection = *value.S.(*pulid.ID)
			}
		default:
			ht.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the HypothesisTest.
// This includes values selected through modifiers, order, etc.
func (ht *HypothesisTest) Value(name string) (ent.Value, error) {
	return ht.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the HypothesisTest entity.
func (ht *HypothesisTest) QueryTenant() *TenantQuery {
	return NewHypothesisTestClient(ht.config).QueryTenant(ht)
}

// QueryHypothesisTestDeliveryOption queries the "hypothesis_test_delivery_option" edge of the HypothesisTest entity.
func (ht *HypothesisTest) QueryHypothesisTestDeliveryOption() *HypothesisTestDeliveryOptionQuery {
	return NewHypothesisTestClient(ht.config).QueryHypothesisTestDeliveryOption(ht)
}

// QueryConnection queries the "connection" edge of the HypothesisTest entity.
func (ht *HypothesisTest) QueryConnection() *ConnectionQuery {
	return NewHypothesisTestClient(ht.config).QueryConnection(ht)
}

// Update returns a builder for updating this HypothesisTest.
// Note that you need to call HypothesisTest.Unwrap() before calling this method if this HypothesisTest
// was returned from a transaction, and the transaction was committed or rolled back.
func (ht *HypothesisTest) Update() *HypothesisTestUpdateOne {
	return NewHypothesisTestClient(ht.config).UpdateOne(ht)
}

// Unwrap unwraps the HypothesisTest entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ht *HypothesisTest) Unwrap() *HypothesisTest {
	_tx, ok := ht.config.driver.(*txDriver)
	if !ok {
		panic("ent: HypothesisTest is not a transactional entity")
	}
	ht.config.driver = _tx.drv
	return ht
}

// String implements the fmt.Stringer.
func (ht *HypothesisTest) String() string {
	var builder strings.Builder
	builder.WriteString("HypothesisTest(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ht.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", ht.TenantID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(ht.Name)
	builder.WriteString(", ")
	builder.WriteString("active=")
	builder.WriteString(fmt.Sprintf("%v", ht.Active))
	builder.WriteByte(')')
	return builder.String()
}

// HypothesisTests is a parsable slice of HypothesisTest.
type HypothesisTests []*HypothesisTest
