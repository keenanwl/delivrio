// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/carrieradditionalservicebring"
	"delivrio.io/go/ent/carrierservice"
	"delivrio.io/go/ent/carrierservicebring"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CarrierServiceBringQuery is the builder for querying CarrierServiceBring entities.
type CarrierServiceBringQuery struct {
	config
	ctx                                    *QueryContext
	order                                  []carrierservicebring.OrderOption
	inters                                 []Interceptor
	predicates                             []predicate.CarrierServiceBring
	withCarrierService                     *CarrierServiceQuery
	withCarrierAdditionalServiceBring      *CarrierAdditionalServiceBringQuery
	withFKs                                bool
	modifiers                              []func(*sql.Selector)
	loadTotal                              []func(context.Context, []*CarrierServiceBring) error
	withNamedCarrierAdditionalServiceBring map[string]*CarrierAdditionalServiceBringQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarrierServiceBringQuery builder.
func (csbq *CarrierServiceBringQuery) Where(ps ...predicate.CarrierServiceBring) *CarrierServiceBringQuery {
	csbq.predicates = append(csbq.predicates, ps...)
	return csbq
}

// Limit the number of records to be returned by this query.
func (csbq *CarrierServiceBringQuery) Limit(limit int) *CarrierServiceBringQuery {
	csbq.ctx.Limit = &limit
	return csbq
}

// Offset to start from.
func (csbq *CarrierServiceBringQuery) Offset(offset int) *CarrierServiceBringQuery {
	csbq.ctx.Offset = &offset
	return csbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (csbq *CarrierServiceBringQuery) Unique(unique bool) *CarrierServiceBringQuery {
	csbq.ctx.Unique = &unique
	return csbq
}

// Order specifies how the records should be ordered.
func (csbq *CarrierServiceBringQuery) Order(o ...carrierservicebring.OrderOption) *CarrierServiceBringQuery {
	csbq.order = append(csbq.order, o...)
	return csbq
}

// QueryCarrierService chains the current query on the "carrier_service" edge.
func (csbq *CarrierServiceBringQuery) QueryCarrierService() *CarrierServiceQuery {
	query := (&CarrierServiceClient{config: csbq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csbq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carrierservicebring.Table, carrierservicebring.FieldID, selector),
			sqlgraph.To(carrierservice.Table, carrierservice.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, carrierservicebring.CarrierServiceTable, carrierservicebring.CarrierServiceColumn),
		)
		fromU = sqlgraph.SetNeighbors(csbq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCarrierAdditionalServiceBring chains the current query on the "carrier_additional_service_bring" edge.
func (csbq *CarrierServiceBringQuery) QueryCarrierAdditionalServiceBring() *CarrierAdditionalServiceBringQuery {
	query := (&CarrierAdditionalServiceBringClient{config: csbq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csbq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carrierservicebring.Table, carrierservicebring.FieldID, selector),
			sqlgraph.To(carrieradditionalservicebring.Table, carrieradditionalservicebring.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, carrierservicebring.CarrierAdditionalServiceBringTable, carrierservicebring.CarrierAdditionalServiceBringColumn),
		)
		fromU = sqlgraph.SetNeighbors(csbq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CarrierServiceBring entity from the query.
// Returns a *NotFoundError when no CarrierServiceBring was found.
func (csbq *CarrierServiceBringQuery) First(ctx context.Context) (*CarrierServiceBring, error) {
	nodes, err := csbq.Limit(1).All(setContextOp(ctx, csbq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{carrierservicebring.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (csbq *CarrierServiceBringQuery) FirstX(ctx context.Context) *CarrierServiceBring {
	node, err := csbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarrierServiceBring ID from the query.
// Returns a *NotFoundError when no CarrierServiceBring ID was found.
func (csbq *CarrierServiceBringQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = csbq.Limit(1).IDs(setContextOp(ctx, csbq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{carrierservicebring.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (csbq *CarrierServiceBringQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := csbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarrierServiceBring entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarrierServiceBring entity is found.
// Returns a *NotFoundError when no CarrierServiceBring entities are found.
func (csbq *CarrierServiceBringQuery) Only(ctx context.Context) (*CarrierServiceBring, error) {
	nodes, err := csbq.Limit(2).All(setContextOp(ctx, csbq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{carrierservicebring.Label}
	default:
		return nil, &NotSingularError{carrierservicebring.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (csbq *CarrierServiceBringQuery) OnlyX(ctx context.Context) *CarrierServiceBring {
	node, err := csbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarrierServiceBring ID in the query.
// Returns a *NotSingularError when more than one CarrierServiceBring ID is found.
// Returns a *NotFoundError when no entities are found.
func (csbq *CarrierServiceBringQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = csbq.Limit(2).IDs(setContextOp(ctx, csbq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{carrierservicebring.Label}
	default:
		err = &NotSingularError{carrierservicebring.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (csbq *CarrierServiceBringQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := csbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarrierServiceBrings.
func (csbq *CarrierServiceBringQuery) All(ctx context.Context) ([]*CarrierServiceBring, error) {
	ctx = setContextOp(ctx, csbq.ctx, ent.OpQueryAll)
	if err := csbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarrierServiceBring, *CarrierServiceBringQuery]()
	return withInterceptors[[]*CarrierServiceBring](ctx, csbq, qr, csbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (csbq *CarrierServiceBringQuery) AllX(ctx context.Context) []*CarrierServiceBring {
	nodes, err := csbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarrierServiceBring IDs.
func (csbq *CarrierServiceBringQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if csbq.ctx.Unique == nil && csbq.path != nil {
		csbq.Unique(true)
	}
	ctx = setContextOp(ctx, csbq.ctx, ent.OpQueryIDs)
	if err = csbq.Select(carrierservicebring.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (csbq *CarrierServiceBringQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := csbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (csbq *CarrierServiceBringQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, csbq.ctx, ent.OpQueryCount)
	if err := csbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, csbq, querierCount[*CarrierServiceBringQuery](), csbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (csbq *CarrierServiceBringQuery) CountX(ctx context.Context) int {
	count, err := csbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (csbq *CarrierServiceBringQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, csbq.ctx, ent.OpQueryExist)
	switch _, err := csbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (csbq *CarrierServiceBringQuery) ExistX(ctx context.Context) bool {
	exist, err := csbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarrierServiceBringQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (csbq *CarrierServiceBringQuery) Clone() *CarrierServiceBringQuery {
	if csbq == nil {
		return nil
	}
	return &CarrierServiceBringQuery{
		config:                            csbq.config,
		ctx:                               csbq.ctx.Clone(),
		order:                             append([]carrierservicebring.OrderOption{}, csbq.order...),
		inters:                            append([]Interceptor{}, csbq.inters...),
		predicates:                        append([]predicate.CarrierServiceBring{}, csbq.predicates...),
		withCarrierService:                csbq.withCarrierService.Clone(),
		withCarrierAdditionalServiceBring: csbq.withCarrierAdditionalServiceBring.Clone(),
		// clone intermediate query.
		sql:  csbq.sql.Clone(),
		path: csbq.path,
	}
}

// WithCarrierService tells the query-builder to eager-load the nodes that are connected to
// the "carrier_service" edge. The optional arguments are used to configure the query builder of the edge.
func (csbq *CarrierServiceBringQuery) WithCarrierService(opts ...func(*CarrierServiceQuery)) *CarrierServiceBringQuery {
	query := (&CarrierServiceClient{config: csbq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csbq.withCarrierService = query
	return csbq
}

// WithCarrierAdditionalServiceBring tells the query-builder to eager-load the nodes that are connected to
// the "carrier_additional_service_bring" edge. The optional arguments are used to configure the query builder of the edge.
func (csbq *CarrierServiceBringQuery) WithCarrierAdditionalServiceBring(opts ...func(*CarrierAdditionalServiceBringQuery)) *CarrierServiceBringQuery {
	query := (&CarrierAdditionalServiceBringClient{config: csbq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csbq.withCarrierAdditionalServiceBring = query
	return csbq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		APIServiceCode string `json:"api_service_code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CarrierServiceBring.Query().
//		GroupBy(carrierservicebring.FieldAPIServiceCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (csbq *CarrierServiceBringQuery) GroupBy(field string, fields ...string) *CarrierServiceBringGroupBy {
	csbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarrierServiceBringGroupBy{build: csbq}
	grbuild.flds = &csbq.ctx.Fields
	grbuild.label = carrierservicebring.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		APIServiceCode string `json:"api_service_code,omitempty"`
//	}
//
//	client.CarrierServiceBring.Query().
//		Select(carrierservicebring.FieldAPIServiceCode).
//		Scan(ctx, &v)
func (csbq *CarrierServiceBringQuery) Select(fields ...string) *CarrierServiceBringSelect {
	csbq.ctx.Fields = append(csbq.ctx.Fields, fields...)
	sbuild := &CarrierServiceBringSelect{CarrierServiceBringQuery: csbq}
	sbuild.label = carrierservicebring.Label
	sbuild.flds, sbuild.scan = &csbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarrierServiceBringSelect configured with the given aggregations.
func (csbq *CarrierServiceBringQuery) Aggregate(fns ...AggregateFunc) *CarrierServiceBringSelect {
	return csbq.Select().Aggregate(fns...)
}

func (csbq *CarrierServiceBringQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range csbq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, csbq); err != nil {
				return err
			}
		}
	}
	for _, f := range csbq.ctx.Fields {
		if !carrierservicebring.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if csbq.path != nil {
		prev, err := csbq.path(ctx)
		if err != nil {
			return err
		}
		csbq.sql = prev
	}
	if carrierservicebring.Policy == nil {
		return errors.New("ent: uninitialized carrierservicebring.Policy (forgotten import ent/runtime?)")
	}
	if err := carrierservicebring.Policy.EvalQuery(ctx, csbq); err != nil {
		return err
	}
	return nil
}

func (csbq *CarrierServiceBringQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarrierServiceBring, error) {
	var (
		nodes       = []*CarrierServiceBring{}
		withFKs     = csbq.withFKs
		_spec       = csbq.querySpec()
		loadedTypes = [2]bool{
			csbq.withCarrierService != nil,
			csbq.withCarrierAdditionalServiceBring != nil,
		}
	)
	if csbq.withCarrierService != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, carrierservicebring.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarrierServiceBring).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarrierServiceBring{config: csbq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(csbq.modifiers) > 0 {
		_spec.Modifiers = csbq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, csbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := csbq.withCarrierService; query != nil {
		if err := csbq.loadCarrierService(ctx, query, nodes, nil,
			func(n *CarrierServiceBring, e *CarrierService) { n.Edges.CarrierService = e }); err != nil {
			return nil, err
		}
	}
	if query := csbq.withCarrierAdditionalServiceBring; query != nil {
		if err := csbq.loadCarrierAdditionalServiceBring(ctx, query, nodes,
			func(n *CarrierServiceBring) {
				n.Edges.CarrierAdditionalServiceBring = []*CarrierAdditionalServiceBring{}
			},
			func(n *CarrierServiceBring, e *CarrierAdditionalServiceBring) {
				n.Edges.CarrierAdditionalServiceBring = append(n.Edges.CarrierAdditionalServiceBring, e)
			}); err != nil {
			return nil, err
		}
	}
	for name, query := range csbq.withNamedCarrierAdditionalServiceBring {
		if err := csbq.loadCarrierAdditionalServiceBring(ctx, query, nodes,
			func(n *CarrierServiceBring) { n.appendNamedCarrierAdditionalServiceBring(name) },
			func(n *CarrierServiceBring, e *CarrierAdditionalServiceBring) {
				n.appendNamedCarrierAdditionalServiceBring(name, e)
			}); err != nil {
			return nil, err
		}
	}
	for i := range csbq.loadTotal {
		if err := csbq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (csbq *CarrierServiceBringQuery) loadCarrierService(ctx context.Context, query *CarrierServiceQuery, nodes []*CarrierServiceBring, init func(*CarrierServiceBring), assign func(*CarrierServiceBring, *CarrierService)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*CarrierServiceBring)
	for i := range nodes {
		if nodes[i].carrier_service_carrier_service_bring == nil {
			continue
		}
		fk := *nodes[i].carrier_service_carrier_service_bring
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(carrierservice.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "carrier_service_carrier_service_bring" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (csbq *CarrierServiceBringQuery) loadCarrierAdditionalServiceBring(ctx context.Context, query *CarrierAdditionalServiceBringQuery, nodes []*CarrierServiceBring, init func(*CarrierServiceBring), assign func(*CarrierServiceBring, *CarrierAdditionalServiceBring)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[pulid.ID]*CarrierServiceBring)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.CarrierAdditionalServiceBring(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(carrierservicebring.CarrierAdditionalServiceBringColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.carrier_service_bring_carrier_additional_service_bring
		if fk == nil {
			return fmt.Errorf(`foreign-key "carrier_service_bring_carrier_additional_service_bring" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "carrier_service_bring_carrier_additional_service_bring" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (csbq *CarrierServiceBringQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := csbq.querySpec()
	if len(csbq.modifiers) > 0 {
		_spec.Modifiers = csbq.modifiers
	}
	_spec.Node.Columns = csbq.ctx.Fields
	if len(csbq.ctx.Fields) > 0 {
		_spec.Unique = csbq.ctx.Unique != nil && *csbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, csbq.driver, _spec)
}

func (csbq *CarrierServiceBringQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(carrierservicebring.Table, carrierservicebring.Columns, sqlgraph.NewFieldSpec(carrierservicebring.FieldID, field.TypeString))
	_spec.From = csbq.sql
	if unique := csbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if csbq.path != nil {
		_spec.Unique = true
	}
	if fields := csbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, carrierservicebring.FieldID)
		for i := range fields {
			if fields[i] != carrierservicebring.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := csbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := csbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := csbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := csbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (csbq *CarrierServiceBringQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(csbq.driver.Dialect())
	t1 := builder.Table(carrierservicebring.Table)
	columns := csbq.ctx.Fields
	if len(columns) == 0 {
		columns = carrierservicebring.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if csbq.sql != nil {
		selector = csbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if csbq.ctx.Unique != nil && *csbq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range csbq.predicates {
		p(selector)
	}
	for _, p := range csbq.order {
		p(selector)
	}
	if offset := csbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := csbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedCarrierAdditionalServiceBring tells the query-builder to eager-load the nodes that are connected to the "carrier_additional_service_bring"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (csbq *CarrierServiceBringQuery) WithNamedCarrierAdditionalServiceBring(name string, opts ...func(*CarrierAdditionalServiceBringQuery)) *CarrierServiceBringQuery {
	query := (&CarrierAdditionalServiceBringClient{config: csbq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if csbq.withNamedCarrierAdditionalServiceBring == nil {
		csbq.withNamedCarrierAdditionalServiceBring = make(map[string]*CarrierAdditionalServiceBringQuery)
	}
	csbq.withNamedCarrierAdditionalServiceBring[name] = query
	return csbq
}

// CarrierServiceBringGroupBy is the group-by builder for CarrierServiceBring entities.
type CarrierServiceBringGroupBy struct {
	selector
	build *CarrierServiceBringQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (csbgb *CarrierServiceBringGroupBy) Aggregate(fns ...AggregateFunc) *CarrierServiceBringGroupBy {
	csbgb.fns = append(csbgb.fns, fns...)
	return csbgb
}

// Scan applies the selector query and scans the result into the given value.
func (csbgb *CarrierServiceBringGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csbgb.build.ctx, ent.OpQueryGroupBy)
	if err := csbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarrierServiceBringQuery, *CarrierServiceBringGroupBy](ctx, csbgb.build, csbgb, csbgb.build.inters, v)
}

func (csbgb *CarrierServiceBringGroupBy) sqlScan(ctx context.Context, root *CarrierServiceBringQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(csbgb.fns))
	for _, fn := range csbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*csbgb.flds)+len(csbgb.fns))
		for _, f := range *csbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*csbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarrierServiceBringSelect is the builder for selecting fields of CarrierServiceBring entities.
type CarrierServiceBringSelect struct {
	*CarrierServiceBringQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (csbs *CarrierServiceBringSelect) Aggregate(fns ...AggregateFunc) *CarrierServiceBringSelect {
	csbs.fns = append(csbs.fns, fns...)
	return csbs
}

// Scan applies the selector query and scans the result into the given value.
func (csbs *CarrierServiceBringSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csbs.ctx, ent.OpQuerySelect)
	if err := csbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarrierServiceBringQuery, *CarrierServiceBringSelect](ctx, csbs.CarrierServiceBringQuery, csbs, csbs.inters, v)
}

func (csbs *CarrierServiceBringSelect) sqlScan(ctx context.Context, root *CarrierServiceBringQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(csbs.fns))
	for _, fn := range csbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*csbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
