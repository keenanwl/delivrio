// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/predicate"
	"delivrio.io/go/ent/shipment"
	"delivrio.io/go/ent/shipmentpostnord"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ShipmentPostNordQuery is the builder for querying ShipmentPostNord entities.
type ShipmentPostNordQuery struct {
	config
	ctx          *QueryContext
	order        []shipmentpostnord.OrderOption
	inters       []Interceptor
	predicates   []predicate.ShipmentPostNord
	withTenant   *TenantQuery
	withShipment *ShipmentQuery
	withFKs      bool
	modifiers    []func(*sql.Selector)
	loadTotal    []func(context.Context, []*ShipmentPostNord) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShipmentPostNordQuery builder.
func (spnq *ShipmentPostNordQuery) Where(ps ...predicate.ShipmentPostNord) *ShipmentPostNordQuery {
	spnq.predicates = append(spnq.predicates, ps...)
	return spnq
}

// Limit the number of records to be returned by this query.
func (spnq *ShipmentPostNordQuery) Limit(limit int) *ShipmentPostNordQuery {
	spnq.ctx.Limit = &limit
	return spnq
}

// Offset to start from.
func (spnq *ShipmentPostNordQuery) Offset(offset int) *ShipmentPostNordQuery {
	spnq.ctx.Offset = &offset
	return spnq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (spnq *ShipmentPostNordQuery) Unique(unique bool) *ShipmentPostNordQuery {
	spnq.ctx.Unique = &unique
	return spnq
}

// Order specifies how the records should be ordered.
func (spnq *ShipmentPostNordQuery) Order(o ...shipmentpostnord.OrderOption) *ShipmentPostNordQuery {
	spnq.order = append(spnq.order, o...)
	return spnq
}

// QueryTenant chains the current query on the "tenant" edge.
func (spnq *ShipmentPostNordQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: spnq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := spnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := spnq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentpostnord.Table, shipmentpostnord.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shipmentpostnord.TenantTable, shipmentpostnord.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(spnq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryShipment chains the current query on the "shipment" edge.
func (spnq *ShipmentPostNordQuery) QueryShipment() *ShipmentQuery {
	query := (&ShipmentClient{config: spnq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := spnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := spnq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmentpostnord.Table, shipmentpostnord.FieldID, selector),
			sqlgraph.To(shipment.Table, shipment.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, shipmentpostnord.ShipmentTable, shipmentpostnord.ShipmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(spnq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShipmentPostNord entity from the query.
// Returns a *NotFoundError when no ShipmentPostNord was found.
func (spnq *ShipmentPostNordQuery) First(ctx context.Context) (*ShipmentPostNord, error) {
	nodes, err := spnq.Limit(1).All(setContextOp(ctx, spnq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shipmentpostnord.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (spnq *ShipmentPostNordQuery) FirstX(ctx context.Context) *ShipmentPostNord {
	node, err := spnq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShipmentPostNord ID from the query.
// Returns a *NotFoundError when no ShipmentPostNord ID was found.
func (spnq *ShipmentPostNordQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = spnq.Limit(1).IDs(setContextOp(ctx, spnq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shipmentpostnord.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (spnq *ShipmentPostNordQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := spnq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShipmentPostNord entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShipmentPostNord entity is found.
// Returns a *NotFoundError when no ShipmentPostNord entities are found.
func (spnq *ShipmentPostNordQuery) Only(ctx context.Context) (*ShipmentPostNord, error) {
	nodes, err := spnq.Limit(2).All(setContextOp(ctx, spnq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shipmentpostnord.Label}
	default:
		return nil, &NotSingularError{shipmentpostnord.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (spnq *ShipmentPostNordQuery) OnlyX(ctx context.Context) *ShipmentPostNord {
	node, err := spnq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShipmentPostNord ID in the query.
// Returns a *NotSingularError when more than one ShipmentPostNord ID is found.
// Returns a *NotFoundError when no entities are found.
func (spnq *ShipmentPostNordQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = spnq.Limit(2).IDs(setContextOp(ctx, spnq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shipmentpostnord.Label}
	default:
		err = &NotSingularError{shipmentpostnord.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (spnq *ShipmentPostNordQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := spnq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShipmentPostNords.
func (spnq *ShipmentPostNordQuery) All(ctx context.Context) ([]*ShipmentPostNord, error) {
	ctx = setContextOp(ctx, spnq.ctx, ent.OpQueryAll)
	if err := spnq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShipmentPostNord, *ShipmentPostNordQuery]()
	return withInterceptors[[]*ShipmentPostNord](ctx, spnq, qr, spnq.inters)
}

// AllX is like All, but panics if an error occurs.
func (spnq *ShipmentPostNordQuery) AllX(ctx context.Context) []*ShipmentPostNord {
	nodes, err := spnq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShipmentPostNord IDs.
func (spnq *ShipmentPostNordQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if spnq.ctx.Unique == nil && spnq.path != nil {
		spnq.Unique(true)
	}
	ctx = setContextOp(ctx, spnq.ctx, ent.OpQueryIDs)
	if err = spnq.Select(shipmentpostnord.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (spnq *ShipmentPostNordQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := spnq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (spnq *ShipmentPostNordQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, spnq.ctx, ent.OpQueryCount)
	if err := spnq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, spnq, querierCount[*ShipmentPostNordQuery](), spnq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (spnq *ShipmentPostNordQuery) CountX(ctx context.Context) int {
	count, err := spnq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (spnq *ShipmentPostNordQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, spnq.ctx, ent.OpQueryExist)
	switch _, err := spnq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (spnq *ShipmentPostNordQuery) ExistX(ctx context.Context) bool {
	exist, err := spnq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShipmentPostNordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (spnq *ShipmentPostNordQuery) Clone() *ShipmentPostNordQuery {
	if spnq == nil {
		return nil
	}
	return &ShipmentPostNordQuery{
		config:       spnq.config,
		ctx:          spnq.ctx.Clone(),
		order:        append([]shipmentpostnord.OrderOption{}, spnq.order...),
		inters:       append([]Interceptor{}, spnq.inters...),
		predicates:   append([]predicate.ShipmentPostNord{}, spnq.predicates...),
		withTenant:   spnq.withTenant.Clone(),
		withShipment: spnq.withShipment.Clone(),
		// clone intermediate query.
		sql:  spnq.sql.Clone(),
		path: spnq.path,
	}
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (spnq *ShipmentPostNordQuery) WithTenant(opts ...func(*TenantQuery)) *ShipmentPostNordQuery {
	query := (&TenantClient{config: spnq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	spnq.withTenant = query
	return spnq
}

// WithShipment tells the query-builder to eager-load the nodes that are connected to
// the "shipment" edge. The optional arguments are used to configure the query builder of the edge.
func (spnq *ShipmentPostNordQuery) WithShipment(opts ...func(*ShipmentQuery)) *ShipmentPostNordQuery {
	query := (&ShipmentClient{config: spnq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	spnq.withShipment = query
	return spnq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShipmentPostNord.Query().
//		GroupBy(shipmentpostnord.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (spnq *ShipmentPostNordQuery) GroupBy(field string, fields ...string) *ShipmentPostNordGroupBy {
	spnq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShipmentPostNordGroupBy{build: spnq}
	grbuild.flds = &spnq.ctx.Fields
	grbuild.label = shipmentpostnord.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//	}
//
//	client.ShipmentPostNord.Query().
//		Select(shipmentpostnord.FieldTenantID).
//		Scan(ctx, &v)
func (spnq *ShipmentPostNordQuery) Select(fields ...string) *ShipmentPostNordSelect {
	spnq.ctx.Fields = append(spnq.ctx.Fields, fields...)
	sbuild := &ShipmentPostNordSelect{ShipmentPostNordQuery: spnq}
	sbuild.label = shipmentpostnord.Label
	sbuild.flds, sbuild.scan = &spnq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShipmentPostNordSelect configured with the given aggregations.
func (spnq *ShipmentPostNordQuery) Aggregate(fns ...AggregateFunc) *ShipmentPostNordSelect {
	return spnq.Select().Aggregate(fns...)
}

func (spnq *ShipmentPostNordQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range spnq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, spnq); err != nil {
				return err
			}
		}
	}
	for _, f := range spnq.ctx.Fields {
		if !shipmentpostnord.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if spnq.path != nil {
		prev, err := spnq.path(ctx)
		if err != nil {
			return err
		}
		spnq.sql = prev
	}
	if shipmentpostnord.Policy == nil {
		return errors.New("ent: uninitialized shipmentpostnord.Policy (forgotten import ent/runtime?)")
	}
	if err := shipmentpostnord.Policy.EvalQuery(ctx, spnq); err != nil {
		return err
	}
	return nil
}

func (spnq *ShipmentPostNordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShipmentPostNord, error) {
	var (
		nodes       = []*ShipmentPostNord{}
		withFKs     = spnq.withFKs
		_spec       = spnq.querySpec()
		loadedTypes = [2]bool{
			spnq.withTenant != nil,
			spnq.withShipment != nil,
		}
	)
	if spnq.withShipment != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, shipmentpostnord.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShipmentPostNord).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShipmentPostNord{config: spnq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(spnq.modifiers) > 0 {
		_spec.Modifiers = spnq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, spnq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := spnq.withTenant; query != nil {
		if err := spnq.loadTenant(ctx, query, nodes, nil,
			func(n *ShipmentPostNord, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	if query := spnq.withShipment; query != nil {
		if err := spnq.loadShipment(ctx, query, nodes, nil,
			func(n *ShipmentPostNord, e *Shipment) { n.Edges.Shipment = e }); err != nil {
			return nil, err
		}
	}
	for i := range spnq.loadTotal {
		if err := spnq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (spnq *ShipmentPostNordQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*ShipmentPostNord, init func(*ShipmentPostNord), assign func(*ShipmentPostNord, *Tenant)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ShipmentPostNord)
	for i := range nodes {
		fk := nodes[i].TenantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (spnq *ShipmentPostNordQuery) loadShipment(ctx context.Context, query *ShipmentQuery, nodes []*ShipmentPostNord, init func(*ShipmentPostNord), assign func(*ShipmentPostNord, *Shipment)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ShipmentPostNord)
	for i := range nodes {
		if nodes[i].shipment_shipment_post_nord == nil {
			continue
		}
		fk := *nodes[i].shipment_shipment_post_nord
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(shipment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shipment_shipment_post_nord" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (spnq *ShipmentPostNordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := spnq.querySpec()
	if len(spnq.modifiers) > 0 {
		_spec.Modifiers = spnq.modifiers
	}
	_spec.Node.Columns = spnq.ctx.Fields
	if len(spnq.ctx.Fields) > 0 {
		_spec.Unique = spnq.ctx.Unique != nil && *spnq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, spnq.driver, _spec)
}

func (spnq *ShipmentPostNordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shipmentpostnord.Table, shipmentpostnord.Columns, sqlgraph.NewFieldSpec(shipmentpostnord.FieldID, field.TypeString))
	_spec.From = spnq.sql
	if unique := spnq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if spnq.path != nil {
		_spec.Unique = true
	}
	if fields := spnq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shipmentpostnord.FieldID)
		for i := range fields {
			if fields[i] != shipmentpostnord.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if spnq.withTenant != nil {
			_spec.Node.AddColumnOnce(shipmentpostnord.FieldTenantID)
		}
	}
	if ps := spnq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := spnq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := spnq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := spnq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (spnq *ShipmentPostNordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(spnq.driver.Dialect())
	t1 := builder.Table(shipmentpostnord.Table)
	columns := spnq.ctx.Fields
	if len(columns) == 0 {
		columns = shipmentpostnord.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if spnq.sql != nil {
		selector = spnq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if spnq.ctx.Unique != nil && *spnq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range spnq.predicates {
		p(selector)
	}
	for _, p := range spnq.order {
		p(selector)
	}
	if offset := spnq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := spnq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ShipmentPostNordGroupBy is the group-by builder for ShipmentPostNord entities.
type ShipmentPostNordGroupBy struct {
	selector
	build *ShipmentPostNordQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (spngb *ShipmentPostNordGroupBy) Aggregate(fns ...AggregateFunc) *ShipmentPostNordGroupBy {
	spngb.fns = append(spngb.fns, fns...)
	return spngb
}

// Scan applies the selector query and scans the result into the given value.
func (spngb *ShipmentPostNordGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, spngb.build.ctx, ent.OpQueryGroupBy)
	if err := spngb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShipmentPostNordQuery, *ShipmentPostNordGroupBy](ctx, spngb.build, spngb, spngb.build.inters, v)
}

func (spngb *ShipmentPostNordGroupBy) sqlScan(ctx context.Context, root *ShipmentPostNordQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(spngb.fns))
	for _, fn := range spngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*spngb.flds)+len(spngb.fns))
		for _, f := range *spngb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*spngb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := spngb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShipmentPostNordSelect is the builder for selecting fields of ShipmentPostNord entities.
type ShipmentPostNordSelect struct {
	*ShipmentPostNordQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (spns *ShipmentPostNordSelect) Aggregate(fns ...AggregateFunc) *ShipmentPostNordSelect {
	spns.fns = append(spns.fns, fns...)
	return spns
}

// Scan applies the selector query and scans the result into the given value.
func (spns *ShipmentPostNordSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, spns.ctx, ent.OpQuerySelect)
	if err := spns.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShipmentPostNordQuery, *ShipmentPostNordSelect](ctx, spns.ShipmentPostNordQuery, spns, spns.inters, v)
}

func (spns *ShipmentPostNordSelect) sqlScan(ctx context.Context, root *ShipmentPostNordQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(spns.fns))
	for _, fn := range spns.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*spns.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := spns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
