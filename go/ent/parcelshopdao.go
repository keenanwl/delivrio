// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/parcelshop"
	"delivrio.io/go/ent/parcelshopdao"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ParcelShopDAO is the model entity for the ParcelShopDAO schema.
type ParcelShopDAO struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// ShopID holds the value of the "shop_id" field.
	ShopID string `json:"shop_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ParcelShopDAOQuery when eager-loading is set.
	Edges                       ParcelShopDAOEdges `json:"edges"`
	parcel_shop_parcel_shop_dao *pulid.ID
	selectValues                sql.SelectValues
}

// ParcelShopDAOEdges holds the relations/edges for other nodes in the graph.
type ParcelShopDAOEdges struct {
	// ParcelShop holds the value of the parcel_shop edge.
	ParcelShop *ParcelShop `json:"parcel_shop,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// ParcelShopOrErr returns the ParcelShop value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ParcelShopDAOEdges) ParcelShopOrErr() (*ParcelShop, error) {
	if e.ParcelShop != nil {
		return e.ParcelShop, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: parcelshop.Label}
	}
	return nil, &NotLoadedError{edge: "parcel_shop"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ParcelShopDAO) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case parcelshopdao.FieldID:
			values[i] = new(pulid.ID)
		case parcelshopdao.FieldShopID:
			values[i] = new(sql.NullString)
		case parcelshopdao.ForeignKeys[0]: // parcel_shop_parcel_shop_dao
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ParcelShopDAO fields.
func (psd *ParcelShopDAO) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case parcelshopdao.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				psd.ID = *value
			}
		case parcelshopdao.FieldShopID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field shop_id", values[i])
			} else if value.Valid {
				psd.ShopID = value.String
			}
		case parcelshopdao.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field parcel_shop_parcel_shop_dao", values[i])
			} else if value.Valid {
				psd.parcel_shop_parcel_shop_dao = new(pulid.ID)
				*psd.parcel_shop_parcel_shop_dao = *value.S.(*pulid.ID)
			}
		default:
			psd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ParcelShopDAO.
// This includes values selected through modifiers, order, etc.
func (psd *ParcelShopDAO) Value(name string) (ent.Value, error) {
	return psd.selectValues.Get(name)
}

// QueryParcelShop queries the "parcel_shop" edge of the ParcelShopDAO entity.
func (psd *ParcelShopDAO) QueryParcelShop() *ParcelShopQuery {
	return NewParcelShopDAOClient(psd.config).QueryParcelShop(psd)
}

// Update returns a builder for updating this ParcelShopDAO.
// Note that you need to call ParcelShopDAO.Unwrap() before calling this method if this ParcelShopDAO
// was returned from a transaction, and the transaction was committed or rolled back.
func (psd *ParcelShopDAO) Update() *ParcelShopDAOUpdateOne {
	return NewParcelShopDAOClient(psd.config).UpdateOne(psd)
}

// Unwrap unwraps the ParcelShopDAO entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (psd *ParcelShopDAO) Unwrap() *ParcelShopDAO {
	_tx, ok := psd.config.driver.(*txDriver)
	if !ok {
		panic("ent: ParcelShopDAO is not a transactional entity")
	}
	psd.config.driver = _tx.drv
	return psd
}

// String implements the fmt.Stringer.
func (psd *ParcelShopDAO) String() string {
	var builder strings.Builder
	builder.WriteString("ParcelShopDAO(")
	builder.WriteString(fmt.Sprintf("id=%v, ", psd.ID))
	builder.WriteString("shop_id=")
	builder.WriteString(psd.ShopID)
	builder.WriteByte(')')
	return builder.String()
}

// ParcelShopDAOs is a parsable slice of ParcelShopDAO.
type ParcelShopDAOs []*ParcelShopDAO
