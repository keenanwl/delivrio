// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/changehistory"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/go/ent/returncolli"
	"delivrio.io/go/ent/returncollihistory"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ReturnColliHistoryQuery is the builder for querying ReturnColliHistory entities.
type ReturnColliHistoryQuery struct {
	config
	ctx               *QueryContext
	order             []returncollihistory.OrderOption
	inters            []Interceptor
	predicates        []predicate.ReturnColliHistory
	withChangeHistory *ChangeHistoryQuery
	withTenant        *TenantQuery
	withReturnColli   *ReturnColliQuery
	withFKs           bool
	modifiers         []func(*sql.Selector)
	loadTotal         []func(context.Context, []*ReturnColliHistory) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ReturnColliHistoryQuery builder.
func (rchq *ReturnColliHistoryQuery) Where(ps ...predicate.ReturnColliHistory) *ReturnColliHistoryQuery {
	rchq.predicates = append(rchq.predicates, ps...)
	return rchq
}

// Limit the number of records to be returned by this query.
func (rchq *ReturnColliHistoryQuery) Limit(limit int) *ReturnColliHistoryQuery {
	rchq.ctx.Limit = &limit
	return rchq
}

// Offset to start from.
func (rchq *ReturnColliHistoryQuery) Offset(offset int) *ReturnColliHistoryQuery {
	rchq.ctx.Offset = &offset
	return rchq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rchq *ReturnColliHistoryQuery) Unique(unique bool) *ReturnColliHistoryQuery {
	rchq.ctx.Unique = &unique
	return rchq
}

// Order specifies how the records should be ordered.
func (rchq *ReturnColliHistoryQuery) Order(o ...returncollihistory.OrderOption) *ReturnColliHistoryQuery {
	rchq.order = append(rchq.order, o...)
	return rchq
}

// QueryChangeHistory chains the current query on the "change_history" edge.
func (rchq *ReturnColliHistoryQuery) QueryChangeHistory() *ChangeHistoryQuery {
	query := (&ChangeHistoryClient{config: rchq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rchq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rchq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(returncollihistory.Table, returncollihistory.FieldID, selector),
			sqlgraph.To(changehistory.Table, changehistory.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, returncollihistory.ChangeHistoryTable, returncollihistory.ChangeHistoryColumn),
		)
		fromU = sqlgraph.SetNeighbors(rchq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTenant chains the current query on the "tenant" edge.
func (rchq *ReturnColliHistoryQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: rchq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rchq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rchq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(returncollihistory.Table, returncollihistory.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, returncollihistory.TenantTable, returncollihistory.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(rchq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReturnColli chains the current query on the "return_colli" edge.
func (rchq *ReturnColliHistoryQuery) QueryReturnColli() *ReturnColliQuery {
	query := (&ReturnColliClient{config: rchq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rchq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rchq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(returncollihistory.Table, returncollihistory.FieldID, selector),
			sqlgraph.To(returncolli.Table, returncolli.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, returncollihistory.ReturnColliTable, returncollihistory.ReturnColliColumn),
		)
		fromU = sqlgraph.SetNeighbors(rchq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ReturnColliHistory entity from the query.
// Returns a *NotFoundError when no ReturnColliHistory was found.
func (rchq *ReturnColliHistoryQuery) First(ctx context.Context) (*ReturnColliHistory, error) {
	nodes, err := rchq.Limit(1).All(setContextOp(ctx, rchq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{returncollihistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rchq *ReturnColliHistoryQuery) FirstX(ctx context.Context) *ReturnColliHistory {
	node, err := rchq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ReturnColliHistory ID from the query.
// Returns a *NotFoundError when no ReturnColliHistory ID was found.
func (rchq *ReturnColliHistoryQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = rchq.Limit(1).IDs(setContextOp(ctx, rchq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{returncollihistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rchq *ReturnColliHistoryQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := rchq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ReturnColliHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ReturnColliHistory entity is found.
// Returns a *NotFoundError when no ReturnColliHistory entities are found.
func (rchq *ReturnColliHistoryQuery) Only(ctx context.Context) (*ReturnColliHistory, error) {
	nodes, err := rchq.Limit(2).All(setContextOp(ctx, rchq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{returncollihistory.Label}
	default:
		return nil, &NotSingularError{returncollihistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rchq *ReturnColliHistoryQuery) OnlyX(ctx context.Context) *ReturnColliHistory {
	node, err := rchq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ReturnColliHistory ID in the query.
// Returns a *NotSingularError when more than one ReturnColliHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (rchq *ReturnColliHistoryQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = rchq.Limit(2).IDs(setContextOp(ctx, rchq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{returncollihistory.Label}
	default:
		err = &NotSingularError{returncollihistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rchq *ReturnColliHistoryQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := rchq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ReturnColliHistories.
func (rchq *ReturnColliHistoryQuery) All(ctx context.Context) ([]*ReturnColliHistory, error) {
	ctx = setContextOp(ctx, rchq.ctx, ent.OpQueryAll)
	if err := rchq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ReturnColliHistory, *ReturnColliHistoryQuery]()
	return withInterceptors[[]*ReturnColliHistory](ctx, rchq, qr, rchq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rchq *ReturnColliHistoryQuery) AllX(ctx context.Context) []*ReturnColliHistory {
	nodes, err := rchq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ReturnColliHistory IDs.
func (rchq *ReturnColliHistoryQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if rchq.ctx.Unique == nil && rchq.path != nil {
		rchq.Unique(true)
	}
	ctx = setContextOp(ctx, rchq.ctx, ent.OpQueryIDs)
	if err = rchq.Select(returncollihistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rchq *ReturnColliHistoryQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := rchq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rchq *ReturnColliHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rchq.ctx, ent.OpQueryCount)
	if err := rchq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rchq, querierCount[*ReturnColliHistoryQuery](), rchq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rchq *ReturnColliHistoryQuery) CountX(ctx context.Context) int {
	count, err := rchq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rchq *ReturnColliHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rchq.ctx, ent.OpQueryExist)
	switch _, err := rchq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rchq *ReturnColliHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := rchq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ReturnColliHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rchq *ReturnColliHistoryQuery) Clone() *ReturnColliHistoryQuery {
	if rchq == nil {
		return nil
	}
	return &ReturnColliHistoryQuery{
		config:            rchq.config,
		ctx:               rchq.ctx.Clone(),
		order:             append([]returncollihistory.OrderOption{}, rchq.order...),
		inters:            append([]Interceptor{}, rchq.inters...),
		predicates:        append([]predicate.ReturnColliHistory{}, rchq.predicates...),
		withChangeHistory: rchq.withChangeHistory.Clone(),
		withTenant:        rchq.withTenant.Clone(),
		withReturnColli:   rchq.withReturnColli.Clone(),
		// clone intermediate query.
		sql:  rchq.sql.Clone(),
		path: rchq.path,
	}
}

// WithChangeHistory tells the query-builder to eager-load the nodes that are connected to
// the "change_history" edge. The optional arguments are used to configure the query builder of the edge.
func (rchq *ReturnColliHistoryQuery) WithChangeHistory(opts ...func(*ChangeHistoryQuery)) *ReturnColliHistoryQuery {
	query := (&ChangeHistoryClient{config: rchq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rchq.withChangeHistory = query
	return rchq
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (rchq *ReturnColliHistoryQuery) WithTenant(opts ...func(*TenantQuery)) *ReturnColliHistoryQuery {
	query := (&TenantClient{config: rchq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rchq.withTenant = query
	return rchq
}

// WithReturnColli tells the query-builder to eager-load the nodes that are connected to
// the "return_colli" edge. The optional arguments are used to configure the query builder of the edge.
func (rchq *ReturnColliHistoryQuery) WithReturnColli(opts ...func(*ReturnColliQuery)) *ReturnColliHistoryQuery {
	query := (&ReturnColliClient{config: rchq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rchq.withReturnColli = query
	return rchq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ReturnColliHistory.Query().
//		GroupBy(returncollihistory.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rchq *ReturnColliHistoryQuery) GroupBy(field string, fields ...string) *ReturnColliHistoryGroupBy {
	rchq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ReturnColliHistoryGroupBy{build: rchq}
	grbuild.flds = &rchq.ctx.Fields
	grbuild.label = returncollihistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//	}
//
//	client.ReturnColliHistory.Query().
//		Select(returncollihistory.FieldTenantID).
//		Scan(ctx, &v)
func (rchq *ReturnColliHistoryQuery) Select(fields ...string) *ReturnColliHistorySelect {
	rchq.ctx.Fields = append(rchq.ctx.Fields, fields...)
	sbuild := &ReturnColliHistorySelect{ReturnColliHistoryQuery: rchq}
	sbuild.label = returncollihistory.Label
	sbuild.flds, sbuild.scan = &rchq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ReturnColliHistorySelect configured with the given aggregations.
func (rchq *ReturnColliHistoryQuery) Aggregate(fns ...AggregateFunc) *ReturnColliHistorySelect {
	return rchq.Select().Aggregate(fns...)
}

func (rchq *ReturnColliHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rchq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rchq); err != nil {
				return err
			}
		}
	}
	for _, f := range rchq.ctx.Fields {
		if !returncollihistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rchq.path != nil {
		prev, err := rchq.path(ctx)
		if err != nil {
			return err
		}
		rchq.sql = prev
	}
	if returncollihistory.Policy == nil {
		return errors.New("ent: uninitialized returncollihistory.Policy (forgotten import ent/runtime?)")
	}
	if err := returncollihistory.Policy.EvalQuery(ctx, rchq); err != nil {
		return err
	}
	return nil
}

func (rchq *ReturnColliHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ReturnColliHistory, error) {
	var (
		nodes       = []*ReturnColliHistory{}
		withFKs     = rchq.withFKs
		_spec       = rchq.querySpec()
		loadedTypes = [3]bool{
			rchq.withChangeHistory != nil,
			rchq.withTenant != nil,
			rchq.withReturnColli != nil,
		}
	)
	if rchq.withChangeHistory != nil || rchq.withReturnColli != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, returncollihistory.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ReturnColliHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ReturnColliHistory{config: rchq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(rchq.modifiers) > 0 {
		_spec.Modifiers = rchq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rchq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rchq.withChangeHistory; query != nil {
		if err := rchq.loadChangeHistory(ctx, query, nodes, nil,
			func(n *ReturnColliHistory, e *ChangeHistory) { n.Edges.ChangeHistory = e }); err != nil {
			return nil, err
		}
	}
	if query := rchq.withTenant; query != nil {
		if err := rchq.loadTenant(ctx, query, nodes, nil,
			func(n *ReturnColliHistory, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	if query := rchq.withReturnColli; query != nil {
		if err := rchq.loadReturnColli(ctx, query, nodes, nil,
			func(n *ReturnColliHistory, e *ReturnColli) { n.Edges.ReturnColli = e }); err != nil {
			return nil, err
		}
	}
	for i := range rchq.loadTotal {
		if err := rchq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rchq *ReturnColliHistoryQuery) loadChangeHistory(ctx context.Context, query *ChangeHistoryQuery, nodes []*ReturnColliHistory, init func(*ReturnColliHistory), assign func(*ReturnColliHistory, *ChangeHistory)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ReturnColliHistory)
	for i := range nodes {
		if nodes[i].change_history_return_colli_history == nil {
			continue
		}
		fk := *nodes[i].change_history_return_colli_history
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(changehistory.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "change_history_return_colli_history" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rchq *ReturnColliHistoryQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*ReturnColliHistory, init func(*ReturnColliHistory), assign func(*ReturnColliHistory, *Tenant)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ReturnColliHistory)
	for i := range nodes {
		fk := nodes[i].TenantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rchq *ReturnColliHistoryQuery) loadReturnColli(ctx context.Context, query *ReturnColliQuery, nodes []*ReturnColliHistory, init func(*ReturnColliHistory), assign func(*ReturnColliHistory, *ReturnColli)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ReturnColliHistory)
	for i := range nodes {
		if nodes[i].return_colli_return_colli_history == nil {
			continue
		}
		fk := *nodes[i].return_colli_return_colli_history
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(returncolli.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "return_colli_return_colli_history" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rchq *ReturnColliHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rchq.querySpec()
	if len(rchq.modifiers) > 0 {
		_spec.Modifiers = rchq.modifiers
	}
	_spec.Node.Columns = rchq.ctx.Fields
	if len(rchq.ctx.Fields) > 0 {
		_spec.Unique = rchq.ctx.Unique != nil && *rchq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rchq.driver, _spec)
}

func (rchq *ReturnColliHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(returncollihistory.Table, returncollihistory.Columns, sqlgraph.NewFieldSpec(returncollihistory.FieldID, field.TypeString))
	_spec.From = rchq.sql
	if unique := rchq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rchq.path != nil {
		_spec.Unique = true
	}
	if fields := rchq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, returncollihistory.FieldID)
		for i := range fields {
			if fields[i] != returncollihistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rchq.withTenant != nil {
			_spec.Node.AddColumnOnce(returncollihistory.FieldTenantID)
		}
	}
	if ps := rchq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rchq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rchq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rchq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rchq *ReturnColliHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rchq.driver.Dialect())
	t1 := builder.Table(returncollihistory.Table)
	columns := rchq.ctx.Fields
	if len(columns) == 0 {
		columns = returncollihistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rchq.sql != nil {
		selector = rchq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rchq.ctx.Unique != nil && *rchq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rchq.predicates {
		p(selector)
	}
	for _, p := range rchq.order {
		p(selector)
	}
	if offset := rchq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rchq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ReturnColliHistoryGroupBy is the group-by builder for ReturnColliHistory entities.
type ReturnColliHistoryGroupBy struct {
	selector
	build *ReturnColliHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rchgb *ReturnColliHistoryGroupBy) Aggregate(fns ...AggregateFunc) *ReturnColliHistoryGroupBy {
	rchgb.fns = append(rchgb.fns, fns...)
	return rchgb
}

// Scan applies the selector query and scans the result into the given value.
func (rchgb *ReturnColliHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rchgb.build.ctx, ent.OpQueryGroupBy)
	if err := rchgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReturnColliHistoryQuery, *ReturnColliHistoryGroupBy](ctx, rchgb.build, rchgb, rchgb.build.inters, v)
}

func (rchgb *ReturnColliHistoryGroupBy) sqlScan(ctx context.Context, root *ReturnColliHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rchgb.fns))
	for _, fn := range rchgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rchgb.flds)+len(rchgb.fns))
		for _, f := range *rchgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rchgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rchgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ReturnColliHistorySelect is the builder for selecting fields of ReturnColliHistory entities.
type ReturnColliHistorySelect struct {
	*ReturnColliHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rchs *ReturnColliHistorySelect) Aggregate(fns ...AggregateFunc) *ReturnColliHistorySelect {
	rchs.fns = append(rchs.fns, fns...)
	return rchs
}

// Scan applies the selector query and scans the result into the given value.
func (rchs *ReturnColliHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rchs.ctx, ent.OpQuerySelect)
	if err := rchs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReturnColliHistoryQuery, *ReturnColliHistorySelect](ctx, rchs.ReturnColliHistoryQuery, rchs, rchs.inters, v)
}

func (rchs *ReturnColliHistorySelect) sqlScan(ctx context.Context, root *ReturnColliHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rchs.fns))
	for _, fn := range rchs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rchs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rchs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
