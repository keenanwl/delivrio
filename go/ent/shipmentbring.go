// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/shipment"
	"delivrio.io/go/ent/shipmentbring"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ShipmentBring is the model entity for the ShipmentBring schema.
type ShipmentBring struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// ConsignmentNumber holds the value of the "consignment_number" field.
	ConsignmentNumber string `json:"consignment_number,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShipmentBringQuery when eager-loading is set.
	Edges                   ShipmentBringEdges `json:"edges"`
	shipment_shipment_bring *pulid.ID
	selectValues            sql.SelectValues
}

// ShipmentBringEdges holds the relations/edges for other nodes in the graph.
type ShipmentBringEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Shipment holds the value of the shipment edge.
	Shipment *Shipment `json:"shipment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentBringEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// ShipmentOrErr returns the Shipment value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentBringEdges) ShipmentOrErr() (*Shipment, error) {
	if e.Shipment != nil {
		return e.Shipment, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: shipment.Label}
	}
	return nil, &NotLoadedError{edge: "shipment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShipmentBring) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shipmentbring.FieldID, shipmentbring.FieldTenantID:
			values[i] = new(pulid.ID)
		case shipmentbring.FieldConsignmentNumber:
			values[i] = new(sql.NullString)
		case shipmentbring.ForeignKeys[0]: // shipment_shipment_bring
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShipmentBring fields.
func (sb *ShipmentBring) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shipmentbring.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sb.ID = *value
			}
		case shipmentbring.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				sb.TenantID = *value
			}
		case shipmentbring.FieldConsignmentNumber:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field consignment_number", values[i])
			} else if value.Valid {
				sb.ConsignmentNumber = value.String
			}
		case shipmentbring.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field shipment_shipment_bring", values[i])
			} else if value.Valid {
				sb.shipment_shipment_bring = new(pulid.ID)
				*sb.shipment_shipment_bring = *value.S.(*pulid.ID)
			}
		default:
			sb.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShipmentBring.
// This includes values selected through modifiers, order, etc.
func (sb *ShipmentBring) Value(name string) (ent.Value, error) {
	return sb.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the ShipmentBring entity.
func (sb *ShipmentBring) QueryTenant() *TenantQuery {
	return NewShipmentBringClient(sb.config).QueryTenant(sb)
}

// QueryShipment queries the "shipment" edge of the ShipmentBring entity.
func (sb *ShipmentBring) QueryShipment() *ShipmentQuery {
	return NewShipmentBringClient(sb.config).QueryShipment(sb)
}

// Update returns a builder for updating this ShipmentBring.
// Note that you need to call ShipmentBring.Unwrap() before calling this method if this ShipmentBring
// was returned from a transaction, and the transaction was committed or rolled back.
func (sb *ShipmentBring) Update() *ShipmentBringUpdateOne {
	return NewShipmentBringClient(sb.config).UpdateOne(sb)
}

// Unwrap unwraps the ShipmentBring entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sb *ShipmentBring) Unwrap() *ShipmentBring {
	_tx, ok := sb.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShipmentBring is not a transactional entity")
	}
	sb.config.driver = _tx.drv
	return sb
}

// String implements the fmt.Stringer.
func (sb *ShipmentBring) String() string {
	var builder strings.Builder
	builder.WriteString("ShipmentBring(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sb.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", sb.TenantID))
	builder.WriteString(", ")
	builder.WriteString("consignment_number=")
	builder.WriteString(sb.ConsignmentNumber)
	builder.WriteByte(')')
	return builder.String()
}

// ShipmentBrings is a parsable slice of ShipmentBring.
type ShipmentBrings []*ShipmentBring
