// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/shipment"
	"delivrio.io/go/ent/shipmentdsv"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ShipmentDSV is the model entity for the ShipmentDSV schema.
type ShipmentDSV struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// BarcodeID holds the value of the "barcode_id" field.
	BarcodeID string `json:"barcode_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShipmentDSVQuery when eager-loading is set.
	Edges                 ShipmentDSVEdges `json:"edges"`
	shipment_shipment_dsv *pulid.ID
	selectValues          sql.SelectValues
}

// ShipmentDSVEdges holds the relations/edges for other nodes in the graph.
type ShipmentDSVEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Shipment holds the value of the shipment edge.
	Shipment *Shipment `json:"shipment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentDSVEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// ShipmentOrErr returns the Shipment value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentDSVEdges) ShipmentOrErr() (*Shipment, error) {
	if e.Shipment != nil {
		return e.Shipment, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: shipment.Label}
	}
	return nil, &NotLoadedError{edge: "shipment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShipmentDSV) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shipmentdsv.FieldID, shipmentdsv.FieldTenantID:
			values[i] = new(pulid.ID)
		case shipmentdsv.FieldBarcodeID:
			values[i] = new(sql.NullString)
		case shipmentdsv.ForeignKeys[0]: // shipment_shipment_dsv
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShipmentDSV fields.
func (sd *ShipmentDSV) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shipmentdsv.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sd.ID = *value
			}
		case shipmentdsv.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				sd.TenantID = *value
			}
		case shipmentdsv.FieldBarcodeID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field barcode_id", values[i])
			} else if value.Valid {
				sd.BarcodeID = value.String
			}
		case shipmentdsv.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field shipment_shipment_dsv", values[i])
			} else if value.Valid {
				sd.shipment_shipment_dsv = new(pulid.ID)
				*sd.shipment_shipment_dsv = *value.S.(*pulid.ID)
			}
		default:
			sd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShipmentDSV.
// This includes values selected through modifiers, order, etc.
func (sd *ShipmentDSV) Value(name string) (ent.Value, error) {
	return sd.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the ShipmentDSV entity.
func (sd *ShipmentDSV) QueryTenant() *TenantQuery {
	return NewShipmentDSVClient(sd.config).QueryTenant(sd)
}

// QueryShipment queries the "shipment" edge of the ShipmentDSV entity.
func (sd *ShipmentDSV) QueryShipment() *ShipmentQuery {
	return NewShipmentDSVClient(sd.config).QueryShipment(sd)
}

// Update returns a builder for updating this ShipmentDSV.
// Note that you need to call ShipmentDSV.Unwrap() before calling this method if this ShipmentDSV
// was returned from a transaction, and the transaction was committed or rolled back.
func (sd *ShipmentDSV) Update() *ShipmentDSVUpdateOne {
	return NewShipmentDSVClient(sd.config).UpdateOne(sd)
}

// Unwrap unwraps the ShipmentDSV entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sd *ShipmentDSV) Unwrap() *ShipmentDSV {
	_tx, ok := sd.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShipmentDSV is not a transactional entity")
	}
	sd.config.driver = _tx.drv
	return sd
}

// String implements the fmt.Stringer.
func (sd *ShipmentDSV) String() string {
	var builder strings.Builder
	builder.WriteString("ShipmentDSV(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sd.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", sd.TenantID))
	builder.WriteString(", ")
	builder.WriteString("barcode_id=")
	builder.WriteString(sd.BarcodeID)
	builder.WriteByte(')')
	return builder.String()
}

// ShipmentDSVs is a parsable slice of ShipmentDSV.
type ShipmentDSVs []*ShipmentDSV
