// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/carrieradditionalservicedsv"
	"delivrio.io/go/ent/carrierservice"
	"delivrio.io/go/ent/carrierservicedsv"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CarrierServiceDSVQuery is the builder for querying CarrierServiceDSV entities.
type CarrierServiceDSVQuery struct {
	config
	ctx                                  *QueryContext
	order                                []carrierservicedsv.OrderOption
	inters                               []Interceptor
	predicates                           []predicate.CarrierServiceDSV
	withCarrierService                   *CarrierServiceQuery
	withCarrierAdditionalServiceDSV      *CarrierAdditionalServiceDSVQuery
	withFKs                              bool
	modifiers                            []func(*sql.Selector)
	loadTotal                            []func(context.Context, []*CarrierServiceDSV) error
	withNamedCarrierAdditionalServiceDSV map[string]*CarrierAdditionalServiceDSVQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarrierServiceDSVQuery builder.
func (csdq *CarrierServiceDSVQuery) Where(ps ...predicate.CarrierServiceDSV) *CarrierServiceDSVQuery {
	csdq.predicates = append(csdq.predicates, ps...)
	return csdq
}

// Limit the number of records to be returned by this query.
func (csdq *CarrierServiceDSVQuery) Limit(limit int) *CarrierServiceDSVQuery {
	csdq.ctx.Limit = &limit
	return csdq
}

// Offset to start from.
func (csdq *CarrierServiceDSVQuery) Offset(offset int) *CarrierServiceDSVQuery {
	csdq.ctx.Offset = &offset
	return csdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (csdq *CarrierServiceDSVQuery) Unique(unique bool) *CarrierServiceDSVQuery {
	csdq.ctx.Unique = &unique
	return csdq
}

// Order specifies how the records should be ordered.
func (csdq *CarrierServiceDSVQuery) Order(o ...carrierservicedsv.OrderOption) *CarrierServiceDSVQuery {
	csdq.order = append(csdq.order, o...)
	return csdq
}

// QueryCarrierService chains the current query on the "carrier_service" edge.
func (csdq *CarrierServiceDSVQuery) QueryCarrierService() *CarrierServiceQuery {
	query := (&CarrierServiceClient{config: csdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carrierservicedsv.Table, carrierservicedsv.FieldID, selector),
			sqlgraph.To(carrierservice.Table, carrierservice.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, carrierservicedsv.CarrierServiceTable, carrierservicedsv.CarrierServiceColumn),
		)
		fromU = sqlgraph.SetNeighbors(csdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCarrierAdditionalServiceDSV chains the current query on the "carrier_additional_service_dsv" edge.
func (csdq *CarrierServiceDSVQuery) QueryCarrierAdditionalServiceDSV() *CarrierAdditionalServiceDSVQuery {
	query := (&CarrierAdditionalServiceDSVClient{config: csdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carrierservicedsv.Table, carrierservicedsv.FieldID, selector),
			sqlgraph.To(carrieradditionalservicedsv.Table, carrieradditionalservicedsv.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, carrierservicedsv.CarrierAdditionalServiceDSVTable, carrierservicedsv.CarrierAdditionalServiceDSVPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(csdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CarrierServiceDSV entity from the query.
// Returns a *NotFoundError when no CarrierServiceDSV was found.
func (csdq *CarrierServiceDSVQuery) First(ctx context.Context) (*CarrierServiceDSV, error) {
	nodes, err := csdq.Limit(1).All(setContextOp(ctx, csdq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{carrierservicedsv.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (csdq *CarrierServiceDSVQuery) FirstX(ctx context.Context) *CarrierServiceDSV {
	node, err := csdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarrierServiceDSV ID from the query.
// Returns a *NotFoundError when no CarrierServiceDSV ID was found.
func (csdq *CarrierServiceDSVQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = csdq.Limit(1).IDs(setContextOp(ctx, csdq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{carrierservicedsv.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (csdq *CarrierServiceDSVQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := csdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarrierServiceDSV entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarrierServiceDSV entity is found.
// Returns a *NotFoundError when no CarrierServiceDSV entities are found.
func (csdq *CarrierServiceDSVQuery) Only(ctx context.Context) (*CarrierServiceDSV, error) {
	nodes, err := csdq.Limit(2).All(setContextOp(ctx, csdq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{carrierservicedsv.Label}
	default:
		return nil, &NotSingularError{carrierservicedsv.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (csdq *CarrierServiceDSVQuery) OnlyX(ctx context.Context) *CarrierServiceDSV {
	node, err := csdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarrierServiceDSV ID in the query.
// Returns a *NotSingularError when more than one CarrierServiceDSV ID is found.
// Returns a *NotFoundError when no entities are found.
func (csdq *CarrierServiceDSVQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = csdq.Limit(2).IDs(setContextOp(ctx, csdq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{carrierservicedsv.Label}
	default:
		err = &NotSingularError{carrierservicedsv.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (csdq *CarrierServiceDSVQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := csdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarrierServiceDSVs.
func (csdq *CarrierServiceDSVQuery) All(ctx context.Context) ([]*CarrierServiceDSV, error) {
	ctx = setContextOp(ctx, csdq.ctx, ent.OpQueryAll)
	if err := csdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarrierServiceDSV, *CarrierServiceDSVQuery]()
	return withInterceptors[[]*CarrierServiceDSV](ctx, csdq, qr, csdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (csdq *CarrierServiceDSVQuery) AllX(ctx context.Context) []*CarrierServiceDSV {
	nodes, err := csdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarrierServiceDSV IDs.
func (csdq *CarrierServiceDSVQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if csdq.ctx.Unique == nil && csdq.path != nil {
		csdq.Unique(true)
	}
	ctx = setContextOp(ctx, csdq.ctx, ent.OpQueryIDs)
	if err = csdq.Select(carrierservicedsv.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (csdq *CarrierServiceDSVQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := csdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (csdq *CarrierServiceDSVQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, csdq.ctx, ent.OpQueryCount)
	if err := csdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, csdq, querierCount[*CarrierServiceDSVQuery](), csdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (csdq *CarrierServiceDSVQuery) CountX(ctx context.Context) int {
	count, err := csdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (csdq *CarrierServiceDSVQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, csdq.ctx, ent.OpQueryExist)
	switch _, err := csdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (csdq *CarrierServiceDSVQuery) ExistX(ctx context.Context) bool {
	exist, err := csdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarrierServiceDSVQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (csdq *CarrierServiceDSVQuery) Clone() *CarrierServiceDSVQuery {
	if csdq == nil {
		return nil
	}
	return &CarrierServiceDSVQuery{
		config:                          csdq.config,
		ctx:                             csdq.ctx.Clone(),
		order:                           append([]carrierservicedsv.OrderOption{}, csdq.order...),
		inters:                          append([]Interceptor{}, csdq.inters...),
		predicates:                      append([]predicate.CarrierServiceDSV{}, csdq.predicates...),
		withCarrierService:              csdq.withCarrierService.Clone(),
		withCarrierAdditionalServiceDSV: csdq.withCarrierAdditionalServiceDSV.Clone(),
		// clone intermediate query.
		sql:  csdq.sql.Clone(),
		path: csdq.path,
	}
}

// WithCarrierService tells the query-builder to eager-load the nodes that are connected to
// the "carrier_service" edge. The optional arguments are used to configure the query builder of the edge.
func (csdq *CarrierServiceDSVQuery) WithCarrierService(opts ...func(*CarrierServiceQuery)) *CarrierServiceDSVQuery {
	query := (&CarrierServiceClient{config: csdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csdq.withCarrierService = query
	return csdq
}

// WithCarrierAdditionalServiceDSV tells the query-builder to eager-load the nodes that are connected to
// the "carrier_additional_service_dsv" edge. The optional arguments are used to configure the query builder of the edge.
func (csdq *CarrierServiceDSVQuery) WithCarrierAdditionalServiceDSV(opts ...func(*CarrierAdditionalServiceDSVQuery)) *CarrierServiceDSVQuery {
	query := (&CarrierAdditionalServiceDSVClient{config: csdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csdq.withCarrierAdditionalServiceDSV = query
	return csdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (csdq *CarrierServiceDSVQuery) GroupBy(field string, fields ...string) *CarrierServiceDSVGroupBy {
	csdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarrierServiceDSVGroupBy{build: csdq}
	grbuild.flds = &csdq.ctx.Fields
	grbuild.label = carrierservicedsv.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (csdq *CarrierServiceDSVQuery) Select(fields ...string) *CarrierServiceDSVSelect {
	csdq.ctx.Fields = append(csdq.ctx.Fields, fields...)
	sbuild := &CarrierServiceDSVSelect{CarrierServiceDSVQuery: csdq}
	sbuild.label = carrierservicedsv.Label
	sbuild.flds, sbuild.scan = &csdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarrierServiceDSVSelect configured with the given aggregations.
func (csdq *CarrierServiceDSVQuery) Aggregate(fns ...AggregateFunc) *CarrierServiceDSVSelect {
	return csdq.Select().Aggregate(fns...)
}

func (csdq *CarrierServiceDSVQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range csdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, csdq); err != nil {
				return err
			}
		}
	}
	for _, f := range csdq.ctx.Fields {
		if !carrierservicedsv.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if csdq.path != nil {
		prev, err := csdq.path(ctx)
		if err != nil {
			return err
		}
		csdq.sql = prev
	}
	if carrierservicedsv.Policy == nil {
		return errors.New("ent: uninitialized carrierservicedsv.Policy (forgotten import ent/runtime?)")
	}
	if err := carrierservicedsv.Policy.EvalQuery(ctx, csdq); err != nil {
		return err
	}
	return nil
}

func (csdq *CarrierServiceDSVQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarrierServiceDSV, error) {
	var (
		nodes       = []*CarrierServiceDSV{}
		withFKs     = csdq.withFKs
		_spec       = csdq.querySpec()
		loadedTypes = [2]bool{
			csdq.withCarrierService != nil,
			csdq.withCarrierAdditionalServiceDSV != nil,
		}
	)
	if csdq.withCarrierService != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, carrierservicedsv.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarrierServiceDSV).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarrierServiceDSV{config: csdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(csdq.modifiers) > 0 {
		_spec.Modifiers = csdq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, csdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := csdq.withCarrierService; query != nil {
		if err := csdq.loadCarrierService(ctx, query, nodes, nil,
			func(n *CarrierServiceDSV, e *CarrierService) { n.Edges.CarrierService = e }); err != nil {
			return nil, err
		}
	}
	if query := csdq.withCarrierAdditionalServiceDSV; query != nil {
		if err := csdq.loadCarrierAdditionalServiceDSV(ctx, query, nodes,
			func(n *CarrierServiceDSV) { n.Edges.CarrierAdditionalServiceDSV = []*CarrierAdditionalServiceDSV{} },
			func(n *CarrierServiceDSV, e *CarrierAdditionalServiceDSV) {
				n.Edges.CarrierAdditionalServiceDSV = append(n.Edges.CarrierAdditionalServiceDSV, e)
			}); err != nil {
			return nil, err
		}
	}
	for name, query := range csdq.withNamedCarrierAdditionalServiceDSV {
		if err := csdq.loadCarrierAdditionalServiceDSV(ctx, query, nodes,
			func(n *CarrierServiceDSV) { n.appendNamedCarrierAdditionalServiceDSV(name) },
			func(n *CarrierServiceDSV, e *CarrierAdditionalServiceDSV) {
				n.appendNamedCarrierAdditionalServiceDSV(name, e)
			}); err != nil {
			return nil, err
		}
	}
	for i := range csdq.loadTotal {
		if err := csdq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (csdq *CarrierServiceDSVQuery) loadCarrierService(ctx context.Context, query *CarrierServiceQuery, nodes []*CarrierServiceDSV, init func(*CarrierServiceDSV), assign func(*CarrierServiceDSV, *CarrierService)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*CarrierServiceDSV)
	for i := range nodes {
		if nodes[i].carrier_service_carrier_service_dsv == nil {
			continue
		}
		fk := *nodes[i].carrier_service_carrier_service_dsv
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(carrierservice.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "carrier_service_carrier_service_dsv" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (csdq *CarrierServiceDSVQuery) loadCarrierAdditionalServiceDSV(ctx context.Context, query *CarrierAdditionalServiceDSVQuery, nodes []*CarrierServiceDSV, init func(*CarrierServiceDSV), assign func(*CarrierServiceDSV, *CarrierAdditionalServiceDSV)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[pulid.ID]*CarrierServiceDSV)
	nids := make(map[pulid.ID]map[*CarrierServiceDSV]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(carrierservicedsv.CarrierAdditionalServiceDSVTable)
		s.Join(joinT).On(s.C(carrieradditionalservicedsv.FieldID), joinT.C(carrierservicedsv.CarrierAdditionalServiceDSVPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(carrierservicedsv.CarrierAdditionalServiceDSVPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(carrierservicedsv.CarrierAdditionalServiceDSVPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(pulid.ID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*pulid.ID)
				inValue := *values[1].(*pulid.ID)
				if nids[inValue] == nil {
					nids[inValue] = map[*CarrierServiceDSV]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*CarrierAdditionalServiceDSV](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "carrier_additional_service_dsv" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (csdq *CarrierServiceDSVQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := csdq.querySpec()
	if len(csdq.modifiers) > 0 {
		_spec.Modifiers = csdq.modifiers
	}
	_spec.Node.Columns = csdq.ctx.Fields
	if len(csdq.ctx.Fields) > 0 {
		_spec.Unique = csdq.ctx.Unique != nil && *csdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, csdq.driver, _spec)
}

func (csdq *CarrierServiceDSVQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(carrierservicedsv.Table, carrierservicedsv.Columns, sqlgraph.NewFieldSpec(carrierservicedsv.FieldID, field.TypeString))
	_spec.From = csdq.sql
	if unique := csdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if csdq.path != nil {
		_spec.Unique = true
	}
	if fields := csdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, carrierservicedsv.FieldID)
		for i := range fields {
			if fields[i] != carrierservicedsv.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := csdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := csdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := csdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := csdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (csdq *CarrierServiceDSVQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(csdq.driver.Dialect())
	t1 := builder.Table(carrierservicedsv.Table)
	columns := csdq.ctx.Fields
	if len(columns) == 0 {
		columns = carrierservicedsv.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if csdq.sql != nil {
		selector = csdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if csdq.ctx.Unique != nil && *csdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range csdq.predicates {
		p(selector)
	}
	for _, p := range csdq.order {
		p(selector)
	}
	if offset := csdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := csdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedCarrierAdditionalServiceDSV tells the query-builder to eager-load the nodes that are connected to the "carrier_additional_service_dsv"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (csdq *CarrierServiceDSVQuery) WithNamedCarrierAdditionalServiceDSV(name string, opts ...func(*CarrierAdditionalServiceDSVQuery)) *CarrierServiceDSVQuery {
	query := (&CarrierAdditionalServiceDSVClient{config: csdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if csdq.withNamedCarrierAdditionalServiceDSV == nil {
		csdq.withNamedCarrierAdditionalServiceDSV = make(map[string]*CarrierAdditionalServiceDSVQuery)
	}
	csdq.withNamedCarrierAdditionalServiceDSV[name] = query
	return csdq
}

// CarrierServiceDSVGroupBy is the group-by builder for CarrierServiceDSV entities.
type CarrierServiceDSVGroupBy struct {
	selector
	build *CarrierServiceDSVQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (csdgb *CarrierServiceDSVGroupBy) Aggregate(fns ...AggregateFunc) *CarrierServiceDSVGroupBy {
	csdgb.fns = append(csdgb.fns, fns...)
	return csdgb
}

// Scan applies the selector query and scans the result into the given value.
func (csdgb *CarrierServiceDSVGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csdgb.build.ctx, ent.OpQueryGroupBy)
	if err := csdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarrierServiceDSVQuery, *CarrierServiceDSVGroupBy](ctx, csdgb.build, csdgb, csdgb.build.inters, v)
}

func (csdgb *CarrierServiceDSVGroupBy) sqlScan(ctx context.Context, root *CarrierServiceDSVQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(csdgb.fns))
	for _, fn := range csdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*csdgb.flds)+len(csdgb.fns))
		for _, f := range *csdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*csdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarrierServiceDSVSelect is the builder for selecting fields of CarrierServiceDSV entities.
type CarrierServiceDSVSelect struct {
	*CarrierServiceDSVQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (csds *CarrierServiceDSVSelect) Aggregate(fns ...AggregateFunc) *CarrierServiceDSVSelect {
	csds.fns = append(csds.fns, fns...)
	return csds
}

// Scan applies the selector query and scans the result into the given value.
func (csds *CarrierServiceDSVSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csds.ctx, ent.OpQuerySelect)
	if err := csds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarrierServiceDSVQuery, *CarrierServiceDSVSelect](ctx, csds.CarrierServiceDSVQuery, csds, csds.inters, v)
}

func (csds *CarrierServiceDSVSelect) sqlScan(ctx context.Context, root *CarrierServiceDSVQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(csds.fns))
	for _, fn := range csds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*csds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
