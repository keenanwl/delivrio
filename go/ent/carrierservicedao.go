// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/carrierservice"
	"delivrio.io/go/ent/carrierservicedao"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CarrierServiceDAO is the model entity for the CarrierServiceDAO schema.
type CarrierServiceDAO struct {
	config
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CarrierServiceDAOQuery when eager-loading is set.
	Edges                               CarrierServiceDAOEdges `json:"edges"`
	carrier_service_carrier_service_dao *pulid.ID
	selectValues                        sql.SelectValues
}

// CarrierServiceDAOEdges holds the relations/edges for other nodes in the graph.
type CarrierServiceDAOEdges struct {
	// CarrierService holds the value of the carrier_service edge.
	CarrierService *CarrierService `json:"carrier_service,omitempty"`
	// CarrierAdditionalServiceDAO holds the value of the carrier_additional_service_dao edge.
	CarrierAdditionalServiceDAO []*CarrierAdditionalServiceDAO `json:"carrier_additional_service_dao,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int

	namedCarrierAdditionalServiceDAO map[string][]*CarrierAdditionalServiceDAO
}

// CarrierServiceOrErr returns the CarrierService value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierServiceDAOEdges) CarrierServiceOrErr() (*CarrierService, error) {
	if e.CarrierService != nil {
		return e.CarrierService, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: carrierservice.Label}
	}
	return nil, &NotLoadedError{edge: "carrier_service"}
}

// CarrierAdditionalServiceDAOOrErr returns the CarrierAdditionalServiceDAO value or an error if the edge
// was not loaded in eager-loading.
func (e CarrierServiceDAOEdges) CarrierAdditionalServiceDAOOrErr() ([]*CarrierAdditionalServiceDAO, error) {
	if e.loadedTypes[1] {
		return e.CarrierAdditionalServiceDAO, nil
	}
	return nil, &NotLoadedError{edge: "carrier_additional_service_dao"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CarrierServiceDAO) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case carrierservicedao.FieldID:
			values[i] = new(pulid.ID)
		case carrierservicedao.ForeignKeys[0]: // carrier_service_carrier_service_dao
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CarrierServiceDAO fields.
func (csd *CarrierServiceDAO) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case carrierservicedao.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				csd.ID = *value
			}
		case carrierservicedao.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field carrier_service_carrier_service_dao", values[i])
			} else if value.Valid {
				csd.carrier_service_carrier_service_dao = new(pulid.ID)
				*csd.carrier_service_carrier_service_dao = *value.S.(*pulid.ID)
			}
		default:
			csd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CarrierServiceDAO.
// This includes values selected through modifiers, order, etc.
func (csd *CarrierServiceDAO) Value(name string) (ent.Value, error) {
	return csd.selectValues.Get(name)
}

// QueryCarrierService queries the "carrier_service" edge of the CarrierServiceDAO entity.
func (csd *CarrierServiceDAO) QueryCarrierService() *CarrierServiceQuery {
	return NewCarrierServiceDAOClient(csd.config).QueryCarrierService(csd)
}

// QueryCarrierAdditionalServiceDAO queries the "carrier_additional_service_dao" edge of the CarrierServiceDAO entity.
func (csd *CarrierServiceDAO) QueryCarrierAdditionalServiceDAO() *CarrierAdditionalServiceDAOQuery {
	return NewCarrierServiceDAOClient(csd.config).QueryCarrierAdditionalServiceDAO(csd)
}

// Update returns a builder for updating this CarrierServiceDAO.
// Note that you need to call CarrierServiceDAO.Unwrap() before calling this method if this CarrierServiceDAO
// was returned from a transaction, and the transaction was committed or rolled back.
func (csd *CarrierServiceDAO) Update() *CarrierServiceDAOUpdateOne {
	return NewCarrierServiceDAOClient(csd.config).UpdateOne(csd)
}

// Unwrap unwraps the CarrierServiceDAO entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (csd *CarrierServiceDAO) Unwrap() *CarrierServiceDAO {
	_tx, ok := csd.config.driver.(*txDriver)
	if !ok {
		panic("ent: CarrierServiceDAO is not a transactional entity")
	}
	csd.config.driver = _tx.drv
	return csd
}

// String implements the fmt.Stringer.
func (csd *CarrierServiceDAO) String() string {
	var builder strings.Builder
	builder.WriteString("CarrierServiceDAO(")
	builder.WriteString(fmt.Sprintf("id=%v", csd.ID))
	builder.WriteByte(')')
	return builder.String()
}

// NamedCarrierAdditionalServiceDAO returns the CarrierAdditionalServiceDAO named value or an error if the edge was not
// loaded in eager-loading with this name.
func (csd *CarrierServiceDAO) NamedCarrierAdditionalServiceDAO(name string) ([]*CarrierAdditionalServiceDAO, error) {
	if csd.Edges.namedCarrierAdditionalServiceDAO == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := csd.Edges.namedCarrierAdditionalServiceDAO[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (csd *CarrierServiceDAO) appendNamedCarrierAdditionalServiceDAO(name string, edges ...*CarrierAdditionalServiceDAO) {
	if csd.Edges.namedCarrierAdditionalServiceDAO == nil {
		csd.Edges.namedCarrierAdditionalServiceDAO = make(map[string][]*CarrierAdditionalServiceDAO)
	}
	if len(edges) == 0 {
		csd.Edges.namedCarrierAdditionalServiceDAO[name] = []*CarrierAdditionalServiceDAO{}
	} else {
		csd.Edges.namedCarrierAdditionalServiceDAO[name] = append(csd.Edges.namedCarrierAdditionalServiceDAO[name], edges...)
	}
}

// CarrierServiceDAOs is a parsable slice of CarrierServiceDAO.
type CarrierServiceDAOs []*CarrierServiceDAO
