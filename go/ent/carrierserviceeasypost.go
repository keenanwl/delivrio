// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/carrierservice"
	"delivrio.io/go/ent/carrierserviceeasypost"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CarrierServiceEasyPost is the model entity for the CarrierServiceEasyPost schema.
type CarrierServiceEasyPost struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// APIKey holds the value of the "api_key" field.
	APIKey carrierserviceeasypost.APIKey `json:"api_key,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CarrierServiceEasyPostQuery when eager-loading is set.
	Edges                                  CarrierServiceEasyPostEdges `json:"edges"`
	carrier_service_carrier_serv_easy_post *pulid.ID
	selectValues                           sql.SelectValues
}

// CarrierServiceEasyPostEdges holds the relations/edges for other nodes in the graph.
type CarrierServiceEasyPostEdges struct {
	// CarrierService holds the value of the carrier_service edge.
	CarrierService *CarrierService `json:"carrier_service,omitempty"`
	// CarrierAddServEasyPost holds the value of the carrier_add_serv_easy_post edge.
	CarrierAddServEasyPost []*CarrierAdditionalServiceEasyPost `json:"carrier_add_serv_easy_post,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int

	namedCarrierAddServEasyPost map[string][]*CarrierAdditionalServiceEasyPost
}

// CarrierServiceOrErr returns the CarrierService value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierServiceEasyPostEdges) CarrierServiceOrErr() (*CarrierService, error) {
	if e.CarrierService != nil {
		return e.CarrierService, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: carrierservice.Label}
	}
	return nil, &NotLoadedError{edge: "carrier_service"}
}

// CarrierAddServEasyPostOrErr returns the CarrierAddServEasyPost value or an error if the edge
// was not loaded in eager-loading.
func (e CarrierServiceEasyPostEdges) CarrierAddServEasyPostOrErr() ([]*CarrierAdditionalServiceEasyPost, error) {
	if e.loadedTypes[1] {
		return e.CarrierAddServEasyPost, nil
	}
	return nil, &NotLoadedError{edge: "carrier_add_serv_easy_post"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CarrierServiceEasyPost) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case carrierserviceeasypost.FieldID:
			values[i] = new(pulid.ID)
		case carrierserviceeasypost.FieldAPIKey:
			values[i] = new(sql.NullString)
		case carrierserviceeasypost.ForeignKeys[0]: // carrier_service_carrier_serv_easy_post
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CarrierServiceEasyPost fields.
func (csep *CarrierServiceEasyPost) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case carrierserviceeasypost.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				csep.ID = *value
			}
		case carrierserviceeasypost.FieldAPIKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field api_key", values[i])
			} else if value.Valid {
				csep.APIKey = carrierserviceeasypost.APIKey(value.String)
			}
		case carrierserviceeasypost.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field carrier_service_carrier_serv_easy_post", values[i])
			} else if value.Valid {
				csep.carrier_service_carrier_serv_easy_post = new(pulid.ID)
				*csep.carrier_service_carrier_serv_easy_post = *value.S.(*pulid.ID)
			}
		default:
			csep.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CarrierServiceEasyPost.
// This includes values selected through modifiers, order, etc.
func (csep *CarrierServiceEasyPost) Value(name string) (ent.Value, error) {
	return csep.selectValues.Get(name)
}

// QueryCarrierService queries the "carrier_service" edge of the CarrierServiceEasyPost entity.
func (csep *CarrierServiceEasyPost) QueryCarrierService() *CarrierServiceQuery {
	return NewCarrierServiceEasyPostClient(csep.config).QueryCarrierService(csep)
}

// QueryCarrierAddServEasyPost queries the "carrier_add_serv_easy_post" edge of the CarrierServiceEasyPost entity.
func (csep *CarrierServiceEasyPost) QueryCarrierAddServEasyPost() *CarrierAdditionalServiceEasyPostQuery {
	return NewCarrierServiceEasyPostClient(csep.config).QueryCarrierAddServEasyPost(csep)
}

// Update returns a builder for updating this CarrierServiceEasyPost.
// Note that you need to call CarrierServiceEasyPost.Unwrap() before calling this method if this CarrierServiceEasyPost
// was returned from a transaction, and the transaction was committed or rolled back.
func (csep *CarrierServiceEasyPost) Update() *CarrierServiceEasyPostUpdateOne {
	return NewCarrierServiceEasyPostClient(csep.config).UpdateOne(csep)
}

// Unwrap unwraps the CarrierServiceEasyPost entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (csep *CarrierServiceEasyPost) Unwrap() *CarrierServiceEasyPost {
	_tx, ok := csep.config.driver.(*txDriver)
	if !ok {
		panic("ent: CarrierServiceEasyPost is not a transactional entity")
	}
	csep.config.driver = _tx.drv
	return csep
}

// String implements the fmt.Stringer.
func (csep *CarrierServiceEasyPost) String() string {
	var builder strings.Builder
	builder.WriteString("CarrierServiceEasyPost(")
	builder.WriteString(fmt.Sprintf("id=%v, ", csep.ID))
	builder.WriteString("api_key=")
	builder.WriteString(fmt.Sprintf("%v", csep.APIKey))
	builder.WriteByte(')')
	return builder.String()
}

// NamedCarrierAddServEasyPost returns the CarrierAddServEasyPost named value or an error if the edge was not
// loaded in eager-loading with this name.
func (csep *CarrierServiceEasyPost) NamedCarrierAddServEasyPost(name string) ([]*CarrierAdditionalServiceEasyPost, error) {
	if csep.Edges.namedCarrierAddServEasyPost == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := csep.Edges.namedCarrierAddServEasyPost[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (csep *CarrierServiceEasyPost) appendNamedCarrierAddServEasyPost(name string, edges ...*CarrierAdditionalServiceEasyPost) {
	if csep.Edges.namedCarrierAddServEasyPost == nil {
		csep.Edges.namedCarrierAddServEasyPost = make(map[string][]*CarrierAdditionalServiceEasyPost)
	}
	if len(edges) == 0 {
		csep.Edges.namedCarrierAddServEasyPost[name] = []*CarrierAdditionalServiceEasyPost{}
	} else {
		csep.Edges.namedCarrierAddServEasyPost[name] = append(csep.Edges.namedCarrierAddServEasyPost[name], edges...)
	}
}

// CarrierServiceEasyPosts is a parsable slice of CarrierServiceEasyPost.
type CarrierServiceEasyPosts []*CarrierServiceEasyPost
