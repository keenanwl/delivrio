// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/carrieradditionalserviceeasypost"
	"delivrio.io/go/ent/carrierservice"
	"delivrio.io/go/ent/carrierserviceeasypost"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CarrierServiceEasyPostQuery is the builder for querying CarrierServiceEasyPost entities.
type CarrierServiceEasyPostQuery struct {
	config
	ctx                             *QueryContext
	order                           []carrierserviceeasypost.OrderOption
	inters                          []Interceptor
	predicates                      []predicate.CarrierServiceEasyPost
	withCarrierService              *CarrierServiceQuery
	withCarrierAddServEasyPost      *CarrierAdditionalServiceEasyPostQuery
	withFKs                         bool
	modifiers                       []func(*sql.Selector)
	loadTotal                       []func(context.Context, []*CarrierServiceEasyPost) error
	withNamedCarrierAddServEasyPost map[string]*CarrierAdditionalServiceEasyPostQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarrierServiceEasyPostQuery builder.
func (csepq *CarrierServiceEasyPostQuery) Where(ps ...predicate.CarrierServiceEasyPost) *CarrierServiceEasyPostQuery {
	csepq.predicates = append(csepq.predicates, ps...)
	return csepq
}

// Limit the number of records to be returned by this query.
func (csepq *CarrierServiceEasyPostQuery) Limit(limit int) *CarrierServiceEasyPostQuery {
	csepq.ctx.Limit = &limit
	return csepq
}

// Offset to start from.
func (csepq *CarrierServiceEasyPostQuery) Offset(offset int) *CarrierServiceEasyPostQuery {
	csepq.ctx.Offset = &offset
	return csepq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (csepq *CarrierServiceEasyPostQuery) Unique(unique bool) *CarrierServiceEasyPostQuery {
	csepq.ctx.Unique = &unique
	return csepq
}

// Order specifies how the records should be ordered.
func (csepq *CarrierServiceEasyPostQuery) Order(o ...carrierserviceeasypost.OrderOption) *CarrierServiceEasyPostQuery {
	csepq.order = append(csepq.order, o...)
	return csepq
}

// QueryCarrierService chains the current query on the "carrier_service" edge.
func (csepq *CarrierServiceEasyPostQuery) QueryCarrierService() *CarrierServiceQuery {
	query := (&CarrierServiceClient{config: csepq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csepq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csepq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carrierserviceeasypost.Table, carrierserviceeasypost.FieldID, selector),
			sqlgraph.To(carrierservice.Table, carrierservice.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, carrierserviceeasypost.CarrierServiceTable, carrierserviceeasypost.CarrierServiceColumn),
		)
		fromU = sqlgraph.SetNeighbors(csepq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCarrierAddServEasyPost chains the current query on the "carrier_add_serv_easy_post" edge.
func (csepq *CarrierServiceEasyPostQuery) QueryCarrierAddServEasyPost() *CarrierAdditionalServiceEasyPostQuery {
	query := (&CarrierAdditionalServiceEasyPostClient{config: csepq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csepq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csepq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carrierserviceeasypost.Table, carrierserviceeasypost.FieldID, selector),
			sqlgraph.To(carrieradditionalserviceeasypost.Table, carrieradditionalserviceeasypost.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, carrierserviceeasypost.CarrierAddServEasyPostTable, carrierserviceeasypost.CarrierAddServEasyPostPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(csepq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CarrierServiceEasyPost entity from the query.
// Returns a *NotFoundError when no CarrierServiceEasyPost was found.
func (csepq *CarrierServiceEasyPostQuery) First(ctx context.Context) (*CarrierServiceEasyPost, error) {
	nodes, err := csepq.Limit(1).All(setContextOp(ctx, csepq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{carrierserviceeasypost.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (csepq *CarrierServiceEasyPostQuery) FirstX(ctx context.Context) *CarrierServiceEasyPost {
	node, err := csepq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarrierServiceEasyPost ID from the query.
// Returns a *NotFoundError when no CarrierServiceEasyPost ID was found.
func (csepq *CarrierServiceEasyPostQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = csepq.Limit(1).IDs(setContextOp(ctx, csepq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{carrierserviceeasypost.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (csepq *CarrierServiceEasyPostQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := csepq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarrierServiceEasyPost entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarrierServiceEasyPost entity is found.
// Returns a *NotFoundError when no CarrierServiceEasyPost entities are found.
func (csepq *CarrierServiceEasyPostQuery) Only(ctx context.Context) (*CarrierServiceEasyPost, error) {
	nodes, err := csepq.Limit(2).All(setContextOp(ctx, csepq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{carrierserviceeasypost.Label}
	default:
		return nil, &NotSingularError{carrierserviceeasypost.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (csepq *CarrierServiceEasyPostQuery) OnlyX(ctx context.Context) *CarrierServiceEasyPost {
	node, err := csepq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarrierServiceEasyPost ID in the query.
// Returns a *NotSingularError when more than one CarrierServiceEasyPost ID is found.
// Returns a *NotFoundError when no entities are found.
func (csepq *CarrierServiceEasyPostQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = csepq.Limit(2).IDs(setContextOp(ctx, csepq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{carrierserviceeasypost.Label}
	default:
		err = &NotSingularError{carrierserviceeasypost.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (csepq *CarrierServiceEasyPostQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := csepq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarrierServiceEasyPosts.
func (csepq *CarrierServiceEasyPostQuery) All(ctx context.Context) ([]*CarrierServiceEasyPost, error) {
	ctx = setContextOp(ctx, csepq.ctx, ent.OpQueryAll)
	if err := csepq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarrierServiceEasyPost, *CarrierServiceEasyPostQuery]()
	return withInterceptors[[]*CarrierServiceEasyPost](ctx, csepq, qr, csepq.inters)
}

// AllX is like All, but panics if an error occurs.
func (csepq *CarrierServiceEasyPostQuery) AllX(ctx context.Context) []*CarrierServiceEasyPost {
	nodes, err := csepq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarrierServiceEasyPost IDs.
func (csepq *CarrierServiceEasyPostQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if csepq.ctx.Unique == nil && csepq.path != nil {
		csepq.Unique(true)
	}
	ctx = setContextOp(ctx, csepq.ctx, ent.OpQueryIDs)
	if err = csepq.Select(carrierserviceeasypost.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (csepq *CarrierServiceEasyPostQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := csepq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (csepq *CarrierServiceEasyPostQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, csepq.ctx, ent.OpQueryCount)
	if err := csepq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, csepq, querierCount[*CarrierServiceEasyPostQuery](), csepq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (csepq *CarrierServiceEasyPostQuery) CountX(ctx context.Context) int {
	count, err := csepq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (csepq *CarrierServiceEasyPostQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, csepq.ctx, ent.OpQueryExist)
	switch _, err := csepq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (csepq *CarrierServiceEasyPostQuery) ExistX(ctx context.Context) bool {
	exist, err := csepq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarrierServiceEasyPostQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (csepq *CarrierServiceEasyPostQuery) Clone() *CarrierServiceEasyPostQuery {
	if csepq == nil {
		return nil
	}
	return &CarrierServiceEasyPostQuery{
		config:                     csepq.config,
		ctx:                        csepq.ctx.Clone(),
		order:                      append([]carrierserviceeasypost.OrderOption{}, csepq.order...),
		inters:                     append([]Interceptor{}, csepq.inters...),
		predicates:                 append([]predicate.CarrierServiceEasyPost{}, csepq.predicates...),
		withCarrierService:         csepq.withCarrierService.Clone(),
		withCarrierAddServEasyPost: csepq.withCarrierAddServEasyPost.Clone(),
		// clone intermediate query.
		sql:  csepq.sql.Clone(),
		path: csepq.path,
	}
}

// WithCarrierService tells the query-builder to eager-load the nodes that are connected to
// the "carrier_service" edge. The optional arguments are used to configure the query builder of the edge.
func (csepq *CarrierServiceEasyPostQuery) WithCarrierService(opts ...func(*CarrierServiceQuery)) *CarrierServiceEasyPostQuery {
	query := (&CarrierServiceClient{config: csepq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csepq.withCarrierService = query
	return csepq
}

// WithCarrierAddServEasyPost tells the query-builder to eager-load the nodes that are connected to
// the "carrier_add_serv_easy_post" edge. The optional arguments are used to configure the query builder of the edge.
func (csepq *CarrierServiceEasyPostQuery) WithCarrierAddServEasyPost(opts ...func(*CarrierAdditionalServiceEasyPostQuery)) *CarrierServiceEasyPostQuery {
	query := (&CarrierAdditionalServiceEasyPostClient{config: csepq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csepq.withCarrierAddServEasyPost = query
	return csepq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		APIKey carrierserviceeasypost.APIKey `json:"api_key,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CarrierServiceEasyPost.Query().
//		GroupBy(carrierserviceeasypost.FieldAPIKey).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (csepq *CarrierServiceEasyPostQuery) GroupBy(field string, fields ...string) *CarrierServiceEasyPostGroupBy {
	csepq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarrierServiceEasyPostGroupBy{build: csepq}
	grbuild.flds = &csepq.ctx.Fields
	grbuild.label = carrierserviceeasypost.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		APIKey carrierserviceeasypost.APIKey `json:"api_key,omitempty"`
//	}
//
//	client.CarrierServiceEasyPost.Query().
//		Select(carrierserviceeasypost.FieldAPIKey).
//		Scan(ctx, &v)
func (csepq *CarrierServiceEasyPostQuery) Select(fields ...string) *CarrierServiceEasyPostSelect {
	csepq.ctx.Fields = append(csepq.ctx.Fields, fields...)
	sbuild := &CarrierServiceEasyPostSelect{CarrierServiceEasyPostQuery: csepq}
	sbuild.label = carrierserviceeasypost.Label
	sbuild.flds, sbuild.scan = &csepq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarrierServiceEasyPostSelect configured with the given aggregations.
func (csepq *CarrierServiceEasyPostQuery) Aggregate(fns ...AggregateFunc) *CarrierServiceEasyPostSelect {
	return csepq.Select().Aggregate(fns...)
}

func (csepq *CarrierServiceEasyPostQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range csepq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, csepq); err != nil {
				return err
			}
		}
	}
	for _, f := range csepq.ctx.Fields {
		if !carrierserviceeasypost.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if csepq.path != nil {
		prev, err := csepq.path(ctx)
		if err != nil {
			return err
		}
		csepq.sql = prev
	}
	if carrierserviceeasypost.Policy == nil {
		return errors.New("ent: uninitialized carrierserviceeasypost.Policy (forgotten import ent/runtime?)")
	}
	if err := carrierserviceeasypost.Policy.EvalQuery(ctx, csepq); err != nil {
		return err
	}
	return nil
}

func (csepq *CarrierServiceEasyPostQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarrierServiceEasyPost, error) {
	var (
		nodes       = []*CarrierServiceEasyPost{}
		withFKs     = csepq.withFKs
		_spec       = csepq.querySpec()
		loadedTypes = [2]bool{
			csepq.withCarrierService != nil,
			csepq.withCarrierAddServEasyPost != nil,
		}
	)
	if csepq.withCarrierService != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, carrierserviceeasypost.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarrierServiceEasyPost).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarrierServiceEasyPost{config: csepq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(csepq.modifiers) > 0 {
		_spec.Modifiers = csepq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, csepq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := csepq.withCarrierService; query != nil {
		if err := csepq.loadCarrierService(ctx, query, nodes, nil,
			func(n *CarrierServiceEasyPost, e *CarrierService) { n.Edges.CarrierService = e }); err != nil {
			return nil, err
		}
	}
	if query := csepq.withCarrierAddServEasyPost; query != nil {
		if err := csepq.loadCarrierAddServEasyPost(ctx, query, nodes,
			func(n *CarrierServiceEasyPost) {
				n.Edges.CarrierAddServEasyPost = []*CarrierAdditionalServiceEasyPost{}
			},
			func(n *CarrierServiceEasyPost, e *CarrierAdditionalServiceEasyPost) {
				n.Edges.CarrierAddServEasyPost = append(n.Edges.CarrierAddServEasyPost, e)
			}); err != nil {
			return nil, err
		}
	}
	for name, query := range csepq.withNamedCarrierAddServEasyPost {
		if err := csepq.loadCarrierAddServEasyPost(ctx, query, nodes,
			func(n *CarrierServiceEasyPost) { n.appendNamedCarrierAddServEasyPost(name) },
			func(n *CarrierServiceEasyPost, e *CarrierAdditionalServiceEasyPost) {
				n.appendNamedCarrierAddServEasyPost(name, e)
			}); err != nil {
			return nil, err
		}
	}
	for i := range csepq.loadTotal {
		if err := csepq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (csepq *CarrierServiceEasyPostQuery) loadCarrierService(ctx context.Context, query *CarrierServiceQuery, nodes []*CarrierServiceEasyPost, init func(*CarrierServiceEasyPost), assign func(*CarrierServiceEasyPost, *CarrierService)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*CarrierServiceEasyPost)
	for i := range nodes {
		if nodes[i].carrier_service_carrier_serv_easy_post == nil {
			continue
		}
		fk := *nodes[i].carrier_service_carrier_serv_easy_post
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(carrierservice.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "carrier_service_carrier_serv_easy_post" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (csepq *CarrierServiceEasyPostQuery) loadCarrierAddServEasyPost(ctx context.Context, query *CarrierAdditionalServiceEasyPostQuery, nodes []*CarrierServiceEasyPost, init func(*CarrierServiceEasyPost), assign func(*CarrierServiceEasyPost, *CarrierAdditionalServiceEasyPost)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[pulid.ID]*CarrierServiceEasyPost)
	nids := make(map[pulid.ID]map[*CarrierServiceEasyPost]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(carrierserviceeasypost.CarrierAddServEasyPostTable)
		s.Join(joinT).On(s.C(carrieradditionalserviceeasypost.FieldID), joinT.C(carrierserviceeasypost.CarrierAddServEasyPostPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(carrierserviceeasypost.CarrierAddServEasyPostPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(carrierserviceeasypost.CarrierAddServEasyPostPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(pulid.ID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*pulid.ID)
				inValue := *values[1].(*pulid.ID)
				if nids[inValue] == nil {
					nids[inValue] = map[*CarrierServiceEasyPost]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*CarrierAdditionalServiceEasyPost](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "carrier_add_serv_easy_post" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (csepq *CarrierServiceEasyPostQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := csepq.querySpec()
	if len(csepq.modifiers) > 0 {
		_spec.Modifiers = csepq.modifiers
	}
	_spec.Node.Columns = csepq.ctx.Fields
	if len(csepq.ctx.Fields) > 0 {
		_spec.Unique = csepq.ctx.Unique != nil && *csepq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, csepq.driver, _spec)
}

func (csepq *CarrierServiceEasyPostQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(carrierserviceeasypost.Table, carrierserviceeasypost.Columns, sqlgraph.NewFieldSpec(carrierserviceeasypost.FieldID, field.TypeString))
	_spec.From = csepq.sql
	if unique := csepq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if csepq.path != nil {
		_spec.Unique = true
	}
	if fields := csepq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, carrierserviceeasypost.FieldID)
		for i := range fields {
			if fields[i] != carrierserviceeasypost.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := csepq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := csepq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := csepq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := csepq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (csepq *CarrierServiceEasyPostQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(csepq.driver.Dialect())
	t1 := builder.Table(carrierserviceeasypost.Table)
	columns := csepq.ctx.Fields
	if len(columns) == 0 {
		columns = carrierserviceeasypost.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if csepq.sql != nil {
		selector = csepq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if csepq.ctx.Unique != nil && *csepq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range csepq.predicates {
		p(selector)
	}
	for _, p := range csepq.order {
		p(selector)
	}
	if offset := csepq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := csepq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedCarrierAddServEasyPost tells the query-builder to eager-load the nodes that are connected to the "carrier_add_serv_easy_post"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (csepq *CarrierServiceEasyPostQuery) WithNamedCarrierAddServEasyPost(name string, opts ...func(*CarrierAdditionalServiceEasyPostQuery)) *CarrierServiceEasyPostQuery {
	query := (&CarrierAdditionalServiceEasyPostClient{config: csepq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if csepq.withNamedCarrierAddServEasyPost == nil {
		csepq.withNamedCarrierAddServEasyPost = make(map[string]*CarrierAdditionalServiceEasyPostQuery)
	}
	csepq.withNamedCarrierAddServEasyPost[name] = query
	return csepq
}

// CarrierServiceEasyPostGroupBy is the group-by builder for CarrierServiceEasyPost entities.
type CarrierServiceEasyPostGroupBy struct {
	selector
	build *CarrierServiceEasyPostQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (csepgb *CarrierServiceEasyPostGroupBy) Aggregate(fns ...AggregateFunc) *CarrierServiceEasyPostGroupBy {
	csepgb.fns = append(csepgb.fns, fns...)
	return csepgb
}

// Scan applies the selector query and scans the result into the given value.
func (csepgb *CarrierServiceEasyPostGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csepgb.build.ctx, ent.OpQueryGroupBy)
	if err := csepgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarrierServiceEasyPostQuery, *CarrierServiceEasyPostGroupBy](ctx, csepgb.build, csepgb, csepgb.build.inters, v)
}

func (csepgb *CarrierServiceEasyPostGroupBy) sqlScan(ctx context.Context, root *CarrierServiceEasyPostQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(csepgb.fns))
	for _, fn := range csepgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*csepgb.flds)+len(csepgb.fns))
		for _, f := range *csepgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*csepgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csepgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarrierServiceEasyPostSelect is the builder for selecting fields of CarrierServiceEasyPost entities.
type CarrierServiceEasyPostSelect struct {
	*CarrierServiceEasyPostQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cseps *CarrierServiceEasyPostSelect) Aggregate(fns ...AggregateFunc) *CarrierServiceEasyPostSelect {
	cseps.fns = append(cseps.fns, fns...)
	return cseps
}

// Scan applies the selector query and scans the result into the given value.
func (cseps *CarrierServiceEasyPostSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cseps.ctx, ent.OpQuerySelect)
	if err := cseps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarrierServiceEasyPostQuery, *CarrierServiceEasyPostSelect](ctx, cseps.CarrierServiceEasyPostQuery, cseps, cseps.inters, v)
}

func (cseps *CarrierServiceEasyPostSelect) sqlScan(ctx context.Context, root *CarrierServiceEasyPostQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cseps.fns))
	for _, fn := range cseps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cseps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cseps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
