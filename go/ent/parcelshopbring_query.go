// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/addressglobal"
	"delivrio.io/go/ent/parcelshop"
	"delivrio.io/go/ent/parcelshopbring"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ParcelShopBringQuery is the builder for querying ParcelShopBring entities.
type ParcelShopBringQuery struct {
	config
	ctx                 *QueryContext
	order               []parcelshopbring.OrderOption
	inters              []Interceptor
	predicates          []predicate.ParcelShopBring
	withParcelShop      *ParcelShopQuery
	withAddressDelivery *AddressGlobalQuery
	withFKs             bool
	modifiers           []func(*sql.Selector)
	loadTotal           []func(context.Context, []*ParcelShopBring) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ParcelShopBringQuery builder.
func (psbq *ParcelShopBringQuery) Where(ps ...predicate.ParcelShopBring) *ParcelShopBringQuery {
	psbq.predicates = append(psbq.predicates, ps...)
	return psbq
}

// Limit the number of records to be returned by this query.
func (psbq *ParcelShopBringQuery) Limit(limit int) *ParcelShopBringQuery {
	psbq.ctx.Limit = &limit
	return psbq
}

// Offset to start from.
func (psbq *ParcelShopBringQuery) Offset(offset int) *ParcelShopBringQuery {
	psbq.ctx.Offset = &offset
	return psbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (psbq *ParcelShopBringQuery) Unique(unique bool) *ParcelShopBringQuery {
	psbq.ctx.Unique = &unique
	return psbq
}

// Order specifies how the records should be ordered.
func (psbq *ParcelShopBringQuery) Order(o ...parcelshopbring.OrderOption) *ParcelShopBringQuery {
	psbq.order = append(psbq.order, o...)
	return psbq
}

// QueryParcelShop chains the current query on the "parcel_shop" edge.
func (psbq *ParcelShopBringQuery) QueryParcelShop() *ParcelShopQuery {
	query := (&ParcelShopClient{config: psbq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := psbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := psbq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(parcelshopbring.Table, parcelshopbring.FieldID, selector),
			sqlgraph.To(parcelshop.Table, parcelshop.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, parcelshopbring.ParcelShopTable, parcelshopbring.ParcelShopColumn),
		)
		fromU = sqlgraph.SetNeighbors(psbq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAddressDelivery chains the current query on the "address_delivery" edge.
func (psbq *ParcelShopBringQuery) QueryAddressDelivery() *AddressGlobalQuery {
	query := (&AddressGlobalClient{config: psbq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := psbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := psbq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(parcelshopbring.Table, parcelshopbring.FieldID, selector),
			sqlgraph.To(addressglobal.Table, addressglobal.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, parcelshopbring.AddressDeliveryTable, parcelshopbring.AddressDeliveryColumn),
		)
		fromU = sqlgraph.SetNeighbors(psbq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ParcelShopBring entity from the query.
// Returns a *NotFoundError when no ParcelShopBring was found.
func (psbq *ParcelShopBringQuery) First(ctx context.Context) (*ParcelShopBring, error) {
	nodes, err := psbq.Limit(1).All(setContextOp(ctx, psbq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{parcelshopbring.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (psbq *ParcelShopBringQuery) FirstX(ctx context.Context) *ParcelShopBring {
	node, err := psbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ParcelShopBring ID from the query.
// Returns a *NotFoundError when no ParcelShopBring ID was found.
func (psbq *ParcelShopBringQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = psbq.Limit(1).IDs(setContextOp(ctx, psbq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{parcelshopbring.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (psbq *ParcelShopBringQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := psbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ParcelShopBring entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ParcelShopBring entity is found.
// Returns a *NotFoundError when no ParcelShopBring entities are found.
func (psbq *ParcelShopBringQuery) Only(ctx context.Context) (*ParcelShopBring, error) {
	nodes, err := psbq.Limit(2).All(setContextOp(ctx, psbq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{parcelshopbring.Label}
	default:
		return nil, &NotSingularError{parcelshopbring.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (psbq *ParcelShopBringQuery) OnlyX(ctx context.Context) *ParcelShopBring {
	node, err := psbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ParcelShopBring ID in the query.
// Returns a *NotSingularError when more than one ParcelShopBring ID is found.
// Returns a *NotFoundError when no entities are found.
func (psbq *ParcelShopBringQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = psbq.Limit(2).IDs(setContextOp(ctx, psbq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{parcelshopbring.Label}
	default:
		err = &NotSingularError{parcelshopbring.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (psbq *ParcelShopBringQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := psbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ParcelShopBrings.
func (psbq *ParcelShopBringQuery) All(ctx context.Context) ([]*ParcelShopBring, error) {
	ctx = setContextOp(ctx, psbq.ctx, ent.OpQueryAll)
	if err := psbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ParcelShopBring, *ParcelShopBringQuery]()
	return withInterceptors[[]*ParcelShopBring](ctx, psbq, qr, psbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (psbq *ParcelShopBringQuery) AllX(ctx context.Context) []*ParcelShopBring {
	nodes, err := psbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ParcelShopBring IDs.
func (psbq *ParcelShopBringQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if psbq.ctx.Unique == nil && psbq.path != nil {
		psbq.Unique(true)
	}
	ctx = setContextOp(ctx, psbq.ctx, ent.OpQueryIDs)
	if err = psbq.Select(parcelshopbring.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (psbq *ParcelShopBringQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := psbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (psbq *ParcelShopBringQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, psbq.ctx, ent.OpQueryCount)
	if err := psbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, psbq, querierCount[*ParcelShopBringQuery](), psbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (psbq *ParcelShopBringQuery) CountX(ctx context.Context) int {
	count, err := psbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (psbq *ParcelShopBringQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, psbq.ctx, ent.OpQueryExist)
	switch _, err := psbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (psbq *ParcelShopBringQuery) ExistX(ctx context.Context) bool {
	exist, err := psbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ParcelShopBringQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (psbq *ParcelShopBringQuery) Clone() *ParcelShopBringQuery {
	if psbq == nil {
		return nil
	}
	return &ParcelShopBringQuery{
		config:              psbq.config,
		ctx:                 psbq.ctx.Clone(),
		order:               append([]parcelshopbring.OrderOption{}, psbq.order...),
		inters:              append([]Interceptor{}, psbq.inters...),
		predicates:          append([]predicate.ParcelShopBring{}, psbq.predicates...),
		withParcelShop:      psbq.withParcelShop.Clone(),
		withAddressDelivery: psbq.withAddressDelivery.Clone(),
		// clone intermediate query.
		sql:  psbq.sql.Clone(),
		path: psbq.path,
	}
}

// WithParcelShop tells the query-builder to eager-load the nodes that are connected to
// the "parcel_shop" edge. The optional arguments are used to configure the query builder of the edge.
func (psbq *ParcelShopBringQuery) WithParcelShop(opts ...func(*ParcelShopQuery)) *ParcelShopBringQuery {
	query := (&ParcelShopClient{config: psbq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	psbq.withParcelShop = query
	return psbq
}

// WithAddressDelivery tells the query-builder to eager-load the nodes that are connected to
// the "address_delivery" edge. The optional arguments are used to configure the query builder of the edge.
func (psbq *ParcelShopBringQuery) WithAddressDelivery(opts ...func(*AddressGlobalQuery)) *ParcelShopBringQuery {
	query := (&AddressGlobalClient{config: psbq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	psbq.withAddressDelivery = query
	return psbq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PointType parcelshopbring.PointType `json:"point_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ParcelShopBring.Query().
//		GroupBy(parcelshopbring.FieldPointType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (psbq *ParcelShopBringQuery) GroupBy(field string, fields ...string) *ParcelShopBringGroupBy {
	psbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ParcelShopBringGroupBy{build: psbq}
	grbuild.flds = &psbq.ctx.Fields
	grbuild.label = parcelshopbring.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PointType parcelshopbring.PointType `json:"point_type,omitempty"`
//	}
//
//	client.ParcelShopBring.Query().
//		Select(parcelshopbring.FieldPointType).
//		Scan(ctx, &v)
func (psbq *ParcelShopBringQuery) Select(fields ...string) *ParcelShopBringSelect {
	psbq.ctx.Fields = append(psbq.ctx.Fields, fields...)
	sbuild := &ParcelShopBringSelect{ParcelShopBringQuery: psbq}
	sbuild.label = parcelshopbring.Label
	sbuild.flds, sbuild.scan = &psbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ParcelShopBringSelect configured with the given aggregations.
func (psbq *ParcelShopBringQuery) Aggregate(fns ...AggregateFunc) *ParcelShopBringSelect {
	return psbq.Select().Aggregate(fns...)
}

func (psbq *ParcelShopBringQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range psbq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, psbq); err != nil {
				return err
			}
		}
	}
	for _, f := range psbq.ctx.Fields {
		if !parcelshopbring.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if psbq.path != nil {
		prev, err := psbq.path(ctx)
		if err != nil {
			return err
		}
		psbq.sql = prev
	}
	if parcelshopbring.Policy == nil {
		return errors.New("ent: uninitialized parcelshopbring.Policy (forgotten import ent/runtime?)")
	}
	if err := parcelshopbring.Policy.EvalQuery(ctx, psbq); err != nil {
		return err
	}
	return nil
}

func (psbq *ParcelShopBringQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ParcelShopBring, error) {
	var (
		nodes       = []*ParcelShopBring{}
		withFKs     = psbq.withFKs
		_spec       = psbq.querySpec()
		loadedTypes = [2]bool{
			psbq.withParcelShop != nil,
			psbq.withAddressDelivery != nil,
		}
	)
	if psbq.withParcelShop != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, parcelshopbring.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ParcelShopBring).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ParcelShopBring{config: psbq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(psbq.modifiers) > 0 {
		_spec.Modifiers = psbq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, psbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := psbq.withParcelShop; query != nil {
		if err := psbq.loadParcelShop(ctx, query, nodes, nil,
			func(n *ParcelShopBring, e *ParcelShop) { n.Edges.ParcelShop = e }); err != nil {
			return nil, err
		}
	}
	if query := psbq.withAddressDelivery; query != nil {
		if err := psbq.loadAddressDelivery(ctx, query, nodes, nil,
			func(n *ParcelShopBring, e *AddressGlobal) { n.Edges.AddressDelivery = e }); err != nil {
			return nil, err
		}
	}
	for i := range psbq.loadTotal {
		if err := psbq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (psbq *ParcelShopBringQuery) loadParcelShop(ctx context.Context, query *ParcelShopQuery, nodes []*ParcelShopBring, init func(*ParcelShopBring), assign func(*ParcelShopBring, *ParcelShop)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ParcelShopBring)
	for i := range nodes {
		if nodes[i].parcel_shop_parcel_shop_bring == nil {
			continue
		}
		fk := *nodes[i].parcel_shop_parcel_shop_bring
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(parcelshop.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parcel_shop_parcel_shop_bring" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (psbq *ParcelShopBringQuery) loadAddressDelivery(ctx context.Context, query *AddressGlobalQuery, nodes []*ParcelShopBring, init func(*ParcelShopBring), assign func(*ParcelShopBring, *AddressGlobal)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[pulid.ID]*ParcelShopBring)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.AddressGlobal(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(parcelshopbring.AddressDeliveryColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.parcel_shop_bring_address_delivery
		if fk == nil {
			return fmt.Errorf(`foreign-key "parcel_shop_bring_address_delivery" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "parcel_shop_bring_address_delivery" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (psbq *ParcelShopBringQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := psbq.querySpec()
	if len(psbq.modifiers) > 0 {
		_spec.Modifiers = psbq.modifiers
	}
	_spec.Node.Columns = psbq.ctx.Fields
	if len(psbq.ctx.Fields) > 0 {
		_spec.Unique = psbq.ctx.Unique != nil && *psbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, psbq.driver, _spec)
}

func (psbq *ParcelShopBringQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(parcelshopbring.Table, parcelshopbring.Columns, sqlgraph.NewFieldSpec(parcelshopbring.FieldID, field.TypeString))
	_spec.From = psbq.sql
	if unique := psbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if psbq.path != nil {
		_spec.Unique = true
	}
	if fields := psbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, parcelshopbring.FieldID)
		for i := range fields {
			if fields[i] != parcelshopbring.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := psbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := psbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := psbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := psbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (psbq *ParcelShopBringQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(psbq.driver.Dialect())
	t1 := builder.Table(parcelshopbring.Table)
	columns := psbq.ctx.Fields
	if len(columns) == 0 {
		columns = parcelshopbring.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if psbq.sql != nil {
		selector = psbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if psbq.ctx.Unique != nil && *psbq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range psbq.predicates {
		p(selector)
	}
	for _, p := range psbq.order {
		p(selector)
	}
	if offset := psbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := psbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ParcelShopBringGroupBy is the group-by builder for ParcelShopBring entities.
type ParcelShopBringGroupBy struct {
	selector
	build *ParcelShopBringQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (psbgb *ParcelShopBringGroupBy) Aggregate(fns ...AggregateFunc) *ParcelShopBringGroupBy {
	psbgb.fns = append(psbgb.fns, fns...)
	return psbgb
}

// Scan applies the selector query and scans the result into the given value.
func (psbgb *ParcelShopBringGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, psbgb.build.ctx, ent.OpQueryGroupBy)
	if err := psbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ParcelShopBringQuery, *ParcelShopBringGroupBy](ctx, psbgb.build, psbgb, psbgb.build.inters, v)
}

func (psbgb *ParcelShopBringGroupBy) sqlScan(ctx context.Context, root *ParcelShopBringQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(psbgb.fns))
	for _, fn := range psbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*psbgb.flds)+len(psbgb.fns))
		for _, f := range *psbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*psbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := psbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ParcelShopBringSelect is the builder for selecting fields of ParcelShopBring entities.
type ParcelShopBringSelect struct {
	*ParcelShopBringQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (psbs *ParcelShopBringSelect) Aggregate(fns ...AggregateFunc) *ParcelShopBringSelect {
	psbs.fns = append(psbs.fns, fns...)
	return psbs
}

// Scan applies the selector query and scans the result into the given value.
func (psbs *ParcelShopBringSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, psbs.ctx, ent.OpQuerySelect)
	if err := psbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ParcelShopBringQuery, *ParcelShopBringSelect](ctx, psbs.ParcelShopBringQuery, psbs, psbs.inters, v)
}

func (psbs *ParcelShopBringSelect) sqlScan(ctx context.Context, root *ParcelShopBringQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(psbs.fns))
	for _, fn := range psbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*psbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := psbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
