// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/carrier"
	"delivrio.io/go/ent/carrierpostnord"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CarrierPostNordQuery is the builder for querying CarrierPostNord entities.
type CarrierPostNordQuery struct {
	config
	ctx         *QueryContext
	order       []carrierpostnord.OrderOption
	inters      []Interceptor
	predicates  []predicate.CarrierPostNord
	withTenant  *TenantQuery
	withCarrier *CarrierQuery
	withFKs     bool
	modifiers   []func(*sql.Selector)
	loadTotal   []func(context.Context, []*CarrierPostNord) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarrierPostNordQuery builder.
func (cpnq *CarrierPostNordQuery) Where(ps ...predicate.CarrierPostNord) *CarrierPostNordQuery {
	cpnq.predicates = append(cpnq.predicates, ps...)
	return cpnq
}

// Limit the number of records to be returned by this query.
func (cpnq *CarrierPostNordQuery) Limit(limit int) *CarrierPostNordQuery {
	cpnq.ctx.Limit = &limit
	return cpnq
}

// Offset to start from.
func (cpnq *CarrierPostNordQuery) Offset(offset int) *CarrierPostNordQuery {
	cpnq.ctx.Offset = &offset
	return cpnq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cpnq *CarrierPostNordQuery) Unique(unique bool) *CarrierPostNordQuery {
	cpnq.ctx.Unique = &unique
	return cpnq
}

// Order specifies how the records should be ordered.
func (cpnq *CarrierPostNordQuery) Order(o ...carrierpostnord.OrderOption) *CarrierPostNordQuery {
	cpnq.order = append(cpnq.order, o...)
	return cpnq
}

// QueryTenant chains the current query on the "tenant" edge.
func (cpnq *CarrierPostNordQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: cpnq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cpnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cpnq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carrierpostnord.Table, carrierpostnord.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, carrierpostnord.TenantTable, carrierpostnord.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(cpnq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCarrier chains the current query on the "carrier" edge.
func (cpnq *CarrierPostNordQuery) QueryCarrier() *CarrierQuery {
	query := (&CarrierClient{config: cpnq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cpnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cpnq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carrierpostnord.Table, carrierpostnord.FieldID, selector),
			sqlgraph.To(carrier.Table, carrier.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, carrierpostnord.CarrierTable, carrierpostnord.CarrierColumn),
		)
		fromU = sqlgraph.SetNeighbors(cpnq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CarrierPostNord entity from the query.
// Returns a *NotFoundError when no CarrierPostNord was found.
func (cpnq *CarrierPostNordQuery) First(ctx context.Context) (*CarrierPostNord, error) {
	nodes, err := cpnq.Limit(1).All(setContextOp(ctx, cpnq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{carrierpostnord.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cpnq *CarrierPostNordQuery) FirstX(ctx context.Context) *CarrierPostNord {
	node, err := cpnq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarrierPostNord ID from the query.
// Returns a *NotFoundError when no CarrierPostNord ID was found.
func (cpnq *CarrierPostNordQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = cpnq.Limit(1).IDs(setContextOp(ctx, cpnq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{carrierpostnord.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cpnq *CarrierPostNordQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := cpnq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarrierPostNord entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarrierPostNord entity is found.
// Returns a *NotFoundError when no CarrierPostNord entities are found.
func (cpnq *CarrierPostNordQuery) Only(ctx context.Context) (*CarrierPostNord, error) {
	nodes, err := cpnq.Limit(2).All(setContextOp(ctx, cpnq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{carrierpostnord.Label}
	default:
		return nil, &NotSingularError{carrierpostnord.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cpnq *CarrierPostNordQuery) OnlyX(ctx context.Context) *CarrierPostNord {
	node, err := cpnq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarrierPostNord ID in the query.
// Returns a *NotSingularError when more than one CarrierPostNord ID is found.
// Returns a *NotFoundError when no entities are found.
func (cpnq *CarrierPostNordQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = cpnq.Limit(2).IDs(setContextOp(ctx, cpnq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{carrierpostnord.Label}
	default:
		err = &NotSingularError{carrierpostnord.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cpnq *CarrierPostNordQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := cpnq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarrierPostNords.
func (cpnq *CarrierPostNordQuery) All(ctx context.Context) ([]*CarrierPostNord, error) {
	ctx = setContextOp(ctx, cpnq.ctx, ent.OpQueryAll)
	if err := cpnq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarrierPostNord, *CarrierPostNordQuery]()
	return withInterceptors[[]*CarrierPostNord](ctx, cpnq, qr, cpnq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cpnq *CarrierPostNordQuery) AllX(ctx context.Context) []*CarrierPostNord {
	nodes, err := cpnq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarrierPostNord IDs.
func (cpnq *CarrierPostNordQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if cpnq.ctx.Unique == nil && cpnq.path != nil {
		cpnq.Unique(true)
	}
	ctx = setContextOp(ctx, cpnq.ctx, ent.OpQueryIDs)
	if err = cpnq.Select(carrierpostnord.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cpnq *CarrierPostNordQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := cpnq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cpnq *CarrierPostNordQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cpnq.ctx, ent.OpQueryCount)
	if err := cpnq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cpnq, querierCount[*CarrierPostNordQuery](), cpnq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cpnq *CarrierPostNordQuery) CountX(ctx context.Context) int {
	count, err := cpnq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cpnq *CarrierPostNordQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cpnq.ctx, ent.OpQueryExist)
	switch _, err := cpnq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cpnq *CarrierPostNordQuery) ExistX(ctx context.Context) bool {
	exist, err := cpnq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarrierPostNordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cpnq *CarrierPostNordQuery) Clone() *CarrierPostNordQuery {
	if cpnq == nil {
		return nil
	}
	return &CarrierPostNordQuery{
		config:      cpnq.config,
		ctx:         cpnq.ctx.Clone(),
		order:       append([]carrierpostnord.OrderOption{}, cpnq.order...),
		inters:      append([]Interceptor{}, cpnq.inters...),
		predicates:  append([]predicate.CarrierPostNord{}, cpnq.predicates...),
		withTenant:  cpnq.withTenant.Clone(),
		withCarrier: cpnq.withCarrier.Clone(),
		// clone intermediate query.
		sql:  cpnq.sql.Clone(),
		path: cpnq.path,
	}
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (cpnq *CarrierPostNordQuery) WithTenant(opts ...func(*TenantQuery)) *CarrierPostNordQuery {
	query := (&TenantClient{config: cpnq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cpnq.withTenant = query
	return cpnq
}

// WithCarrier tells the query-builder to eager-load the nodes that are connected to
// the "carrier" edge. The optional arguments are used to configure the query builder of the edge.
func (cpnq *CarrierPostNordQuery) WithCarrier(opts ...func(*CarrierQuery)) *CarrierPostNordQuery {
	query := (&CarrierClient{config: cpnq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cpnq.withCarrier = query
	return cpnq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CarrierPostNord.Query().
//		GroupBy(carrierpostnord.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cpnq *CarrierPostNordQuery) GroupBy(field string, fields ...string) *CarrierPostNordGroupBy {
	cpnq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarrierPostNordGroupBy{build: cpnq}
	grbuild.flds = &cpnq.ctx.Fields
	grbuild.label = carrierpostnord.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//	}
//
//	client.CarrierPostNord.Query().
//		Select(carrierpostnord.FieldTenantID).
//		Scan(ctx, &v)
func (cpnq *CarrierPostNordQuery) Select(fields ...string) *CarrierPostNordSelect {
	cpnq.ctx.Fields = append(cpnq.ctx.Fields, fields...)
	sbuild := &CarrierPostNordSelect{CarrierPostNordQuery: cpnq}
	sbuild.label = carrierpostnord.Label
	sbuild.flds, sbuild.scan = &cpnq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarrierPostNordSelect configured with the given aggregations.
func (cpnq *CarrierPostNordQuery) Aggregate(fns ...AggregateFunc) *CarrierPostNordSelect {
	return cpnq.Select().Aggregate(fns...)
}

func (cpnq *CarrierPostNordQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cpnq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cpnq); err != nil {
				return err
			}
		}
	}
	for _, f := range cpnq.ctx.Fields {
		if !carrierpostnord.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cpnq.path != nil {
		prev, err := cpnq.path(ctx)
		if err != nil {
			return err
		}
		cpnq.sql = prev
	}
	if carrierpostnord.Policy == nil {
		return errors.New("ent: uninitialized carrierpostnord.Policy (forgotten import ent/runtime?)")
	}
	if err := carrierpostnord.Policy.EvalQuery(ctx, cpnq); err != nil {
		return err
	}
	return nil
}

func (cpnq *CarrierPostNordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarrierPostNord, error) {
	var (
		nodes       = []*CarrierPostNord{}
		withFKs     = cpnq.withFKs
		_spec       = cpnq.querySpec()
		loadedTypes = [2]bool{
			cpnq.withTenant != nil,
			cpnq.withCarrier != nil,
		}
	)
	if cpnq.withCarrier != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, carrierpostnord.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarrierPostNord).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarrierPostNord{config: cpnq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(cpnq.modifiers) > 0 {
		_spec.Modifiers = cpnq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cpnq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cpnq.withTenant; query != nil {
		if err := cpnq.loadTenant(ctx, query, nodes, nil,
			func(n *CarrierPostNord, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	if query := cpnq.withCarrier; query != nil {
		if err := cpnq.loadCarrier(ctx, query, nodes, nil,
			func(n *CarrierPostNord, e *Carrier) { n.Edges.Carrier = e }); err != nil {
			return nil, err
		}
	}
	for i := range cpnq.loadTotal {
		if err := cpnq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cpnq *CarrierPostNordQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*CarrierPostNord, init func(*CarrierPostNord), assign func(*CarrierPostNord, *Tenant)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*CarrierPostNord)
	for i := range nodes {
		fk := nodes[i].TenantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (cpnq *CarrierPostNordQuery) loadCarrier(ctx context.Context, query *CarrierQuery, nodes []*CarrierPostNord, init func(*CarrierPostNord), assign func(*CarrierPostNord, *Carrier)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*CarrierPostNord)
	for i := range nodes {
		if nodes[i].carrier_carrier_post_nord == nil {
			continue
		}
		fk := *nodes[i].carrier_carrier_post_nord
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(carrier.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "carrier_carrier_post_nord" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (cpnq *CarrierPostNordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cpnq.querySpec()
	if len(cpnq.modifiers) > 0 {
		_spec.Modifiers = cpnq.modifiers
	}
	_spec.Node.Columns = cpnq.ctx.Fields
	if len(cpnq.ctx.Fields) > 0 {
		_spec.Unique = cpnq.ctx.Unique != nil && *cpnq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cpnq.driver, _spec)
}

func (cpnq *CarrierPostNordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(carrierpostnord.Table, carrierpostnord.Columns, sqlgraph.NewFieldSpec(carrierpostnord.FieldID, field.TypeString))
	_spec.From = cpnq.sql
	if unique := cpnq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cpnq.path != nil {
		_spec.Unique = true
	}
	if fields := cpnq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, carrierpostnord.FieldID)
		for i := range fields {
			if fields[i] != carrierpostnord.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if cpnq.withTenant != nil {
			_spec.Node.AddColumnOnce(carrierpostnord.FieldTenantID)
		}
	}
	if ps := cpnq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cpnq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cpnq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cpnq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cpnq *CarrierPostNordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cpnq.driver.Dialect())
	t1 := builder.Table(carrierpostnord.Table)
	columns := cpnq.ctx.Fields
	if len(columns) == 0 {
		columns = carrierpostnord.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cpnq.sql != nil {
		selector = cpnq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cpnq.ctx.Unique != nil && *cpnq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cpnq.predicates {
		p(selector)
	}
	for _, p := range cpnq.order {
		p(selector)
	}
	if offset := cpnq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cpnq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CarrierPostNordGroupBy is the group-by builder for CarrierPostNord entities.
type CarrierPostNordGroupBy struct {
	selector
	build *CarrierPostNordQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cpngb *CarrierPostNordGroupBy) Aggregate(fns ...AggregateFunc) *CarrierPostNordGroupBy {
	cpngb.fns = append(cpngb.fns, fns...)
	return cpngb
}

// Scan applies the selector query and scans the result into the given value.
func (cpngb *CarrierPostNordGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cpngb.build.ctx, ent.OpQueryGroupBy)
	if err := cpngb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarrierPostNordQuery, *CarrierPostNordGroupBy](ctx, cpngb.build, cpngb, cpngb.build.inters, v)
}

func (cpngb *CarrierPostNordGroupBy) sqlScan(ctx context.Context, root *CarrierPostNordQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cpngb.fns))
	for _, fn := range cpngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cpngb.flds)+len(cpngb.fns))
		for _, f := range *cpngb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cpngb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cpngb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarrierPostNordSelect is the builder for selecting fields of CarrierPostNord entities.
type CarrierPostNordSelect struct {
	*CarrierPostNordQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cpns *CarrierPostNordSelect) Aggregate(fns ...AggregateFunc) *CarrierPostNordSelect {
	cpns.fns = append(cpns.fns, fns...)
	return cpns
}

// Scan applies the selector query and scans the result into the given value.
func (cpns *CarrierPostNordSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cpns.ctx, ent.OpQuerySelect)
	if err := cpns.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarrierPostNordQuery, *CarrierPostNordSelect](ctx, cpns.CarrierPostNordQuery, cpns, cpns.inters, v)
}

func (cpns *CarrierPostNordSelect) sqlScan(ctx context.Context, root *CarrierPostNordQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cpns.fns))
	for _, fn := range cpns.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cpns.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cpns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
