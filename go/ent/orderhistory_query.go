// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/changehistory"
	"delivrio.io/go/ent/order"
	"delivrio.io/go/ent/orderhistory"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// OrderHistoryQuery is the builder for querying OrderHistory entities.
type OrderHistoryQuery struct {
	config
	ctx               *QueryContext
	order             []orderhistory.OrderOption
	inters            []Interceptor
	predicates        []predicate.OrderHistory
	withChangeHistory *ChangeHistoryQuery
	withTenant        *TenantQuery
	withOrder         *OrderQuery
	withFKs           bool
	modifiers         []func(*sql.Selector)
	loadTotal         []func(context.Context, []*OrderHistory) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OrderHistoryQuery builder.
func (ohq *OrderHistoryQuery) Where(ps ...predicate.OrderHistory) *OrderHistoryQuery {
	ohq.predicates = append(ohq.predicates, ps...)
	return ohq
}

// Limit the number of records to be returned by this query.
func (ohq *OrderHistoryQuery) Limit(limit int) *OrderHistoryQuery {
	ohq.ctx.Limit = &limit
	return ohq
}

// Offset to start from.
func (ohq *OrderHistoryQuery) Offset(offset int) *OrderHistoryQuery {
	ohq.ctx.Offset = &offset
	return ohq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ohq *OrderHistoryQuery) Unique(unique bool) *OrderHistoryQuery {
	ohq.ctx.Unique = &unique
	return ohq
}

// Order specifies how the records should be ordered.
func (ohq *OrderHistoryQuery) Order(o ...orderhistory.OrderOption) *OrderHistoryQuery {
	ohq.order = append(ohq.order, o...)
	return ohq
}

// QueryChangeHistory chains the current query on the "change_history" edge.
func (ohq *OrderHistoryQuery) QueryChangeHistory() *ChangeHistoryQuery {
	query := (&ChangeHistoryClient{config: ohq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ohq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ohq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(orderhistory.Table, orderhistory.FieldID, selector),
			sqlgraph.To(changehistory.Table, changehistory.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, orderhistory.ChangeHistoryTable, orderhistory.ChangeHistoryColumn),
		)
		fromU = sqlgraph.SetNeighbors(ohq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTenant chains the current query on the "tenant" edge.
func (ohq *OrderHistoryQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: ohq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ohq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ohq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(orderhistory.Table, orderhistory.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, orderhistory.TenantTable, orderhistory.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(ohq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrder chains the current query on the "order" edge.
func (ohq *OrderHistoryQuery) QueryOrder() *OrderQuery {
	query := (&OrderClient{config: ohq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ohq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ohq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(orderhistory.Table, orderhistory.FieldID, selector),
			sqlgraph.To(order.Table, order.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, orderhistory.OrderTable, orderhistory.OrderColumn),
		)
		fromU = sqlgraph.SetNeighbors(ohq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OrderHistory entity from the query.
// Returns a *NotFoundError when no OrderHistory was found.
func (ohq *OrderHistoryQuery) First(ctx context.Context) (*OrderHistory, error) {
	nodes, err := ohq.Limit(1).All(setContextOp(ctx, ohq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{orderhistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ohq *OrderHistoryQuery) FirstX(ctx context.Context) *OrderHistory {
	node, err := ohq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OrderHistory ID from the query.
// Returns a *NotFoundError when no OrderHistory ID was found.
func (ohq *OrderHistoryQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = ohq.Limit(1).IDs(setContextOp(ctx, ohq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{orderhistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ohq *OrderHistoryQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := ohq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OrderHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OrderHistory entity is found.
// Returns a *NotFoundError when no OrderHistory entities are found.
func (ohq *OrderHistoryQuery) Only(ctx context.Context) (*OrderHistory, error) {
	nodes, err := ohq.Limit(2).All(setContextOp(ctx, ohq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{orderhistory.Label}
	default:
		return nil, &NotSingularError{orderhistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ohq *OrderHistoryQuery) OnlyX(ctx context.Context) *OrderHistory {
	node, err := ohq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OrderHistory ID in the query.
// Returns a *NotSingularError when more than one OrderHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (ohq *OrderHistoryQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = ohq.Limit(2).IDs(setContextOp(ctx, ohq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{orderhistory.Label}
	default:
		err = &NotSingularError{orderhistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ohq *OrderHistoryQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := ohq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OrderHistories.
func (ohq *OrderHistoryQuery) All(ctx context.Context) ([]*OrderHistory, error) {
	ctx = setContextOp(ctx, ohq.ctx, ent.OpQueryAll)
	if err := ohq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OrderHistory, *OrderHistoryQuery]()
	return withInterceptors[[]*OrderHistory](ctx, ohq, qr, ohq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ohq *OrderHistoryQuery) AllX(ctx context.Context) []*OrderHistory {
	nodes, err := ohq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OrderHistory IDs.
func (ohq *OrderHistoryQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if ohq.ctx.Unique == nil && ohq.path != nil {
		ohq.Unique(true)
	}
	ctx = setContextOp(ctx, ohq.ctx, ent.OpQueryIDs)
	if err = ohq.Select(orderhistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ohq *OrderHistoryQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := ohq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ohq *OrderHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ohq.ctx, ent.OpQueryCount)
	if err := ohq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ohq, querierCount[*OrderHistoryQuery](), ohq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ohq *OrderHistoryQuery) CountX(ctx context.Context) int {
	count, err := ohq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ohq *OrderHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ohq.ctx, ent.OpQueryExist)
	switch _, err := ohq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ohq *OrderHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := ohq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OrderHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ohq *OrderHistoryQuery) Clone() *OrderHistoryQuery {
	if ohq == nil {
		return nil
	}
	return &OrderHistoryQuery{
		config:            ohq.config,
		ctx:               ohq.ctx.Clone(),
		order:             append([]orderhistory.OrderOption{}, ohq.order...),
		inters:            append([]Interceptor{}, ohq.inters...),
		predicates:        append([]predicate.OrderHistory{}, ohq.predicates...),
		withChangeHistory: ohq.withChangeHistory.Clone(),
		withTenant:        ohq.withTenant.Clone(),
		withOrder:         ohq.withOrder.Clone(),
		// clone intermediate query.
		sql:  ohq.sql.Clone(),
		path: ohq.path,
	}
}

// WithChangeHistory tells the query-builder to eager-load the nodes that are connected to
// the "change_history" edge. The optional arguments are used to configure the query builder of the edge.
func (ohq *OrderHistoryQuery) WithChangeHistory(opts ...func(*ChangeHistoryQuery)) *OrderHistoryQuery {
	query := (&ChangeHistoryClient{config: ohq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ohq.withChangeHistory = query
	return ohq
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (ohq *OrderHistoryQuery) WithTenant(opts ...func(*TenantQuery)) *OrderHistoryQuery {
	query := (&TenantClient{config: ohq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ohq.withTenant = query
	return ohq
}

// WithOrder tells the query-builder to eager-load the nodes that are connected to
// the "order" edge. The optional arguments are used to configure the query builder of the edge.
func (ohq *OrderHistoryQuery) WithOrder(opts ...func(*OrderQuery)) *OrderHistoryQuery {
	query := (&OrderClient{config: ohq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ohq.withOrder = query
	return ohq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OrderHistory.Query().
//		GroupBy(orderhistory.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ohq *OrderHistoryQuery) GroupBy(field string, fields ...string) *OrderHistoryGroupBy {
	ohq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OrderHistoryGroupBy{build: ohq}
	grbuild.flds = &ohq.ctx.Fields
	grbuild.label = orderhistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//	}
//
//	client.OrderHistory.Query().
//		Select(orderhistory.FieldTenantID).
//		Scan(ctx, &v)
func (ohq *OrderHistoryQuery) Select(fields ...string) *OrderHistorySelect {
	ohq.ctx.Fields = append(ohq.ctx.Fields, fields...)
	sbuild := &OrderHistorySelect{OrderHistoryQuery: ohq}
	sbuild.label = orderhistory.Label
	sbuild.flds, sbuild.scan = &ohq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OrderHistorySelect configured with the given aggregations.
func (ohq *OrderHistoryQuery) Aggregate(fns ...AggregateFunc) *OrderHistorySelect {
	return ohq.Select().Aggregate(fns...)
}

func (ohq *OrderHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ohq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ohq); err != nil {
				return err
			}
		}
	}
	for _, f := range ohq.ctx.Fields {
		if !orderhistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ohq.path != nil {
		prev, err := ohq.path(ctx)
		if err != nil {
			return err
		}
		ohq.sql = prev
	}
	if orderhistory.Policy == nil {
		return errors.New("ent: uninitialized orderhistory.Policy (forgotten import ent/runtime?)")
	}
	if err := orderhistory.Policy.EvalQuery(ctx, ohq); err != nil {
		return err
	}
	return nil
}

func (ohq *OrderHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OrderHistory, error) {
	var (
		nodes       = []*OrderHistory{}
		withFKs     = ohq.withFKs
		_spec       = ohq.querySpec()
		loadedTypes = [3]bool{
			ohq.withChangeHistory != nil,
			ohq.withTenant != nil,
			ohq.withOrder != nil,
		}
	)
	if ohq.withChangeHistory != nil || ohq.withOrder != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, orderhistory.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OrderHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OrderHistory{config: ohq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ohq.modifiers) > 0 {
		_spec.Modifiers = ohq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ohq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ohq.withChangeHistory; query != nil {
		if err := ohq.loadChangeHistory(ctx, query, nodes, nil,
			func(n *OrderHistory, e *ChangeHistory) { n.Edges.ChangeHistory = e }); err != nil {
			return nil, err
		}
	}
	if query := ohq.withTenant; query != nil {
		if err := ohq.loadTenant(ctx, query, nodes, nil,
			func(n *OrderHistory, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	if query := ohq.withOrder; query != nil {
		if err := ohq.loadOrder(ctx, query, nodes, nil,
			func(n *OrderHistory, e *Order) { n.Edges.Order = e }); err != nil {
			return nil, err
		}
	}
	for i := range ohq.loadTotal {
		if err := ohq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ohq *OrderHistoryQuery) loadChangeHistory(ctx context.Context, query *ChangeHistoryQuery, nodes []*OrderHistory, init func(*OrderHistory), assign func(*OrderHistory, *ChangeHistory)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*OrderHistory)
	for i := range nodes {
		if nodes[i].change_history_order_history == nil {
			continue
		}
		fk := *nodes[i].change_history_order_history
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(changehistory.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "change_history_order_history" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ohq *OrderHistoryQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*OrderHistory, init func(*OrderHistory), assign func(*OrderHistory, *Tenant)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*OrderHistory)
	for i := range nodes {
		fk := nodes[i].TenantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ohq *OrderHistoryQuery) loadOrder(ctx context.Context, query *OrderQuery, nodes []*OrderHistory, init func(*OrderHistory), assign func(*OrderHistory, *Order)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*OrderHistory)
	for i := range nodes {
		if nodes[i].order_order_history == nil {
			continue
		}
		fk := *nodes[i].order_order_history
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(order.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "order_order_history" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ohq *OrderHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ohq.querySpec()
	if len(ohq.modifiers) > 0 {
		_spec.Modifiers = ohq.modifiers
	}
	_spec.Node.Columns = ohq.ctx.Fields
	if len(ohq.ctx.Fields) > 0 {
		_spec.Unique = ohq.ctx.Unique != nil && *ohq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ohq.driver, _spec)
}

func (ohq *OrderHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(orderhistory.Table, orderhistory.Columns, sqlgraph.NewFieldSpec(orderhistory.FieldID, field.TypeString))
	_spec.From = ohq.sql
	if unique := ohq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ohq.path != nil {
		_spec.Unique = true
	}
	if fields := ohq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, orderhistory.FieldID)
		for i := range fields {
			if fields[i] != orderhistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ohq.withTenant != nil {
			_spec.Node.AddColumnOnce(orderhistory.FieldTenantID)
		}
	}
	if ps := ohq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ohq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ohq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ohq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ohq *OrderHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ohq.driver.Dialect())
	t1 := builder.Table(orderhistory.Table)
	columns := ohq.ctx.Fields
	if len(columns) == 0 {
		columns = orderhistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ohq.sql != nil {
		selector = ohq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ohq.ctx.Unique != nil && *ohq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ohq.predicates {
		p(selector)
	}
	for _, p := range ohq.order {
		p(selector)
	}
	if offset := ohq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ohq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OrderHistoryGroupBy is the group-by builder for OrderHistory entities.
type OrderHistoryGroupBy struct {
	selector
	build *OrderHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ohgb *OrderHistoryGroupBy) Aggregate(fns ...AggregateFunc) *OrderHistoryGroupBy {
	ohgb.fns = append(ohgb.fns, fns...)
	return ohgb
}

// Scan applies the selector query and scans the result into the given value.
func (ohgb *OrderHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ohgb.build.ctx, ent.OpQueryGroupBy)
	if err := ohgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderHistoryQuery, *OrderHistoryGroupBy](ctx, ohgb.build, ohgb, ohgb.build.inters, v)
}

func (ohgb *OrderHistoryGroupBy) sqlScan(ctx context.Context, root *OrderHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ohgb.fns))
	for _, fn := range ohgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ohgb.flds)+len(ohgb.fns))
		for _, f := range *ohgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ohgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ohgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OrderHistorySelect is the builder for selecting fields of OrderHistory entities.
type OrderHistorySelect struct {
	*OrderHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ohs *OrderHistorySelect) Aggregate(fns ...AggregateFunc) *OrderHistorySelect {
	ohs.fns = append(ohs.fns, fns...)
	return ohs
}

// Scan applies the selector query and scans the result into the given value.
func (ohs *OrderHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ohs.ctx, ent.OpQuerySelect)
	if err := ohs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderHistoryQuery, *OrderHistorySelect](ctx, ohs.OrderHistoryQuery, ohs, ohs.inters, v)
}

func (ohs *OrderHistorySelect) sqlScan(ctx context.Context, root *OrderHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ohs.fns))
	for _, fn := range ohs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ohs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ohs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
