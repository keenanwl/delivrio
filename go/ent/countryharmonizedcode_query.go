// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/country"
	"delivrio.io/go/ent/countryharmonizedcode"
	"delivrio.io/go/ent/inventoryitem"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CountryHarmonizedCodeQuery is the builder for querying CountryHarmonizedCode entities.
type CountryHarmonizedCodeQuery struct {
	config
	ctx               *QueryContext
	order             []countryharmonizedcode.OrderOption
	inters            []Interceptor
	predicates        []predicate.CountryHarmonizedCode
	withTenant        *TenantQuery
	withInventoryItem *InventoryItemQuery
	withCountry       *CountryQuery
	withFKs           bool
	modifiers         []func(*sql.Selector)
	loadTotal         []func(context.Context, []*CountryHarmonizedCode) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CountryHarmonizedCodeQuery builder.
func (chcq *CountryHarmonizedCodeQuery) Where(ps ...predicate.CountryHarmonizedCode) *CountryHarmonizedCodeQuery {
	chcq.predicates = append(chcq.predicates, ps...)
	return chcq
}

// Limit the number of records to be returned by this query.
func (chcq *CountryHarmonizedCodeQuery) Limit(limit int) *CountryHarmonizedCodeQuery {
	chcq.ctx.Limit = &limit
	return chcq
}

// Offset to start from.
func (chcq *CountryHarmonizedCodeQuery) Offset(offset int) *CountryHarmonizedCodeQuery {
	chcq.ctx.Offset = &offset
	return chcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (chcq *CountryHarmonizedCodeQuery) Unique(unique bool) *CountryHarmonizedCodeQuery {
	chcq.ctx.Unique = &unique
	return chcq
}

// Order specifies how the records should be ordered.
func (chcq *CountryHarmonizedCodeQuery) Order(o ...countryharmonizedcode.OrderOption) *CountryHarmonizedCodeQuery {
	chcq.order = append(chcq.order, o...)
	return chcq
}

// QueryTenant chains the current query on the "tenant" edge.
func (chcq *CountryHarmonizedCodeQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: chcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := chcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := chcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(countryharmonizedcode.Table, countryharmonizedcode.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, countryharmonizedcode.TenantTable, countryharmonizedcode.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(chcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryInventoryItem chains the current query on the "inventory_item" edge.
func (chcq *CountryHarmonizedCodeQuery) QueryInventoryItem() *InventoryItemQuery {
	query := (&InventoryItemClient{config: chcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := chcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := chcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(countryharmonizedcode.Table, countryharmonizedcode.FieldID, selector),
			sqlgraph.To(inventoryitem.Table, inventoryitem.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, countryharmonizedcode.InventoryItemTable, countryharmonizedcode.InventoryItemColumn),
		)
		fromU = sqlgraph.SetNeighbors(chcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCountry chains the current query on the "country" edge.
func (chcq *CountryHarmonizedCodeQuery) QueryCountry() *CountryQuery {
	query := (&CountryClient{config: chcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := chcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := chcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(countryharmonizedcode.Table, countryharmonizedcode.FieldID, selector),
			sqlgraph.To(country.Table, country.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, countryharmonizedcode.CountryTable, countryharmonizedcode.CountryColumn),
		)
		fromU = sqlgraph.SetNeighbors(chcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CountryHarmonizedCode entity from the query.
// Returns a *NotFoundError when no CountryHarmonizedCode was found.
func (chcq *CountryHarmonizedCodeQuery) First(ctx context.Context) (*CountryHarmonizedCode, error) {
	nodes, err := chcq.Limit(1).All(setContextOp(ctx, chcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{countryharmonizedcode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (chcq *CountryHarmonizedCodeQuery) FirstX(ctx context.Context) *CountryHarmonizedCode {
	node, err := chcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CountryHarmonizedCode ID from the query.
// Returns a *NotFoundError when no CountryHarmonizedCode ID was found.
func (chcq *CountryHarmonizedCodeQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = chcq.Limit(1).IDs(setContextOp(ctx, chcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{countryharmonizedcode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (chcq *CountryHarmonizedCodeQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := chcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CountryHarmonizedCode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CountryHarmonizedCode entity is found.
// Returns a *NotFoundError when no CountryHarmonizedCode entities are found.
func (chcq *CountryHarmonizedCodeQuery) Only(ctx context.Context) (*CountryHarmonizedCode, error) {
	nodes, err := chcq.Limit(2).All(setContextOp(ctx, chcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{countryharmonizedcode.Label}
	default:
		return nil, &NotSingularError{countryharmonizedcode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (chcq *CountryHarmonizedCodeQuery) OnlyX(ctx context.Context) *CountryHarmonizedCode {
	node, err := chcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CountryHarmonizedCode ID in the query.
// Returns a *NotSingularError when more than one CountryHarmonizedCode ID is found.
// Returns a *NotFoundError when no entities are found.
func (chcq *CountryHarmonizedCodeQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = chcq.Limit(2).IDs(setContextOp(ctx, chcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{countryharmonizedcode.Label}
	default:
		err = &NotSingularError{countryharmonizedcode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (chcq *CountryHarmonizedCodeQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := chcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CountryHarmonizedCodes.
func (chcq *CountryHarmonizedCodeQuery) All(ctx context.Context) ([]*CountryHarmonizedCode, error) {
	ctx = setContextOp(ctx, chcq.ctx, ent.OpQueryAll)
	if err := chcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CountryHarmonizedCode, *CountryHarmonizedCodeQuery]()
	return withInterceptors[[]*CountryHarmonizedCode](ctx, chcq, qr, chcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (chcq *CountryHarmonizedCodeQuery) AllX(ctx context.Context) []*CountryHarmonizedCode {
	nodes, err := chcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CountryHarmonizedCode IDs.
func (chcq *CountryHarmonizedCodeQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if chcq.ctx.Unique == nil && chcq.path != nil {
		chcq.Unique(true)
	}
	ctx = setContextOp(ctx, chcq.ctx, ent.OpQueryIDs)
	if err = chcq.Select(countryharmonizedcode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (chcq *CountryHarmonizedCodeQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := chcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (chcq *CountryHarmonizedCodeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, chcq.ctx, ent.OpQueryCount)
	if err := chcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, chcq, querierCount[*CountryHarmonizedCodeQuery](), chcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (chcq *CountryHarmonizedCodeQuery) CountX(ctx context.Context) int {
	count, err := chcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (chcq *CountryHarmonizedCodeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, chcq.ctx, ent.OpQueryExist)
	switch _, err := chcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (chcq *CountryHarmonizedCodeQuery) ExistX(ctx context.Context) bool {
	exist, err := chcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CountryHarmonizedCodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (chcq *CountryHarmonizedCodeQuery) Clone() *CountryHarmonizedCodeQuery {
	if chcq == nil {
		return nil
	}
	return &CountryHarmonizedCodeQuery{
		config:            chcq.config,
		ctx:               chcq.ctx.Clone(),
		order:             append([]countryharmonizedcode.OrderOption{}, chcq.order...),
		inters:            append([]Interceptor{}, chcq.inters...),
		predicates:        append([]predicate.CountryHarmonizedCode{}, chcq.predicates...),
		withTenant:        chcq.withTenant.Clone(),
		withInventoryItem: chcq.withInventoryItem.Clone(),
		withCountry:       chcq.withCountry.Clone(),
		// clone intermediate query.
		sql:  chcq.sql.Clone(),
		path: chcq.path,
	}
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (chcq *CountryHarmonizedCodeQuery) WithTenant(opts ...func(*TenantQuery)) *CountryHarmonizedCodeQuery {
	query := (&TenantClient{config: chcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	chcq.withTenant = query
	return chcq
}

// WithInventoryItem tells the query-builder to eager-load the nodes that are connected to
// the "inventory_item" edge. The optional arguments are used to configure the query builder of the edge.
func (chcq *CountryHarmonizedCodeQuery) WithInventoryItem(opts ...func(*InventoryItemQuery)) *CountryHarmonizedCodeQuery {
	query := (&InventoryItemClient{config: chcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	chcq.withInventoryItem = query
	return chcq
}

// WithCountry tells the query-builder to eager-load the nodes that are connected to
// the "country" edge. The optional arguments are used to configure the query builder of the edge.
func (chcq *CountryHarmonizedCodeQuery) WithCountry(opts ...func(*CountryQuery)) *CountryHarmonizedCodeQuery {
	query := (&CountryClient{config: chcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	chcq.withCountry = query
	return chcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CountryHarmonizedCode.Query().
//		GroupBy(countryharmonizedcode.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (chcq *CountryHarmonizedCodeQuery) GroupBy(field string, fields ...string) *CountryHarmonizedCodeGroupBy {
	chcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CountryHarmonizedCodeGroupBy{build: chcq}
	grbuild.flds = &chcq.ctx.Fields
	grbuild.label = countryharmonizedcode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID pulid.ID `json:"tenant_id,omitempty"`
//	}
//
//	client.CountryHarmonizedCode.Query().
//		Select(countryharmonizedcode.FieldTenantID).
//		Scan(ctx, &v)
func (chcq *CountryHarmonizedCodeQuery) Select(fields ...string) *CountryHarmonizedCodeSelect {
	chcq.ctx.Fields = append(chcq.ctx.Fields, fields...)
	sbuild := &CountryHarmonizedCodeSelect{CountryHarmonizedCodeQuery: chcq}
	sbuild.label = countryharmonizedcode.Label
	sbuild.flds, sbuild.scan = &chcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CountryHarmonizedCodeSelect configured with the given aggregations.
func (chcq *CountryHarmonizedCodeQuery) Aggregate(fns ...AggregateFunc) *CountryHarmonizedCodeSelect {
	return chcq.Select().Aggregate(fns...)
}

func (chcq *CountryHarmonizedCodeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range chcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, chcq); err != nil {
				return err
			}
		}
	}
	for _, f := range chcq.ctx.Fields {
		if !countryharmonizedcode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if chcq.path != nil {
		prev, err := chcq.path(ctx)
		if err != nil {
			return err
		}
		chcq.sql = prev
	}
	if countryharmonizedcode.Policy == nil {
		return errors.New("ent: uninitialized countryharmonizedcode.Policy (forgotten import ent/runtime?)")
	}
	if err := countryharmonizedcode.Policy.EvalQuery(ctx, chcq); err != nil {
		return err
	}
	return nil
}

func (chcq *CountryHarmonizedCodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CountryHarmonizedCode, error) {
	var (
		nodes       = []*CountryHarmonizedCode{}
		withFKs     = chcq.withFKs
		_spec       = chcq.querySpec()
		loadedTypes = [3]bool{
			chcq.withTenant != nil,
			chcq.withInventoryItem != nil,
			chcq.withCountry != nil,
		}
	)
	if chcq.withInventoryItem != nil || chcq.withCountry != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, countryharmonizedcode.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CountryHarmonizedCode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CountryHarmonizedCode{config: chcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(chcq.modifiers) > 0 {
		_spec.Modifiers = chcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, chcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := chcq.withTenant; query != nil {
		if err := chcq.loadTenant(ctx, query, nodes, nil,
			func(n *CountryHarmonizedCode, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	if query := chcq.withInventoryItem; query != nil {
		if err := chcq.loadInventoryItem(ctx, query, nodes, nil,
			func(n *CountryHarmonizedCode, e *InventoryItem) { n.Edges.InventoryItem = e }); err != nil {
			return nil, err
		}
	}
	if query := chcq.withCountry; query != nil {
		if err := chcq.loadCountry(ctx, query, nodes, nil,
			func(n *CountryHarmonizedCode, e *Country) { n.Edges.Country = e }); err != nil {
			return nil, err
		}
	}
	for i := range chcq.loadTotal {
		if err := chcq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (chcq *CountryHarmonizedCodeQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*CountryHarmonizedCode, init func(*CountryHarmonizedCode), assign func(*CountryHarmonizedCode, *Tenant)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*CountryHarmonizedCode)
	for i := range nodes {
		fk := nodes[i].TenantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (chcq *CountryHarmonizedCodeQuery) loadInventoryItem(ctx context.Context, query *InventoryItemQuery, nodes []*CountryHarmonizedCode, init func(*CountryHarmonizedCode), assign func(*CountryHarmonizedCode, *InventoryItem)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*CountryHarmonizedCode)
	for i := range nodes {
		if nodes[i].inventory_item_country_harmonized_code == nil {
			continue
		}
		fk := *nodes[i].inventory_item_country_harmonized_code
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(inventoryitem.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "inventory_item_country_harmonized_code" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (chcq *CountryHarmonizedCodeQuery) loadCountry(ctx context.Context, query *CountryQuery, nodes []*CountryHarmonizedCode, init func(*CountryHarmonizedCode), assign func(*CountryHarmonizedCode, *Country)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*CountryHarmonizedCode)
	for i := range nodes {
		if nodes[i].country_harmonized_code_country == nil {
			continue
		}
		fk := *nodes[i].country_harmonized_code_country
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(country.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "country_harmonized_code_country" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (chcq *CountryHarmonizedCodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := chcq.querySpec()
	if len(chcq.modifiers) > 0 {
		_spec.Modifiers = chcq.modifiers
	}
	_spec.Node.Columns = chcq.ctx.Fields
	if len(chcq.ctx.Fields) > 0 {
		_spec.Unique = chcq.ctx.Unique != nil && *chcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, chcq.driver, _spec)
}

func (chcq *CountryHarmonizedCodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(countryharmonizedcode.Table, countryharmonizedcode.Columns, sqlgraph.NewFieldSpec(countryharmonizedcode.FieldID, field.TypeString))
	_spec.From = chcq.sql
	if unique := chcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if chcq.path != nil {
		_spec.Unique = true
	}
	if fields := chcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, countryharmonizedcode.FieldID)
		for i := range fields {
			if fields[i] != countryharmonizedcode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if chcq.withTenant != nil {
			_spec.Node.AddColumnOnce(countryharmonizedcode.FieldTenantID)
		}
	}
	if ps := chcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := chcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := chcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := chcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (chcq *CountryHarmonizedCodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(chcq.driver.Dialect())
	t1 := builder.Table(countryharmonizedcode.Table)
	columns := chcq.ctx.Fields
	if len(columns) == 0 {
		columns = countryharmonizedcode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if chcq.sql != nil {
		selector = chcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if chcq.ctx.Unique != nil && *chcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range chcq.predicates {
		p(selector)
	}
	for _, p := range chcq.order {
		p(selector)
	}
	if offset := chcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := chcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CountryHarmonizedCodeGroupBy is the group-by builder for CountryHarmonizedCode entities.
type CountryHarmonizedCodeGroupBy struct {
	selector
	build *CountryHarmonizedCodeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (chcgb *CountryHarmonizedCodeGroupBy) Aggregate(fns ...AggregateFunc) *CountryHarmonizedCodeGroupBy {
	chcgb.fns = append(chcgb.fns, fns...)
	return chcgb
}

// Scan applies the selector query and scans the result into the given value.
func (chcgb *CountryHarmonizedCodeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, chcgb.build.ctx, ent.OpQueryGroupBy)
	if err := chcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CountryHarmonizedCodeQuery, *CountryHarmonizedCodeGroupBy](ctx, chcgb.build, chcgb, chcgb.build.inters, v)
}

func (chcgb *CountryHarmonizedCodeGroupBy) sqlScan(ctx context.Context, root *CountryHarmonizedCodeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(chcgb.fns))
	for _, fn := range chcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*chcgb.flds)+len(chcgb.fns))
		for _, f := range *chcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*chcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := chcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CountryHarmonizedCodeSelect is the builder for selecting fields of CountryHarmonizedCode entities.
type CountryHarmonizedCodeSelect struct {
	*CountryHarmonizedCodeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (chcs *CountryHarmonizedCodeSelect) Aggregate(fns ...AggregateFunc) *CountryHarmonizedCodeSelect {
	chcs.fns = append(chcs.fns, fns...)
	return chcs
}

// Scan applies the selector query and scans the result into the given value.
func (chcs *CountryHarmonizedCodeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, chcs.ctx, ent.OpQuerySelect)
	if err := chcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CountryHarmonizedCodeQuery, *CountryHarmonizedCodeSelect](ctx, chcs.CountryHarmonizedCodeQuery, chcs, chcs.inters, v)
}

func (chcs *CountryHarmonizedCodeSelect) sqlScan(ctx context.Context, root *CountryHarmonizedCodeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(chcs.fns))
	for _, fn := range chcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*chcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := chcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
