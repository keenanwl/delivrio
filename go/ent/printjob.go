// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"delivrio.io/go/ent/colli"
	"delivrio.io/go/ent/printer"
	"delivrio.io/go/ent/printjob"
	"delivrio.io/go/ent/shipmentparcel"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PrintJob is the model entity for the PrintJob schema.
type PrintJob struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// Status holds the value of the "status" field.
	Status printjob.Status `json:"status,omitempty"`
	// FileExtension holds the value of the "file_extension" field.
	FileExtension printjob.FileExtension `json:"file_extension,omitempty"`
	// DocumentType holds the value of the "document_type" field.
	DocumentType printjob.DocumentType `json:"document_type,omitempty"`
	// PrinterMessages holds the value of the "printer_messages" field.
	PrinterMessages []string `json:"printer_messages,omitempty"`
	// Base64PrintData holds the value of the "base64_print_data" field.
	Base64PrintData string `json:"base64_print_data,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PrintJobQuery when eager-loading is set.
	Edges                     PrintJobEdges `json:"edges"`
	print_job_printer         *pulid.ID
	print_job_colli           *pulid.ID
	print_job_shipment_parcel *pulid.ID
	selectValues              sql.SelectValues
}

// PrintJobEdges holds the relations/edges for other nodes in the graph.
type PrintJobEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Printer holds the value of the printer edge.
	Printer *Printer `json:"printer,omitempty"`
	// Colli holds the value of the colli edge.
	Colli *Colli `json:"colli,omitempty"`
	// ShipmentParcel holds the value of the shipment_parcel edge.
	ShipmentParcel *ShipmentParcel `json:"shipment_parcel,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
	// totalCount holds the count of the edges above.
	totalCount [4]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PrintJobEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// PrinterOrErr returns the Printer value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PrintJobEdges) PrinterOrErr() (*Printer, error) {
	if e.Printer != nil {
		return e.Printer, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: printer.Label}
	}
	return nil, &NotLoadedError{edge: "printer"}
}

// ColliOrErr returns the Colli value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PrintJobEdges) ColliOrErr() (*Colli, error) {
	if e.Colli != nil {
		return e.Colli, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: colli.Label}
	}
	return nil, &NotLoadedError{edge: "colli"}
}

// ShipmentParcelOrErr returns the ShipmentParcel value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PrintJobEdges) ShipmentParcelOrErr() (*ShipmentParcel, error) {
	if e.ShipmentParcel != nil {
		return e.ShipmentParcel, nil
	} else if e.loadedTypes[3] {
		return nil, &NotFoundError{label: shipmentparcel.Label}
	}
	return nil, &NotLoadedError{edge: "shipment_parcel"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PrintJob) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case printjob.FieldPrinterMessages:
			values[i] = new([]byte)
		case printjob.FieldID, printjob.FieldTenantID:
			values[i] = new(pulid.ID)
		case printjob.FieldStatus, printjob.FieldFileExtension, printjob.FieldDocumentType, printjob.FieldBase64PrintData:
			values[i] = new(sql.NullString)
		case printjob.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case printjob.ForeignKeys[0]: // print_job_printer
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		case printjob.ForeignKeys[1]: // print_job_colli
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		case printjob.ForeignKeys[2]: // print_job_shipment_parcel
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PrintJob fields.
func (pj *PrintJob) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case printjob.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pj.ID = *value
			}
		case printjob.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				pj.TenantID = *value
			}
		case printjob.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				pj.Status = printjob.Status(value.String)
			}
		case printjob.FieldFileExtension:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field file_extension", values[i])
			} else if value.Valid {
				pj.FileExtension = printjob.FileExtension(value.String)
			}
		case printjob.FieldDocumentType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field document_type", values[i])
			} else if value.Valid {
				pj.DocumentType = printjob.DocumentType(value.String)
			}
		case printjob.FieldPrinterMessages:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field printer_messages", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &pj.PrinterMessages); err != nil {
					return fmt.Errorf("unmarshal field printer_messages: %w", err)
				}
			}
		case printjob.FieldBase64PrintData:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field base64_print_data", values[i])
			} else if value.Valid {
				pj.Base64PrintData = value.String
			}
		case printjob.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pj.CreatedAt = value.Time
			}
		case printjob.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field print_job_printer", values[i])
			} else if value.Valid {
				pj.print_job_printer = new(pulid.ID)
				*pj.print_job_printer = *value.S.(*pulid.ID)
			}
		case printjob.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field print_job_colli", values[i])
			} else if value.Valid {
				pj.print_job_colli = new(pulid.ID)
				*pj.print_job_colli = *value.S.(*pulid.ID)
			}
		case printjob.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field print_job_shipment_parcel", values[i])
			} else if value.Valid {
				pj.print_job_shipment_parcel = new(pulid.ID)
				*pj.print_job_shipment_parcel = *value.S.(*pulid.ID)
			}
		default:
			pj.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PrintJob.
// This includes values selected through modifiers, order, etc.
func (pj *PrintJob) Value(name string) (ent.Value, error) {
	return pj.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the PrintJob entity.
func (pj *PrintJob) QueryTenant() *TenantQuery {
	return NewPrintJobClient(pj.config).QueryTenant(pj)
}

// QueryPrinter queries the "printer" edge of the PrintJob entity.
func (pj *PrintJob) QueryPrinter() *PrinterQuery {
	return NewPrintJobClient(pj.config).QueryPrinter(pj)
}

// QueryColli queries the "colli" edge of the PrintJob entity.
func (pj *PrintJob) QueryColli() *ColliQuery {
	return NewPrintJobClient(pj.config).QueryColli(pj)
}

// QueryShipmentParcel queries the "shipment_parcel" edge of the PrintJob entity.
func (pj *PrintJob) QueryShipmentParcel() *ShipmentParcelQuery {
	return NewPrintJobClient(pj.config).QueryShipmentParcel(pj)
}

// Update returns a builder for updating this PrintJob.
// Note that you need to call PrintJob.Unwrap() before calling this method if this PrintJob
// was returned from a transaction, and the transaction was committed or rolled back.
func (pj *PrintJob) Update() *PrintJobUpdateOne {
	return NewPrintJobClient(pj.config).UpdateOne(pj)
}

// Unwrap unwraps the PrintJob entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pj *PrintJob) Unwrap() *PrintJob {
	_tx, ok := pj.config.driver.(*txDriver)
	if !ok {
		panic("ent: PrintJob is not a transactional entity")
	}
	pj.config.driver = _tx.drv
	return pj
}

// String implements the fmt.Stringer.
func (pj *PrintJob) String() string {
	var builder strings.Builder
	builder.WriteString("PrintJob(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pj.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", pj.TenantID))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", pj.Status))
	builder.WriteString(", ")
	builder.WriteString("file_extension=")
	builder.WriteString(fmt.Sprintf("%v", pj.FileExtension))
	builder.WriteString(", ")
	builder.WriteString("document_type=")
	builder.WriteString(fmt.Sprintf("%v", pj.DocumentType))
	builder.WriteString(", ")
	builder.WriteString("printer_messages=")
	builder.WriteString(fmt.Sprintf("%v", pj.PrinterMessages))
	builder.WriteString(", ")
	builder.WriteString("base64_print_data=")
	builder.WriteString(pj.Base64PrintData)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(pj.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PrintJobs is a parsable slice of PrintJob.
type PrintJobs []*PrintJob
