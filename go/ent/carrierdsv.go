// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/carrier"
	"delivrio.io/go/ent/carrierdsv"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CarrierDSV is the model entity for the CarrierDSV schema.
type CarrierDSV struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CarrierDSVQuery when eager-loading is set.
	Edges               CarrierDSVEdges `json:"edges"`
	carrier_carrier_dsv *pulid.ID
	selectValues        sql.SelectValues
}

// CarrierDSVEdges holds the relations/edges for other nodes in the graph.
type CarrierDSVEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Carrier holds the value of the carrier edge.
	Carrier *Carrier `json:"carrier,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierDSVEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// CarrierOrErr returns the Carrier value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierDSVEdges) CarrierOrErr() (*Carrier, error) {
	if e.Carrier != nil {
		return e.Carrier, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: carrier.Label}
	}
	return nil, &NotLoadedError{edge: "carrier"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CarrierDSV) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case carrierdsv.FieldID, carrierdsv.FieldTenantID:
			values[i] = new(pulid.ID)
		case carrierdsv.ForeignKeys[0]: // carrier_carrier_dsv
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CarrierDSV fields.
func (cd *CarrierDSV) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case carrierdsv.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				cd.ID = *value
			}
		case carrierdsv.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				cd.TenantID = *value
			}
		case carrierdsv.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field carrier_carrier_dsv", values[i])
			} else if value.Valid {
				cd.carrier_carrier_dsv = new(pulid.ID)
				*cd.carrier_carrier_dsv = *value.S.(*pulid.ID)
			}
		default:
			cd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CarrierDSV.
// This includes values selected through modifiers, order, etc.
func (cd *CarrierDSV) Value(name string) (ent.Value, error) {
	return cd.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the CarrierDSV entity.
func (cd *CarrierDSV) QueryTenant() *TenantQuery {
	return NewCarrierDSVClient(cd.config).QueryTenant(cd)
}

// QueryCarrier queries the "carrier" edge of the CarrierDSV entity.
func (cd *CarrierDSV) QueryCarrier() *CarrierQuery {
	return NewCarrierDSVClient(cd.config).QueryCarrier(cd)
}

// Update returns a builder for updating this CarrierDSV.
// Note that you need to call CarrierDSV.Unwrap() before calling this method if this CarrierDSV
// was returned from a transaction, and the transaction was committed or rolled back.
func (cd *CarrierDSV) Update() *CarrierDSVUpdateOne {
	return NewCarrierDSVClient(cd.config).UpdateOne(cd)
}

// Unwrap unwraps the CarrierDSV entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cd *CarrierDSV) Unwrap() *CarrierDSV {
	_tx, ok := cd.config.driver.(*txDriver)
	if !ok {
		panic("ent: CarrierDSV is not a transactional entity")
	}
	cd.config.driver = _tx.drv
	return cd
}

// String implements the fmt.Stringer.
func (cd *CarrierDSV) String() string {
	var builder strings.Builder
	builder.WriteString("CarrierDSV(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cd.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", cd.TenantID))
	builder.WriteByte(')')
	return builder.String()
}

// CarrierDSVs is a parsable slice of CarrierDSV.
type CarrierDSVs []*CarrierDSV
