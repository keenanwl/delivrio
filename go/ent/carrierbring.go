// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/carrier"
	"delivrio.io/go/ent/carrierbring"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CarrierBring is the model entity for the CarrierBring schema.
type CarrierBring struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// APIKey holds the value of the "api_key" field.
	APIKey string `json:"api_key,omitempty"`
	// CustomerNumber holds the value of the "customer_number" field.
	CustomerNumber string `json:"customer_number,omitempty"`
	// Test holds the value of the "test" field.
	Test bool `json:"test,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CarrierBringQuery when eager-loading is set.
	Edges                 CarrierBringEdges `json:"edges"`
	carrier_carrier_bring *pulid.ID
	selectValues          sql.SelectValues
}

// CarrierBringEdges holds the relations/edges for other nodes in the graph.
type CarrierBringEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Carrier holds the value of the carrier edge.
	Carrier *Carrier `json:"carrier,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierBringEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// CarrierOrErr returns the Carrier value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierBringEdges) CarrierOrErr() (*Carrier, error) {
	if e.Carrier != nil {
		return e.Carrier, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: carrier.Label}
	}
	return nil, &NotLoadedError{edge: "carrier"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CarrierBring) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case carrierbring.FieldID, carrierbring.FieldTenantID:
			values[i] = new(pulid.ID)
		case carrierbring.FieldTest:
			values[i] = new(sql.NullBool)
		case carrierbring.FieldAPIKey, carrierbring.FieldCustomerNumber:
			values[i] = new(sql.NullString)
		case carrierbring.ForeignKeys[0]: // carrier_carrier_bring
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CarrierBring fields.
func (cb *CarrierBring) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case carrierbring.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				cb.ID = *value
			}
		case carrierbring.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				cb.TenantID = *value
			}
		case carrierbring.FieldAPIKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field api_key", values[i])
			} else if value.Valid {
				cb.APIKey = value.String
			}
		case carrierbring.FieldCustomerNumber:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field customer_number", values[i])
			} else if value.Valid {
				cb.CustomerNumber = value.String
			}
		case carrierbring.FieldTest:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field test", values[i])
			} else if value.Valid {
				cb.Test = value.Bool
			}
		case carrierbring.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field carrier_carrier_bring", values[i])
			} else if value.Valid {
				cb.carrier_carrier_bring = new(pulid.ID)
				*cb.carrier_carrier_bring = *value.S.(*pulid.ID)
			}
		default:
			cb.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CarrierBring.
// This includes values selected through modifiers, order, etc.
func (cb *CarrierBring) Value(name string) (ent.Value, error) {
	return cb.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the CarrierBring entity.
func (cb *CarrierBring) QueryTenant() *TenantQuery {
	return NewCarrierBringClient(cb.config).QueryTenant(cb)
}

// QueryCarrier queries the "carrier" edge of the CarrierBring entity.
func (cb *CarrierBring) QueryCarrier() *CarrierQuery {
	return NewCarrierBringClient(cb.config).QueryCarrier(cb)
}

// Update returns a builder for updating this CarrierBring.
// Note that you need to call CarrierBring.Unwrap() before calling this method if this CarrierBring
// was returned from a transaction, and the transaction was committed or rolled back.
func (cb *CarrierBring) Update() *CarrierBringUpdateOne {
	return NewCarrierBringClient(cb.config).UpdateOne(cb)
}

// Unwrap unwraps the CarrierBring entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cb *CarrierBring) Unwrap() *CarrierBring {
	_tx, ok := cb.config.driver.(*txDriver)
	if !ok {
		panic("ent: CarrierBring is not a transactional entity")
	}
	cb.config.driver = _tx.drv
	return cb
}

// String implements the fmt.Stringer.
func (cb *CarrierBring) String() string {
	var builder strings.Builder
	builder.WriteString("CarrierBring(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cb.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", cb.TenantID))
	builder.WriteString(", ")
	builder.WriteString("api_key=")
	builder.WriteString(cb.APIKey)
	builder.WriteString(", ")
	builder.WriteString("customer_number=")
	builder.WriteString(cb.CustomerNumber)
	builder.WriteString(", ")
	builder.WriteString("test=")
	builder.WriteString(fmt.Sprintf("%v", cb.Test))
	builder.WriteByte(')')
	return builder.String()
}

// CarrierBrings is a parsable slice of CarrierBring.
type CarrierBrings []*CarrierBring
