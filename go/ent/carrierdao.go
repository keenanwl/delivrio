// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/carrier"
	"delivrio.io/go/ent/carrierdao"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CarrierDAO is the model entity for the CarrierDAO schema.
type CarrierDAO struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// CustomerID holds the value of the "customer_id" field.
	CustomerID string `json:"customer_id,omitempty"`
	// APIKey holds the value of the "api_key" field.
	APIKey string `json:"api_key,omitempty"`
	// Test holds the value of the "Test" field.
	Test bool `json:"Test,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CarrierDAOQuery when eager-loading is set.
	Edges               CarrierDAOEdges `json:"edges"`
	carrier_carrier_dao *pulid.ID
	selectValues        sql.SelectValues
}

// CarrierDAOEdges holds the relations/edges for other nodes in the graph.
type CarrierDAOEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Carrier holds the value of the carrier edge.
	Carrier *Carrier `json:"carrier,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierDAOEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// CarrierOrErr returns the Carrier value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CarrierDAOEdges) CarrierOrErr() (*Carrier, error) {
	if e.Carrier != nil {
		return e.Carrier, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: carrier.Label}
	}
	return nil, &NotLoadedError{edge: "carrier"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CarrierDAO) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case carrierdao.FieldID, carrierdao.FieldTenantID:
			values[i] = new(pulid.ID)
		case carrierdao.FieldTest:
			values[i] = new(sql.NullBool)
		case carrierdao.FieldCustomerID, carrierdao.FieldAPIKey:
			values[i] = new(sql.NullString)
		case carrierdao.ForeignKeys[0]: // carrier_carrier_dao
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CarrierDAO fields.
func (cd *CarrierDAO) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case carrierdao.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				cd.ID = *value
			}
		case carrierdao.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				cd.TenantID = *value
			}
		case carrierdao.FieldCustomerID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field customer_id", values[i])
			} else if value.Valid {
				cd.CustomerID = value.String
			}
		case carrierdao.FieldAPIKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field api_key", values[i])
			} else if value.Valid {
				cd.APIKey = value.String
			}
		case carrierdao.FieldTest:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field Test", values[i])
			} else if value.Valid {
				cd.Test = value.Bool
			}
		case carrierdao.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field carrier_carrier_dao", values[i])
			} else if value.Valid {
				cd.carrier_carrier_dao = new(pulid.ID)
				*cd.carrier_carrier_dao = *value.S.(*pulid.ID)
			}
		default:
			cd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CarrierDAO.
// This includes values selected through modifiers, order, etc.
func (cd *CarrierDAO) Value(name string) (ent.Value, error) {
	return cd.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the CarrierDAO entity.
func (cd *CarrierDAO) QueryTenant() *TenantQuery {
	return NewCarrierDAOClient(cd.config).QueryTenant(cd)
}

// QueryCarrier queries the "carrier" edge of the CarrierDAO entity.
func (cd *CarrierDAO) QueryCarrier() *CarrierQuery {
	return NewCarrierDAOClient(cd.config).QueryCarrier(cd)
}

// Update returns a builder for updating this CarrierDAO.
// Note that you need to call CarrierDAO.Unwrap() before calling this method if this CarrierDAO
// was returned from a transaction, and the transaction was committed or rolled back.
func (cd *CarrierDAO) Update() *CarrierDAOUpdateOne {
	return NewCarrierDAOClient(cd.config).UpdateOne(cd)
}

// Unwrap unwraps the CarrierDAO entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cd *CarrierDAO) Unwrap() *CarrierDAO {
	_tx, ok := cd.config.driver.(*txDriver)
	if !ok {
		panic("ent: CarrierDAO is not a transactional entity")
	}
	cd.config.driver = _tx.drv
	return cd
}

// String implements the fmt.Stringer.
func (cd *CarrierDAO) String() string {
	var builder strings.Builder
	builder.WriteString("CarrierDAO(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cd.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", cd.TenantID))
	builder.WriteString(", ")
	builder.WriteString("customer_id=")
	builder.WriteString(cd.CustomerID)
	builder.WriteString(", ")
	builder.WriteString("api_key=")
	builder.WriteString(cd.APIKey)
	builder.WriteString(", ")
	builder.WriteString("Test=")
	builder.WriteString(fmt.Sprintf("%v", cd.Test))
	builder.WriteByte(')')
	return builder.String()
}

// CarrierDAOs is a parsable slice of CarrierDAO.
type CarrierDAOs []*CarrierDAO
