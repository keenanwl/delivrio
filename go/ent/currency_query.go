// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/currency"
	"delivrio.io/go/ent/deliveryrule"
	"delivrio.io/go/ent/orderline"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CurrencyQuery is the builder for querying Currency entities.
type CurrencyQuery struct {
	config
	ctx                   *QueryContext
	order                 []currency.OrderOption
	inters                []Interceptor
	predicates            []predicate.Currency
	withOrderLine         *OrderLineQuery
	withDeliveryRule      *DeliveryRuleQuery
	modifiers             []func(*sql.Selector)
	loadTotal             []func(context.Context, []*Currency) error
	withNamedOrderLine    map[string]*OrderLineQuery
	withNamedDeliveryRule map[string]*DeliveryRuleQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CurrencyQuery builder.
func (cq *CurrencyQuery) Where(ps ...predicate.Currency) *CurrencyQuery {
	cq.predicates = append(cq.predicates, ps...)
	return cq
}

// Limit the number of records to be returned by this query.
func (cq *CurrencyQuery) Limit(limit int) *CurrencyQuery {
	cq.ctx.Limit = &limit
	return cq
}

// Offset to start from.
func (cq *CurrencyQuery) Offset(offset int) *CurrencyQuery {
	cq.ctx.Offset = &offset
	return cq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cq *CurrencyQuery) Unique(unique bool) *CurrencyQuery {
	cq.ctx.Unique = &unique
	return cq
}

// Order specifies how the records should be ordered.
func (cq *CurrencyQuery) Order(o ...currency.OrderOption) *CurrencyQuery {
	cq.order = append(cq.order, o...)
	return cq
}

// QueryOrderLine chains the current query on the "order_line" edge.
func (cq *CurrencyQuery) QueryOrderLine() *OrderLineQuery {
	query := (&OrderLineClient{config: cq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(currency.Table, currency.FieldID, selector),
			sqlgraph.To(orderline.Table, orderline.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, currency.OrderLineTable, currency.OrderLineColumn),
		)
		fromU = sqlgraph.SetNeighbors(cq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDeliveryRule chains the current query on the "delivery_rule" edge.
func (cq *CurrencyQuery) QueryDeliveryRule() *DeliveryRuleQuery {
	query := (&DeliveryRuleClient{config: cq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(currency.Table, currency.FieldID, selector),
			sqlgraph.To(deliveryrule.Table, deliveryrule.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, currency.DeliveryRuleTable, currency.DeliveryRuleColumn),
		)
		fromU = sqlgraph.SetNeighbors(cq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Currency entity from the query.
// Returns a *NotFoundError when no Currency was found.
func (cq *CurrencyQuery) First(ctx context.Context) (*Currency, error) {
	nodes, err := cq.Limit(1).All(setContextOp(ctx, cq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{currency.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cq *CurrencyQuery) FirstX(ctx context.Context) *Currency {
	node, err := cq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Currency ID from the query.
// Returns a *NotFoundError when no Currency ID was found.
func (cq *CurrencyQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = cq.Limit(1).IDs(setContextOp(ctx, cq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{currency.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cq *CurrencyQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := cq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Currency entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Currency entity is found.
// Returns a *NotFoundError when no Currency entities are found.
func (cq *CurrencyQuery) Only(ctx context.Context) (*Currency, error) {
	nodes, err := cq.Limit(2).All(setContextOp(ctx, cq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{currency.Label}
	default:
		return nil, &NotSingularError{currency.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cq *CurrencyQuery) OnlyX(ctx context.Context) *Currency {
	node, err := cq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Currency ID in the query.
// Returns a *NotSingularError when more than one Currency ID is found.
// Returns a *NotFoundError when no entities are found.
func (cq *CurrencyQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = cq.Limit(2).IDs(setContextOp(ctx, cq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{currency.Label}
	default:
		err = &NotSingularError{currency.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cq *CurrencyQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := cq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Currencies.
func (cq *CurrencyQuery) All(ctx context.Context) ([]*Currency, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryAll)
	if err := cq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Currency, *CurrencyQuery]()
	return withInterceptors[[]*Currency](ctx, cq, qr, cq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cq *CurrencyQuery) AllX(ctx context.Context) []*Currency {
	nodes, err := cq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Currency IDs.
func (cq *CurrencyQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if cq.ctx.Unique == nil && cq.path != nil {
		cq.Unique(true)
	}
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryIDs)
	if err = cq.Select(currency.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cq *CurrencyQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := cq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cq *CurrencyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryCount)
	if err := cq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cq, querierCount[*CurrencyQuery](), cq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cq *CurrencyQuery) CountX(ctx context.Context) int {
	count, err := cq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cq *CurrencyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryExist)
	switch _, err := cq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cq *CurrencyQuery) ExistX(ctx context.Context) bool {
	exist, err := cq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CurrencyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cq *CurrencyQuery) Clone() *CurrencyQuery {
	if cq == nil {
		return nil
	}
	return &CurrencyQuery{
		config:           cq.config,
		ctx:              cq.ctx.Clone(),
		order:            append([]currency.OrderOption{}, cq.order...),
		inters:           append([]Interceptor{}, cq.inters...),
		predicates:       append([]predicate.Currency{}, cq.predicates...),
		withOrderLine:    cq.withOrderLine.Clone(),
		withDeliveryRule: cq.withDeliveryRule.Clone(),
		// clone intermediate query.
		sql:  cq.sql.Clone(),
		path: cq.path,
	}
}

// WithOrderLine tells the query-builder to eager-load the nodes that are connected to
// the "order_line" edge. The optional arguments are used to configure the query builder of the edge.
func (cq *CurrencyQuery) WithOrderLine(opts ...func(*OrderLineQuery)) *CurrencyQuery {
	query := (&OrderLineClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cq.withOrderLine = query
	return cq
}

// WithDeliveryRule tells the query-builder to eager-load the nodes that are connected to
// the "delivery_rule" edge. The optional arguments are used to configure the query builder of the edge.
func (cq *CurrencyQuery) WithDeliveryRule(opts ...func(*DeliveryRuleQuery)) *CurrencyQuery {
	query := (&DeliveryRuleClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cq.withDeliveryRule = query
	return cq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Display string `json:"display,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Currency.Query().
//		GroupBy(currency.FieldDisplay).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cq *CurrencyQuery) GroupBy(field string, fields ...string) *CurrencyGroupBy {
	cq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CurrencyGroupBy{build: cq}
	grbuild.flds = &cq.ctx.Fields
	grbuild.label = currency.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Display string `json:"display,omitempty"`
//	}
//
//	client.Currency.Query().
//		Select(currency.FieldDisplay).
//		Scan(ctx, &v)
func (cq *CurrencyQuery) Select(fields ...string) *CurrencySelect {
	cq.ctx.Fields = append(cq.ctx.Fields, fields...)
	sbuild := &CurrencySelect{CurrencyQuery: cq}
	sbuild.label = currency.Label
	sbuild.flds, sbuild.scan = &cq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CurrencySelect configured with the given aggregations.
func (cq *CurrencyQuery) Aggregate(fns ...AggregateFunc) *CurrencySelect {
	return cq.Select().Aggregate(fns...)
}

func (cq *CurrencyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cq); err != nil {
				return err
			}
		}
	}
	for _, f := range cq.ctx.Fields {
		if !currency.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cq.path != nil {
		prev, err := cq.path(ctx)
		if err != nil {
			return err
		}
		cq.sql = prev
	}
	if currency.Policy == nil {
		return errors.New("ent: uninitialized currency.Policy (forgotten import ent/runtime?)")
	}
	if err := currency.Policy.EvalQuery(ctx, cq); err != nil {
		return err
	}
	return nil
}

func (cq *CurrencyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Currency, error) {
	var (
		nodes       = []*Currency{}
		_spec       = cq.querySpec()
		loadedTypes = [2]bool{
			cq.withOrderLine != nil,
			cq.withDeliveryRule != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Currency).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Currency{config: cq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(cq.modifiers) > 0 {
		_spec.Modifiers = cq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cq.withOrderLine; query != nil {
		if err := cq.loadOrderLine(ctx, query, nodes,
			func(n *Currency) { n.Edges.OrderLine = []*OrderLine{} },
			func(n *Currency, e *OrderLine) { n.Edges.OrderLine = append(n.Edges.OrderLine, e) }); err != nil {
			return nil, err
		}
	}
	if query := cq.withDeliveryRule; query != nil {
		if err := cq.loadDeliveryRule(ctx, query, nodes,
			func(n *Currency) { n.Edges.DeliveryRule = []*DeliveryRule{} },
			func(n *Currency, e *DeliveryRule) { n.Edges.DeliveryRule = append(n.Edges.DeliveryRule, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range cq.withNamedOrderLine {
		if err := cq.loadOrderLine(ctx, query, nodes,
			func(n *Currency) { n.appendNamedOrderLine(name) },
			func(n *Currency, e *OrderLine) { n.appendNamedOrderLine(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range cq.withNamedDeliveryRule {
		if err := cq.loadDeliveryRule(ctx, query, nodes,
			func(n *Currency) { n.appendNamedDeliveryRule(name) },
			func(n *Currency, e *DeliveryRule) { n.appendNamedDeliveryRule(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range cq.loadTotal {
		if err := cq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cq *CurrencyQuery) loadOrderLine(ctx context.Context, query *OrderLineQuery, nodes []*Currency, init func(*Currency), assign func(*Currency, *OrderLine)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[pulid.ID]*Currency)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.OrderLine(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(currency.OrderLineColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.order_line_currency
		if fk == nil {
			return fmt.Errorf(`foreign-key "order_line_currency" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "order_line_currency" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (cq *CurrencyQuery) loadDeliveryRule(ctx context.Context, query *DeliveryRuleQuery, nodes []*Currency, init func(*Currency), assign func(*Currency, *DeliveryRule)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[pulid.ID]*Currency)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.DeliveryRule(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(currency.DeliveryRuleColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.delivery_rule_currency
		if fk == nil {
			return fmt.Errorf(`foreign-key "delivery_rule_currency" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "delivery_rule_currency" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (cq *CurrencyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cq.querySpec()
	if len(cq.modifiers) > 0 {
		_spec.Modifiers = cq.modifiers
	}
	_spec.Node.Columns = cq.ctx.Fields
	if len(cq.ctx.Fields) > 0 {
		_spec.Unique = cq.ctx.Unique != nil && *cq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cq.driver, _spec)
}

func (cq *CurrencyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(currency.Table, currency.Columns, sqlgraph.NewFieldSpec(currency.FieldID, field.TypeString))
	_spec.From = cq.sql
	if unique := cq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cq.path != nil {
		_spec.Unique = true
	}
	if fields := cq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, currency.FieldID)
		for i := range fields {
			if fields[i] != currency.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cq *CurrencyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cq.driver.Dialect())
	t1 := builder.Table(currency.Table)
	columns := cq.ctx.Fields
	if len(columns) == 0 {
		columns = currency.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cq.sql != nil {
		selector = cq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cq.ctx.Unique != nil && *cq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cq.predicates {
		p(selector)
	}
	for _, p := range cq.order {
		p(selector)
	}
	if offset := cq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedOrderLine tells the query-builder to eager-load the nodes that are connected to the "order_line"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (cq *CurrencyQuery) WithNamedOrderLine(name string, opts ...func(*OrderLineQuery)) *CurrencyQuery {
	query := (&OrderLineClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if cq.withNamedOrderLine == nil {
		cq.withNamedOrderLine = make(map[string]*OrderLineQuery)
	}
	cq.withNamedOrderLine[name] = query
	return cq
}

// WithNamedDeliveryRule tells the query-builder to eager-load the nodes that are connected to the "delivery_rule"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (cq *CurrencyQuery) WithNamedDeliveryRule(name string, opts ...func(*DeliveryRuleQuery)) *CurrencyQuery {
	query := (&DeliveryRuleClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if cq.withNamedDeliveryRule == nil {
		cq.withNamedDeliveryRule = make(map[string]*DeliveryRuleQuery)
	}
	cq.withNamedDeliveryRule[name] = query
	return cq
}

// CurrencyGroupBy is the group-by builder for Currency entities.
type CurrencyGroupBy struct {
	selector
	build *CurrencyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cgb *CurrencyGroupBy) Aggregate(fns ...AggregateFunc) *CurrencyGroupBy {
	cgb.fns = append(cgb.fns, fns...)
	return cgb
}

// Scan applies the selector query and scans the result into the given value.
func (cgb *CurrencyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cgb.build.ctx, ent.OpQueryGroupBy)
	if err := cgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CurrencyQuery, *CurrencyGroupBy](ctx, cgb.build, cgb, cgb.build.inters, v)
}

func (cgb *CurrencyGroupBy) sqlScan(ctx context.Context, root *CurrencyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cgb.fns))
	for _, fn := range cgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cgb.flds)+len(cgb.fns))
		for _, f := range *cgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CurrencySelect is the builder for selecting fields of Currency entities.
type CurrencySelect struct {
	*CurrencyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cs *CurrencySelect) Aggregate(fns ...AggregateFunc) *CurrencySelect {
	cs.fns = append(cs.fns, fns...)
	return cs
}

// Scan applies the selector query and scans the result into the given value.
func (cs *CurrencySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cs.ctx, ent.OpQuerySelect)
	if err := cs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CurrencyQuery, *CurrencySelect](ctx, cs.CurrencyQuery, cs, cs.inters, v)
}

func (cs *CurrencySelect) sqlScan(ctx context.Context, root *CurrencyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cs.fns))
	for _, fn := range cs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
