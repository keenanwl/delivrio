// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"delivrio.io/go/ent/addressglobal"
	"delivrio.io/go/ent/country"
	"delivrio.io/go/ent/parcelshop"
	"delivrio.io/go/ent/parcelshopbring"
	"delivrio.io/go/ent/parcelshoppostnord"
	"delivrio.io/go/ent/predicate"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// AddressGlobalQuery is the builder for querying AddressGlobal entities.
type AddressGlobalQuery struct {
	config
	ctx                            *QueryContext
	order                          []addressglobal.OrderOption
	inters                         []Interceptor
	predicates                     []predicate.AddressGlobal
	withParcelShopPostNordDelivery *ParcelShopPostNordQuery
	withParcelShopBringDelivery    *ParcelShopBringQuery
	withParcelShop                 *ParcelShopQuery
	withCountry                    *CountryQuery
	withFKs                        bool
	modifiers                      []func(*sql.Selector)
	loadTotal                      []func(context.Context, []*AddressGlobal) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AddressGlobalQuery builder.
func (agq *AddressGlobalQuery) Where(ps ...predicate.AddressGlobal) *AddressGlobalQuery {
	agq.predicates = append(agq.predicates, ps...)
	return agq
}

// Limit the number of records to be returned by this query.
func (agq *AddressGlobalQuery) Limit(limit int) *AddressGlobalQuery {
	agq.ctx.Limit = &limit
	return agq
}

// Offset to start from.
func (agq *AddressGlobalQuery) Offset(offset int) *AddressGlobalQuery {
	agq.ctx.Offset = &offset
	return agq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (agq *AddressGlobalQuery) Unique(unique bool) *AddressGlobalQuery {
	agq.ctx.Unique = &unique
	return agq
}

// Order specifies how the records should be ordered.
func (agq *AddressGlobalQuery) Order(o ...addressglobal.OrderOption) *AddressGlobalQuery {
	agq.order = append(agq.order, o...)
	return agq
}

// QueryParcelShopPostNordDelivery chains the current query on the "parcel_shop_post_nord_delivery" edge.
func (agq *AddressGlobalQuery) QueryParcelShopPostNordDelivery() *ParcelShopPostNordQuery {
	query := (&ParcelShopPostNordClient{config: agq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := agq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := agq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(addressglobal.Table, addressglobal.FieldID, selector),
			sqlgraph.To(parcelshoppostnord.Table, parcelshoppostnord.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, addressglobal.ParcelShopPostNordDeliveryTable, addressglobal.ParcelShopPostNordDeliveryColumn),
		)
		fromU = sqlgraph.SetNeighbors(agq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParcelShopBringDelivery chains the current query on the "parcel_shop_bring_delivery" edge.
func (agq *AddressGlobalQuery) QueryParcelShopBringDelivery() *ParcelShopBringQuery {
	query := (&ParcelShopBringClient{config: agq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := agq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := agq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(addressglobal.Table, addressglobal.FieldID, selector),
			sqlgraph.To(parcelshopbring.Table, parcelshopbring.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, addressglobal.ParcelShopBringDeliveryTable, addressglobal.ParcelShopBringDeliveryColumn),
		)
		fromU = sqlgraph.SetNeighbors(agq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParcelShop chains the current query on the "parcel_shop" edge.
func (agq *AddressGlobalQuery) QueryParcelShop() *ParcelShopQuery {
	query := (&ParcelShopClient{config: agq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := agq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := agq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(addressglobal.Table, addressglobal.FieldID, selector),
			sqlgraph.To(parcelshop.Table, parcelshop.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, addressglobal.ParcelShopTable, addressglobal.ParcelShopColumn),
		)
		fromU = sqlgraph.SetNeighbors(agq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCountry chains the current query on the "country" edge.
func (agq *AddressGlobalQuery) QueryCountry() *CountryQuery {
	query := (&CountryClient{config: agq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := agq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := agq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(addressglobal.Table, addressglobal.FieldID, selector),
			sqlgraph.To(country.Table, country.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, addressglobal.CountryTable, addressglobal.CountryColumn),
		)
		fromU = sqlgraph.SetNeighbors(agq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AddressGlobal entity from the query.
// Returns a *NotFoundError when no AddressGlobal was found.
func (agq *AddressGlobalQuery) First(ctx context.Context) (*AddressGlobal, error) {
	nodes, err := agq.Limit(1).All(setContextOp(ctx, agq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{addressglobal.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (agq *AddressGlobalQuery) FirstX(ctx context.Context) *AddressGlobal {
	node, err := agq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AddressGlobal ID from the query.
// Returns a *NotFoundError when no AddressGlobal ID was found.
func (agq *AddressGlobalQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = agq.Limit(1).IDs(setContextOp(ctx, agq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{addressglobal.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (agq *AddressGlobalQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := agq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AddressGlobal entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AddressGlobal entity is found.
// Returns a *NotFoundError when no AddressGlobal entities are found.
func (agq *AddressGlobalQuery) Only(ctx context.Context) (*AddressGlobal, error) {
	nodes, err := agq.Limit(2).All(setContextOp(ctx, agq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{addressglobal.Label}
	default:
		return nil, &NotSingularError{addressglobal.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (agq *AddressGlobalQuery) OnlyX(ctx context.Context) *AddressGlobal {
	node, err := agq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AddressGlobal ID in the query.
// Returns a *NotSingularError when more than one AddressGlobal ID is found.
// Returns a *NotFoundError when no entities are found.
func (agq *AddressGlobalQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = agq.Limit(2).IDs(setContextOp(ctx, agq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{addressglobal.Label}
	default:
		err = &NotSingularError{addressglobal.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (agq *AddressGlobalQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := agq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AddressGlobals.
func (agq *AddressGlobalQuery) All(ctx context.Context) ([]*AddressGlobal, error) {
	ctx = setContextOp(ctx, agq.ctx, ent.OpQueryAll)
	if err := agq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AddressGlobal, *AddressGlobalQuery]()
	return withInterceptors[[]*AddressGlobal](ctx, agq, qr, agq.inters)
}

// AllX is like All, but panics if an error occurs.
func (agq *AddressGlobalQuery) AllX(ctx context.Context) []*AddressGlobal {
	nodes, err := agq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AddressGlobal IDs.
func (agq *AddressGlobalQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if agq.ctx.Unique == nil && agq.path != nil {
		agq.Unique(true)
	}
	ctx = setContextOp(ctx, agq.ctx, ent.OpQueryIDs)
	if err = agq.Select(addressglobal.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (agq *AddressGlobalQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := agq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (agq *AddressGlobalQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, agq.ctx, ent.OpQueryCount)
	if err := agq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, agq, querierCount[*AddressGlobalQuery](), agq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (agq *AddressGlobalQuery) CountX(ctx context.Context) int {
	count, err := agq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (agq *AddressGlobalQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, agq.ctx, ent.OpQueryExist)
	switch _, err := agq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (agq *AddressGlobalQuery) ExistX(ctx context.Context) bool {
	exist, err := agq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AddressGlobalQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (agq *AddressGlobalQuery) Clone() *AddressGlobalQuery {
	if agq == nil {
		return nil
	}
	return &AddressGlobalQuery{
		config:                         agq.config,
		ctx:                            agq.ctx.Clone(),
		order:                          append([]addressglobal.OrderOption{}, agq.order...),
		inters:                         append([]Interceptor{}, agq.inters...),
		predicates:                     append([]predicate.AddressGlobal{}, agq.predicates...),
		withParcelShopPostNordDelivery: agq.withParcelShopPostNordDelivery.Clone(),
		withParcelShopBringDelivery:    agq.withParcelShopBringDelivery.Clone(),
		withParcelShop:                 agq.withParcelShop.Clone(),
		withCountry:                    agq.withCountry.Clone(),
		// clone intermediate query.
		sql:  agq.sql.Clone(),
		path: agq.path,
	}
}

// WithParcelShopPostNordDelivery tells the query-builder to eager-load the nodes that are connected to
// the "parcel_shop_post_nord_delivery" edge. The optional arguments are used to configure the query builder of the edge.
func (agq *AddressGlobalQuery) WithParcelShopPostNordDelivery(opts ...func(*ParcelShopPostNordQuery)) *AddressGlobalQuery {
	query := (&ParcelShopPostNordClient{config: agq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	agq.withParcelShopPostNordDelivery = query
	return agq
}

// WithParcelShopBringDelivery tells the query-builder to eager-load the nodes that are connected to
// the "parcel_shop_bring_delivery" edge. The optional arguments are used to configure the query builder of the edge.
func (agq *AddressGlobalQuery) WithParcelShopBringDelivery(opts ...func(*ParcelShopBringQuery)) *AddressGlobalQuery {
	query := (&ParcelShopBringClient{config: agq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	agq.withParcelShopBringDelivery = query
	return agq
}

// WithParcelShop tells the query-builder to eager-load the nodes that are connected to
// the "parcel_shop" edge. The optional arguments are used to configure the query builder of the edge.
func (agq *AddressGlobalQuery) WithParcelShop(opts ...func(*ParcelShopQuery)) *AddressGlobalQuery {
	query := (&ParcelShopClient{config: agq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	agq.withParcelShop = query
	return agq
}

// WithCountry tells the query-builder to eager-load the nodes that are connected to
// the "country" edge. The optional arguments are used to configure the query builder of the edge.
func (agq *AddressGlobalQuery) WithCountry(opts ...func(*CountryQuery)) *AddressGlobalQuery {
	query := (&CountryClient{config: agq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	agq.withCountry = query
	return agq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UniquenessID string `json:"uniqueness_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AddressGlobal.Query().
//		GroupBy(addressglobal.FieldUniquenessID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (agq *AddressGlobalQuery) GroupBy(field string, fields ...string) *AddressGlobalGroupBy {
	agq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AddressGlobalGroupBy{build: agq}
	grbuild.flds = &agq.ctx.Fields
	grbuild.label = addressglobal.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UniquenessID string `json:"uniqueness_id,omitempty"`
//	}
//
//	client.AddressGlobal.Query().
//		Select(addressglobal.FieldUniquenessID).
//		Scan(ctx, &v)
func (agq *AddressGlobalQuery) Select(fields ...string) *AddressGlobalSelect {
	agq.ctx.Fields = append(agq.ctx.Fields, fields...)
	sbuild := &AddressGlobalSelect{AddressGlobalQuery: agq}
	sbuild.label = addressglobal.Label
	sbuild.flds, sbuild.scan = &agq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AddressGlobalSelect configured with the given aggregations.
func (agq *AddressGlobalQuery) Aggregate(fns ...AggregateFunc) *AddressGlobalSelect {
	return agq.Select().Aggregate(fns...)
}

func (agq *AddressGlobalQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range agq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, agq); err != nil {
				return err
			}
		}
	}
	for _, f := range agq.ctx.Fields {
		if !addressglobal.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if agq.path != nil {
		prev, err := agq.path(ctx)
		if err != nil {
			return err
		}
		agq.sql = prev
	}
	if addressglobal.Policy == nil {
		return errors.New("ent: uninitialized addressglobal.Policy (forgotten import ent/runtime?)")
	}
	if err := addressglobal.Policy.EvalQuery(ctx, agq); err != nil {
		return err
	}
	return nil
}

func (agq *AddressGlobalQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AddressGlobal, error) {
	var (
		nodes       = []*AddressGlobal{}
		withFKs     = agq.withFKs
		_spec       = agq.querySpec()
		loadedTypes = [4]bool{
			agq.withParcelShopPostNordDelivery != nil,
			agq.withParcelShopBringDelivery != nil,
			agq.withParcelShop != nil,
			agq.withCountry != nil,
		}
	)
	if agq.withParcelShopPostNordDelivery != nil || agq.withParcelShopBringDelivery != nil || agq.withParcelShop != nil || agq.withCountry != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, addressglobal.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AddressGlobal).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AddressGlobal{config: agq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(agq.modifiers) > 0 {
		_spec.Modifiers = agq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, agq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := agq.withParcelShopPostNordDelivery; query != nil {
		if err := agq.loadParcelShopPostNordDelivery(ctx, query, nodes, nil,
			func(n *AddressGlobal, e *ParcelShopPostNord) { n.Edges.ParcelShopPostNordDelivery = e }); err != nil {
			return nil, err
		}
	}
	if query := agq.withParcelShopBringDelivery; query != nil {
		if err := agq.loadParcelShopBringDelivery(ctx, query, nodes, nil,
			func(n *AddressGlobal, e *ParcelShopBring) { n.Edges.ParcelShopBringDelivery = e }); err != nil {
			return nil, err
		}
	}
	if query := agq.withParcelShop; query != nil {
		if err := agq.loadParcelShop(ctx, query, nodes, nil,
			func(n *AddressGlobal, e *ParcelShop) { n.Edges.ParcelShop = e }); err != nil {
			return nil, err
		}
	}
	if query := agq.withCountry; query != nil {
		if err := agq.loadCountry(ctx, query, nodes, nil,
			func(n *AddressGlobal, e *Country) { n.Edges.Country = e }); err != nil {
			return nil, err
		}
	}
	for i := range agq.loadTotal {
		if err := agq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (agq *AddressGlobalQuery) loadParcelShopPostNordDelivery(ctx context.Context, query *ParcelShopPostNordQuery, nodes []*AddressGlobal, init func(*AddressGlobal), assign func(*AddressGlobal, *ParcelShopPostNord)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*AddressGlobal)
	for i := range nodes {
		if nodes[i].parcel_shop_post_nord_address_delivery == nil {
			continue
		}
		fk := *nodes[i].parcel_shop_post_nord_address_delivery
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(parcelshoppostnord.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parcel_shop_post_nord_address_delivery" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (agq *AddressGlobalQuery) loadParcelShopBringDelivery(ctx context.Context, query *ParcelShopBringQuery, nodes []*AddressGlobal, init func(*AddressGlobal), assign func(*AddressGlobal, *ParcelShopBring)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*AddressGlobal)
	for i := range nodes {
		if nodes[i].parcel_shop_bring_address_delivery == nil {
			continue
		}
		fk := *nodes[i].parcel_shop_bring_address_delivery
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(parcelshopbring.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parcel_shop_bring_address_delivery" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (agq *AddressGlobalQuery) loadParcelShop(ctx context.Context, query *ParcelShopQuery, nodes []*AddressGlobal, init func(*AddressGlobal), assign func(*AddressGlobal, *ParcelShop)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*AddressGlobal)
	for i := range nodes {
		if nodes[i].parcel_shop_address == nil {
			continue
		}
		fk := *nodes[i].parcel_shop_address
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(parcelshop.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parcel_shop_address" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (agq *AddressGlobalQuery) loadCountry(ctx context.Context, query *CountryQuery, nodes []*AddressGlobal, init func(*AddressGlobal), assign func(*AddressGlobal, *Country)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*AddressGlobal)
	for i := range nodes {
		if nodes[i].address_global_country == nil {
			continue
		}
		fk := *nodes[i].address_global_country
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(country.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "address_global_country" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (agq *AddressGlobalQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := agq.querySpec()
	if len(agq.modifiers) > 0 {
		_spec.Modifiers = agq.modifiers
	}
	_spec.Node.Columns = agq.ctx.Fields
	if len(agq.ctx.Fields) > 0 {
		_spec.Unique = agq.ctx.Unique != nil && *agq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, agq.driver, _spec)
}

func (agq *AddressGlobalQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(addressglobal.Table, addressglobal.Columns, sqlgraph.NewFieldSpec(addressglobal.FieldID, field.TypeString))
	_spec.From = agq.sql
	if unique := agq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if agq.path != nil {
		_spec.Unique = true
	}
	if fields := agq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, addressglobal.FieldID)
		for i := range fields {
			if fields[i] != addressglobal.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := agq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := agq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := agq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := agq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (agq *AddressGlobalQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(agq.driver.Dialect())
	t1 := builder.Table(addressglobal.Table)
	columns := agq.ctx.Fields
	if len(columns) == 0 {
		columns = addressglobal.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if agq.sql != nil {
		selector = agq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if agq.ctx.Unique != nil && *agq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range agq.predicates {
		p(selector)
	}
	for _, p := range agq.order {
		p(selector)
	}
	if offset := agq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := agq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AddressGlobalGroupBy is the group-by builder for AddressGlobal entities.
type AddressGlobalGroupBy struct {
	selector
	build *AddressGlobalQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aggb *AddressGlobalGroupBy) Aggregate(fns ...AggregateFunc) *AddressGlobalGroupBy {
	aggb.fns = append(aggb.fns, fns...)
	return aggb
}

// Scan applies the selector query and scans the result into the given value.
func (aggb *AddressGlobalGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aggb.build.ctx, ent.OpQueryGroupBy)
	if err := aggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AddressGlobalQuery, *AddressGlobalGroupBy](ctx, aggb.build, aggb, aggb.build.inters, v)
}

func (aggb *AddressGlobalGroupBy) sqlScan(ctx context.Context, root *AddressGlobalQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(aggb.fns))
	for _, fn := range aggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*aggb.flds)+len(aggb.fns))
		for _, f := range *aggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*aggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AddressGlobalSelect is the builder for selecting fields of AddressGlobal entities.
type AddressGlobalSelect struct {
	*AddressGlobalQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ags *AddressGlobalSelect) Aggregate(fns ...AggregateFunc) *AddressGlobalSelect {
	ags.fns = append(ags.fns, fns...)
	return ags
}

// Scan applies the selector query and scans the result into the given value.
func (ags *AddressGlobalSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ags.ctx, ent.OpQuerySelect)
	if err := ags.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AddressGlobalQuery, *AddressGlobalSelect](ctx, ags.AddressGlobalQuery, ags, ags.inters, v)
}

func (ags *AddressGlobalSelect) sqlScan(ctx context.Context, root *AddressGlobalQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ags.fns))
	for _, fn := range ags.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ags.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ags.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
