// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"delivrio.io/go/ent/shipment"
	"delivrio.io/go/ent/shipmentdf"
	"delivrio.io/go/ent/tenant"
	"delivrio.io/shared-utils/pulid"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ShipmentDF is the model entity for the ShipmentDF schema.
type ShipmentDF struct {
	config `json:"-"`
	// ID of the ent.
	ID pulid.ID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID pulid.ID `json:"tenant_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShipmentDFQuery when eager-loading is set.
	Edges                ShipmentDFEdges `json:"edges"`
	shipment_shipment_df *pulid.ID
	selectValues         sql.SelectValues
}

// ShipmentDFEdges holds the relations/edges for other nodes in the graph.
type ShipmentDFEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Shipment holds the value of the shipment edge.
	Shipment *Shipment `json:"shipment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentDFEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// ShipmentOrErr returns the Shipment value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShipmentDFEdges) ShipmentOrErr() (*Shipment, error) {
	if e.Shipment != nil {
		return e.Shipment, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: shipment.Label}
	}
	return nil, &NotLoadedError{edge: "shipment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShipmentDF) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shipmentdf.FieldID, shipmentdf.FieldTenantID:
			values[i] = new(pulid.ID)
		case shipmentdf.ForeignKeys[0]: // shipment_shipment_df
			values[i] = &sql.NullScanner{S: new(pulid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShipmentDF fields.
func (sd *ShipmentDF) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shipmentdf.FieldID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sd.ID = *value
			}
		case shipmentdf.FieldTenantID:
			if value, ok := values[i].(*pulid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				sd.TenantID = *value
			}
		case shipmentdf.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field shipment_shipment_df", values[i])
			} else if value.Valid {
				sd.shipment_shipment_df = new(pulid.ID)
				*sd.shipment_shipment_df = *value.S.(*pulid.ID)
			}
		default:
			sd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShipmentDF.
// This includes values selected through modifiers, order, etc.
func (sd *ShipmentDF) Value(name string) (ent.Value, error) {
	return sd.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the ShipmentDF entity.
func (sd *ShipmentDF) QueryTenant() *TenantQuery {
	return NewShipmentDFClient(sd.config).QueryTenant(sd)
}

// QueryShipment queries the "shipment" edge of the ShipmentDF entity.
func (sd *ShipmentDF) QueryShipment() *ShipmentQuery {
	return NewShipmentDFClient(sd.config).QueryShipment(sd)
}

// Update returns a builder for updating this ShipmentDF.
// Note that you need to call ShipmentDF.Unwrap() before calling this method if this ShipmentDF
// was returned from a transaction, and the transaction was committed or rolled back.
func (sd *ShipmentDF) Update() *ShipmentDFUpdateOne {
	return NewShipmentDFClient(sd.config).UpdateOne(sd)
}

// Unwrap unwraps the ShipmentDF entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sd *ShipmentDF) Unwrap() *ShipmentDF {
	_tx, ok := sd.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShipmentDF is not a transactional entity")
	}
	sd.config.driver = _tx.drv
	return sd
}

// String implements the fmt.Stringer.
func (sd *ShipmentDF) String() string {
	var builder strings.Builder
	builder.WriteString("ShipmentDF(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sd.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", sd.TenantID))
	builder.WriteByte(')')
	return builder.String()
}

// ShipmentDFs is a parsable slice of ShipmentDF.
type ShipmentDFs []*ShipmentDF
